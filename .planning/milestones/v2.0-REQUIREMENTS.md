# Requirements Archive: v2.0 pecan_ID + Raw Directory Inventory

**Archived:** 2026-09-24
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# REQUIREMENTS.md — PeCAN Master Dataset Integration

**Project:** PCM | **Version:** 2.0 | **Date:** 2026-09-23

---

## v2.0 Requirements

### Raw Directory Inventory (Phase 19)

- [x] **INV-01** — User can inspect a recursive file listing of `P:\PeCAN Master Data\Gerard\raw` containing path, file name, extension, size, last-modified date, and SHA-256 checksum for every file in the tree; no file is opened for write
- [x] **INV-02** — User can see row count, column count, and sheet name(s) for every readable data file (sas7bdat, csv, xlsx/xls); FILES has one row per file; multi-sheet workbooks are profiled per sheet with row and column counts recorded in a separate SHEETS sheet
- [x] **INV-03** — User can see variable-level inventory (variable name, type, length, label or original header, pct_missing (percent of rows where the value is SAS-missing), pct_sentinel (separate column: percent of rows equal to -999 numeric or 'NULL' string)) for every variable in every readable data file, whether or not it is in the PRECEDE data dictionary
- [x] **INV-04** — User can see which files carry `PRECEDE_STUDY_ID`, `ENCRYPTED_MRN`, or `ENCRYPTED_ENCOUNTER`; matching uses normalized headers with all naming variants enumerated (e.g. "Encrypted MRN", "ENCRYPTED_MRN", positional VARnn names from the XLSX engine) per PCM-T-12 so that key flags cannot miss files
- [x] **INV-05** — User can see which files are known (md1–md8 raw\master mapping or Phase 18 supplemental set) and which are flagged NEW (present in raw but absent from both known sets)
- [x] **INV-06** — User can confirm that every file under `raw` has a resolved status; assertion holds: n_profiled + n_listed_not_profiled + n_read_failed = n_total_files, zero files of unknown status; `read-failed` covers any readable-extension file whose import failed (locked, corrupt, or XLSX sheet-count shortfall)
- [ ] **INV-07** — User can open `qc/19_raw_inventory.xlsx` with KEY sheet leftmost, UF blue (#0021A5) column headers, and the following sheets in order: KEY (legend), FILES, SHEETS, VARIABLES, KEY_COLUMNS, RECONCILIATION, FAMILIES; the KEY sheet is a column-by-column legend describing every field in every other sheet, including a note that `type` in VARIABLES reflects the SAS-imported type (not the source system type); the FAMILIES sheet lists known wide-column families with per-file column counts and missingness range (min, median, max)

### pecan_ID Derivation (Phase 20)

- [x] **PID-01** — User can confirm the pecan_ID source file (CSV in raw\master vs xlsx copy; brief names `2018_2022_X_MASTER_DATASET_20240402`) is identified, checksummed, and its checksum verified against the INV-01 record before Phase 20 reads it; ENCRYPTED_MRN is read as CHARACTER regardless of source typing
- [x] **PID-02** — User can view a source audit: count of blank or placeholder MRNs, count of distinct MRNs, count of distinct PRECEDE_STUDY_IDs, and both-direction cardinalities (PRECEDE→MRN and MRN→PRECEDE)
- [x] **PID-03** — User can confirm that every PRECEDE_STUDY_ID maps to exactly one ENCRYPTED_MRN (program aborts otherwise); MRN→many PRECEDE is expected and reported, not asserted
- [x] **PID-04** — User can confirm crosswalk `g.pecan_id_xwalk` (ENCRYPTED_MRN, pecan_ID) is built per PCM-D-17; if surrogate derivation is chosen, the crosswalk is append-only and existing assignments are never renumbered
- [x] **PID-05** — User can confirm attachment per PCM-D-18, with assertions: row count unchanged in every file that receives pecan_ID (41,150 merged or harmonized per PCM-D-18; 13,890 analytic cohort), zero blank pecan_ID among rows with a non-blank ENCRYPTED_MRN, zero PRECEDE_STUDY_IDs gaining a second pecan_ID after attachment
- [x] **PID-06** — User can see distinct pecan_ID counts in the merged file and the analytic cohort, and the distribution of encounters per pecan_ID (how many patients have 1, 2, 3+ encounters)
- [x] **PID-07** — User can see which raw files carry ENCRYPTED_MRN (from INV-04 key-column flags) and their match rate to `g.pecan_id_xwalk`; output explicitly tests whether r7/r8/r9 (2022 files) link on MRN where they failed on PRECEDE_STUDY_ID (PCM-D-16)
- [x] **PID-08** — User can find pecan_ID with a derivation note in `DATA_DICTIONARY.xlsx`; PCM-D-17 and PCM-D-18 are recorded, attributed, and dated in `DECISIONS.md`

### Runner Wiring & D3 Fix (Phase 21)

- [x] **RUN-01** — User can execute the full pipeline end-to-end via `99_run_all.sas`, covering programs 1–8, 10b, 16b, 17, 18, 19, and 20; each program is a separate batch invocation per PCM-C-05 (restart SAS between programs; exit code 3 on abort per PCM-D-12)
- [x] **FIX-01** — User can regenerate `qc/17_summary_stats_by_domain.xlsx` with COGNITIVE_SCORE and COGNITIVE_CATEGORY assigned to instrument stat_route in the domain lookup DATALINES, applied under the DOMAIN_MAP_APPROVED gate; the regenerated workbook contains a D3 sheet and both cognitive variables appear on it

---

## Future Requirements

- PCM-D-15 extension-column gap-fill wiring into base file — deferred to v2.1; depends on PID-07 result (2022 join key may shift from PRECEDE_STUDY_ID to MRN if r7/r8/r9 link on MRN)

---

## Out of Scope

- REC-05 (PCM-D-07) — Age floor of 64 investigation: upstream inclusion criterion; not pursuing
- Statistical modelling — project ends at analysis-ready file
- INS abstract / dCDT pipeline — separate project
- UTF-8 encoding repair — encoding damage confined to <=9 rows of Base_Procedure_1; flag only (PCM-C-01)

---

## Traceability

| REQ-ID | Phase | Notes |
|--------|-------|-------|
| INV-01 | 19 | Checksum record reused by PID-01 |
| INV-02 | 19 | |
| INV-03 | 19 | |
| INV-04 | 19 | Key-column flags reused by PID-07 |
| INV-05 | 19 | |
| INV-06 | 19 | |
| INV-07 | 19 | |
| PID-01 | 20 | Depends on INV-01 checksum record |
| PID-02 | 20 | |
| PID-03 | 20 | |
| PID-04 | 20 | Depends on PCM-D-17 resolution |
| PID-05 | 20 | Depends on PCM-D-18 resolution |
| PID-06 | 20 | |
| PID-07 | 20 | Depends on INV-04 key-column flags |
| PID-08 | 20 | |
| RUN-01 | 21 | Must follow Phases 19 and 20 (programs 19, 20 included) |
| FIX-01 | 21 | Independent of Phases 19/20; grouped for delivery efficiency |
