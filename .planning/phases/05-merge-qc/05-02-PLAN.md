---
phase: 05-merge-qc
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - qc/05_qc_merge_report.txt
autonomous: false
requirements: [QC-01, QC-02, QC-03, QC-04, QC-05]

must_haves:
  truths:
    - "sas/05_qc_merge.sas passes all static PCM checks before any SAS run (no &SQLOBS, %abort cancel only in macros, no data X;set X;, no PROC SQL UPDATE)"
    - "User can run sas/05_qc_merge.sas in a clean SAS session with P: mapped and see zero ERROR lines"
    - "The log shows every QC ASSERTION OK line: QC-01 (1), QC-02 (2), QC-03 (1), QC-04 (8), QC-05 (8) — 20 OK lines"
    - "qc/05_qc_merge_report.txt exists as a committed plain-text artifact ending in ALL QC CHECKS PASSED"
    - "The report contains counts only (no PRECEDE_STUDY_ID values) so it is safe to commit (no PHI)"
  artifacts:
    - path: "qc/05_qc_merge_report.txt"
      provides: "Committed plain-text QC report: pass/fail per section, QC-04 Part A md8 non-missing counts, QC-05 range-violation counts; ends with ALL QC CHECKS PASSED"
      min_lines: 20
  key_links:
    - from: "sas/05_qc_merge.sas SECTION 2-5 assertions"
      to: "g.master_data_merged"
      via: "PROC SQL SELECT COUNT(*) INTO :macvar TRIMMED; %assert_eq(...)"
      pattern: "assert_eq"
    - from: "sas/05_qc_merge.sas SECTION 1-6"
      to: "qc/05_qc_merge_report.txt"
      via: "FILE ... MOD progressive writes"
      pattern: "05_qc_merge_report\\.txt"
---

<objective>
Static validation of sas/05_qc_merge.sas, then a human-run of the program in SAS, confirming all 20 QC assertions pass and committing qc/05_qc_merge_report.txt.

Purpose: The QC sentinel (05-01) cannot be verified without SAS executing on the machine with P: mapped and g.master_data_merged present. This plan runs the agent-checkable static checks first, then coordinates the SAS run, inspects the log and report, and gates the phase on a clean result.

Output: Confirmed ERROR-free SAS log with 20 QC ASSERTION OK lines; committed qc/05_qc_merge_report.txt ending in ALL QC CHECKS PASSED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-merge-qc/05-01-SUMMARY.md
@.planning/phases/05-merge-qc/05-RESEARCH.md
@qc/04_merge_provenance.txt
@sas/05_qc_merge.sas
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Static validation of sas/05_qc_merge.sas before SAS execution</name>
  <files>sas/05_qc_merge.sas</files>
  <read_first>
    - sas/05_qc_merge.sas (read fully before running any grep checks)
    - qc/03_charvars_all.txt (source of truth for owner widths — use to spot-check the QC-02 reference table covers every character variable in the merged file)
    - qc/04_merge_provenance.txt (in_md8 = 22,473; non-md8 = 18,677 — the QC-04 row-count facts)
    - .planning/phases/05-merge-qc/05-RESEARCH.md (Static Verification Commands section; Pitfalls 1-7)
  </read_first>
  <action>
Run the following static checks against sas/05_qc_merge.sas. For each check that fails, fix the program before proceeding to the human-verify checkpoint. Document each fix in the task output.

CHECK 1 -- Section markers present:
  grep -c "SECTION" sas/05_qc_merge.sas
  Expected: >= 7 (SECTION 0 through SECTION 6).

CHECK 2 -- Assertions coded:
  grep -c "assert_eq" sas/05_qc_merge.sas
  Expected: >= 20 (1 macro def + 1 QC-01 + 2 QC-02 + 1 QC-03 + 8 QC-04 + 8 QC-05 = 21).

CHECK 3 -- QC-01 row count:
  grep -in "expected=41150.*label=QC-01" sas/05_qc_merge.sas   -- expected 1 line.

CHECK 4 -- QC-02 owner widths, not cross-source max (RESEARCH Pitfall 3):
  grep -n "'Emergent', *1," sas/05_qc_merge.sas                -- expected 1 line (md3 $1)
  grep -n "'Emergent', *4," sas/05_qc_merge.sas                -- expected 0 lines (never md8 $4)
  Also confirm the QC-02 completeness guard exists:
  grep -in "label=QC-02 character variables missing" sas/05_qc_merge.sas  -- expected 1 line.

CHECK 5 -- QC-03 scans ALL character variables (RESEARCH Pitfall 1):
  grep -c "_CHARACTER_" sas/05_qc_merge.sas                    -- expected >= 1.
  grep -in "expected=0.*label=QC-03 NULL" sas/05_qc_merge.sas  -- expected 1 line.

CHECK 6 -- QC-04 Part B (eight) and Part A (eight logged):
  grep -c "in_md8 = 0 and" sas/05_qc_merge.sas                 -- expected 8.
  grep -c "in_md8 = 1 and" sas/05_qc_merge.sas                 -- expected 8.
  grep -c "label=QC-04" sas/05_qc_merge.sas                    -- expected 8 (Part B asserted; Part A logged only, RESEARCH Pitfall 2).

CHECK 7 -- QC-05 range guards (RESEARCH Pitfall 4):
  grep -c "is not missing and (" sas/05_qc_merge.sas           -- expected 8 (every range check guarded).
  grep -c "label=QC-05" sas/05_qc_merge.sas                    -- expected 8.

CHECK 8 -- No &SQLOBS:
  grep -in "&SQLOBS" sas/05_qc_merge.sas                       -- expected 0 lines.

CHECK 9 -- %abort cancel inside macros only (PCM-R-05):
  Confirm every `%abort cancel` lies between a `%macro` and its matching `%mend`.

CHECK 10 -- No in-place rewrite (MULTI-LINE — a single-line grep cannot see this, PCM-T-02):
  grep -A2 -i "data g.master_data_merged" sas/05_qc_merge.sas | grep -in "set g.master_data_merged"
  Expected: 0 lines. (There should be no `data g.master_data_merged` at all in Phase 5 — it only READS.)

CHECK 11 -- No PROC SQL UPDATE (MULTI-LINE, PCM-T-01):
  grep -A5 -i "proc sql" sas/05_qc_merge.sas | grep -in "^[[:space:]]*update "
  Expected: 0 lines.

CHECK 12 -- P: g_path (RESEARCH Pitfall 7):
  grep -n "g_path *= *P:\\PeCAN Master Data" sas/05_qc_merge.sas
  Expected: 1 line. If the path is a C: value, g.master_data_merged will not be found. Fix to the
  exact value from sas/04_merge.sas SECTION 0.

CHECK 13 -- Standalone (no %include of 04_merge.sas):
  grep -in "%include" sas/05_qc_merge.sas                      -- expected 0 lines.
  grep -c "%macro assert_eq" sas/05_qc_merge.sas               -- expected 1 (macro defined locally).

CHECK 14 -- Report write is coded and progressive (RESEARCH Pitfall 6):
  grep -c "05_qc_merge_report.txt" sas/05_qc_merge.sas         -- expected >= 4.
  grep -c " mod;" sas/05_qc_merge.sas                          -- expected >= 3 (progressive MOD writes).

CHECK 15 -- QC-02 reference completeness spot-check (manual, using qc/03_charvars_all.txt):
  Read qc/03_charvars_all.txt and confirm every character variable that lands in the merged file
  (owner-resolved) appears in the work.expected_widths reference table in the program at its OWNER
  width. If any is missing, the program's own completeness guard (CHECK 4) will abort at run time —
  but catch it here to save a round trip. Add any missing rows.

If any check fails, fix sas/05_qc_merge.sas before proceeding.
  </action>
  <verify>
    <automated>grep -c "assert_eq" sas/05_qc_merge.sas</automated>
  </verify>
  <acceptance_criteria>
    - All 14 automated static checks pass with no failures (CHECK 15 is a manual completeness review)
    - `grep -c "assert_eq" sas/05_qc_merge.sas` returns >= 20
    - `grep -c "SECTION" sas/05_qc_merge.sas` returns >= 7
    - `grep -in "&SQLOBS" sas/05_qc_merge.sas` returns 0
    - `grep -c "in_md8 = 0 and" sas/05_qc_merge.sas` returns 8
    - `grep -c "is not missing and (" sas/05_qc_merge.sas` returns 8
    - `grep -n "'Emergent', *4," sas/05_qc_merge.sas` returns 0 lines (owner width $1, not md8 $4)
    - `grep -n "g_path *= *P:\\\\PeCAN Master Data" sas/05_qc_merge.sas` returns 1 line
    - the multi-line PROC SQL UPDATE (CHECK 11) and in-place-rewrite (CHECK 10) checks both return 0
  </acceptance_criteria>
  <done>All 14 static checks pass; sas/05_qc_merge.sas is free of PCM violations, uses the P: g_path, is standalone, scans all char vars for QC-03, guards QC-05 ranges, and writes the report progressively.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification -- run sas/05_qc_merge.sas and confirm all 20 QC assertions pass</name>
  <files>qc/05_qc_merge_report.txt (produced by the SAS run; committed by this task)</files>
  <action>Run sas/05_qc_merge.sas in a SAS 9.4 session with the P: drive mapped, then confirm the log is ERROR-free with 20 QC ASSERTION OK lines, review the QC-04 Part A md8 non-missing counts in qc/05_qc_merge_report.txt, confirm the report ends in ALL QC CHECKS PASSED and holds counts only (no PHI), and commit qc/05_qc_merge_report.txt. See how-to-verify for exact steps and failure diagnosis.</action>
  <what-built>
    sas/05_qc_merge.sas: the complete Phase 5 QC sentinel (SECTION 0 through SECTION 6).
    When run against g.master_data_merged it: (1) checks preconditions, (2) asserts row count = 41150
    (QC-01) and no truncated character widths (QC-02), (3) scans all character variables for literal
    NULL (QC-03), (4) asserts the eight md8 hemodynamic numerics are missing outside md8 rows and logs
    their md8 non-missing counts (QC-04), (5) asserts the eight numerics are within clinical ranges
    (QC-05), and writes qc/05_qc_merge_report.txt progressively.
  </what-built>
  <how-to-verify>
    Step 1: Ensure the P: drive is mapped (required for the g library and g.master_data_merged).

    Step 2: Submit sas/05_qc_merge.sas in a clean SAS 9.4 session:
      Option A (batch): sas -sysin "C:\Master_Renamed_same_format_accross\sas\05_qc_merge.sas" -log "C:\Master_Renamed_same_format_accross\logs\05_qc_merge.log"
      Option B (interactive): Open SAS, File > Open > sas/05_qc_merge.sas > Run.

    Step 3: Check the SAS log for ERROR lines:
      grep -i "^ERROR" logs/05_qc_merge.log
      Expected: zero matches. Any ERROR line is a failure.

    Step 4: Confirm all 20 assertions passed:
      grep "QC ASSERTION" logs/05_qc_merge.log
      Expected: every line contains "QC ASSERTION OK"; no "QC ASSERTION FAILED" lines.
      You should see these 20 OK lines:
        NOTE: QC ASSERTION OK -- QC-01 merged row count = 41150
        NOTE: QC ASSERTION OK -- QC-02 character variables missing from width reference = 0
        NOTE: QC ASSERTION OK -- QC-02 truncated character variables = 0
        NOTE: QC ASSERTION OK -- QC-03 NULL strings in any character variable = 0
        NOTE: QC ASSERTION OK -- QC-04 Admit_BMI non-missing outside md8 rows = 0
        NOTE: QC ASSERTION OK -- QC-04 ASA__Anesth_Record_ non-missing outside md8 rows = 0
        NOTE: QC ASSERTION OK -- QC-04 Age_at_Encounter non-missing outside md8 rows = 0
        NOTE: QC ASSERTION OK -- QC-04 Cognitive_Score non-missing outside md8 rows = 0
        NOTE: QC ASSERTION OK -- QC-04 Frailty_Score non-missing outside md8 rows = 0
        NOTE: QC ASSERTION OK -- QC-04 rt_INCISE_to_DRESS_mins non-missing outside md8 rows = 0
        NOTE: QC ASSERTION OK -- QC-04 rt_RM_START_to_INCISION_mins non-missing outside md8 rows = 0
        NOTE: QC ASSERTION OK -- QC-04 rt_RM_START_to_RM_END_mins non-missing outside md8 rows = 0
        NOTE: QC ASSERTION OK -- QC-05 Admit_BMI out of range 10-100 = 0
        NOTE: QC ASSERTION OK -- QC-05 ASA__Anesth_Record_ out of range 1-6 = 0
        NOTE: QC ASSERTION OK -- QC-05 Age_at_Encounter out of range 18-120 = 0
        NOTE: QC ASSERTION OK -- QC-05 Cognitive_Score out of range 0-30 = 0
        NOTE: QC ASSERTION OK -- QC-05 Frailty_Score out of range 0-5 = 0
        NOTE: QC ASSERTION OK -- QC-05 rt_INCISE_to_DRESS_mins out of range 0-2000 = 0
        NOTE: QC ASSERTION OK -- QC-05 rt_RM_START_to_INCISION_mins out of range 0-500 = 0
        NOTE: QC ASSERTION OK -- QC-05 rt_RM_START_to_RM_END_mins out of range 0-2000 = 0

    Step 5: Confirm the Phase 5 completion marker:
      grep "Phase 5 QC complete" logs/05_qc_merge.log   -- expected 1 match.

    Step 6: Review the QC-04 Part A counts in the report (informational, human judgment):
      type qc\05_qc_merge_report.txt   (or: cat qc/05_qc_merge_report.txt)
      The Part A section lists the md8 non-missing count for each of the eight hemodynamic variables
      against the 22,473 md8 rows. These may legitimately fall short of 22,473 if a md8 patient had a
      missing source value (RESEARCH Pitfall 2). Confirm any shortfall is explainable; a count that is
      wildly low (e.g., 0) signals a conversion problem worth investigating.

    Step 7: Confirm the report ends in the pass footer:
      grep "ALL QC CHECKS PASSED" qc/05_qc_merge_report.txt   -- expected 1 match.

    Step 8: Confirm the report holds counts only (no PHI) before committing:
      Open qc/05_qc_merge_report.txt and confirm there are NO PRECEDE_STUDY_ID values or MRNs — only
      variable names, counts, and PASS labels.

    Step 9: Commit the report:
      git add qc/05_qc_merge_report.txt
      git commit -m "qc(05-merge-qc): add post-merge QC report artifact"

    If any assertion fails, report which one and the actual value observed.
    Common failure modes and diagnosis:
      - "QC-01 merged row count: expected 41150 got N" --> wrong dataset or g.master_data_merged was
        rebuilt; re-run Phase 4 and re-check qc/04_merge_provenance.txt.
      - "QC-02 character variables missing from width reference: expected 0 got N" --> the
        work.expected_widths table does not cover all character variables; add the N missing variables
        at their OWNER width from qc/03_charvars_all.txt (Plan 01 Task 1).
      - "QC-02 truncated character variables: expected 0 got N" --> a merged column is NARROWER than its
        owner width; this is a real truncation from the merge and must be fixed in Phase 4, not masked here.
      - "QC-03 NULL strings ... got N" --> a literal NULL survived from a source; identify the column via a
        PROC FREQ and trace back to the owning prep program.
      - "QC-04 <var> non-missing outside md8 rows: expected 0 got N" --> a non-md8 source populated a
        hemodynamic variable (ownership bleed); check the Phase 4 KEEP= lists.
      - "QC-05 <var> out of range ... got N" --> inspect the out-of-range rows; decide whether the range
        ceiling is too tight or the data has a genuine problem, adjust and document in docs/DECISIONS.md
        before re-running (RESEARCH Pattern 6 — an abort here is a flag for human investigation).
  </how-to-verify>
  <resume-signal>Type "approved" if all 20 QC assertions passed, the report ends in ALL QC CHECKS PASSED, and qc/05_qc_merge_report.txt is committed. Or describe which assertion failed and the actual value seen.</resume-signal>
</task>

</tasks>

<verification>
Phase 5 is complete when:
1. logs/05_qc_merge.log exists with zero ERROR lines and 20 "QC ASSERTION OK" lines
2. qc/05_qc_merge_report.txt exists, ends in "ALL QC CHECKS PASSED", holds counts only, and is committed to git
3. grep "Phase 5 QC complete" logs/05_qc_merge.log returns 1 match
4. QC-04 Part A md8 non-missing counts in the report have been reviewed and any shortfall is explainable
</verification>

<success_criteria>
- SAS log is ERROR-free
- All 20 QC assertions pass (QC-01 x1, QC-02 x2, QC-03 x1, QC-04 x8, QC-05 x8)
- qc/05_qc_merge_report.txt committed with per-section results and Part A counts, ending in ALL QC CHECKS PASSED
- QC-04 Part A md8 non-missing counts reviewed by human (informational, not asserted)
- Report contains no PHI (counts only)
</success_criteria>

<output>
After completion, create .planning/phases/05-merge-qc/05-02-SUMMARY.md
</output>
