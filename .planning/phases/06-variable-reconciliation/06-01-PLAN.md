---
phase: 06-variable-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DECISIONS.md
autonomous: false
requirements:
  - PCM-D-10
  - REC-06
must_haves:
  truths:
    - "PREP-09 report logs/03_negtime_md3.txt has been read and every rt_* variable's negative count is accounted for"
    - "rt_ANCHOR_to_ADMIT_days, rt_ANCHOR_to_SURGERY_days, rt_ANCHOR_to_DISCHG_days negatives are explicitly declared legitimate and left unmodified"
    - "Any rt_*_mins duration with negatives beyond the three PREP-08 variables is flagged for a Phase 3->4->5 re-run decision by a human"
    - "PCM-D-10 has a resolved entry in docs/DECISIONS.md naming the anchor-offset variables"
  artifacts:
    - path: "docs/DECISIONS.md"
      provides: "PCM-D-10 resolution entry with per-variable triage from PREP-09 report"
      contains: "PCM-D-10"
  key_links:
    - from: "logs/03_negtime_md3.txt"
      to: "docs/DECISIONS.md PCM-D-10 entry"
      via: "human reads report, transcribes triage decision"
      pattern: "PCM-D-10"
---

<objective>
Triage the PCM-D-10 open item: negatives in rt_* variables OTHER than the three PREP-08
already null (rt_INCISE_to_DRESS_mins, rt_RM_START_to_INCISION_mins, rt_RM_START_to_RM_END_mins).
Read the PREP-09 report (logs/03_negtime_md3.txt), apply the documented triage rule, and
record the resolution in docs/DECISIONS.md.

Purpose: PCM-D-10 is the ONLY open technical decision in Phase 6. Everything else is
documentation of already-resolved decisions. This plan closes it on evidence, not on
pattern-matching the rt_ prefix.
Output: A resolved PCM-D-10 entry in docs/DECISIONS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/AMENDMENT-01-timestamp-integrity.md
@.planning/phases/06-variable-reconciliation/06-RESEARCH.md
@docs/DECISIONS.md
</context>

<critical_facts>
- rt_ANCHOR_to_ADMIT_days, rt_ANCHOR_to_SURGERY_days, rt_ANCHOR_to_DISCHG_days are OFFSETS
  from an anchor date, NOT durations. Negative values are LEGITIMATE (an event preceding
  the anchor). These MUST NEVER be nulled.
- PREP-08 already nulled negatives in exactly three variables: rt_INCISE_to_DRESS_mins (52),
  rt_RM_START_to_INCISION_mins (15), rt_RM_START_to_RM_END_mins (0). Do not re-null these.
- Any OTHER rt_*_mins duration variable showing negatives is a new finding requiring a
  human decision, because nulling it triggers a full Phase 3 -> 4 -> 5 re-run (see AMENDMENT-01).
- The full rt_* variable list is in docs/DECISIONS.md OWN-03 block (lines RT_ADMIT_TO_*
  through RT_RM_START_TO_RM_END_MINS). rt_ANCHOR_TO_ADMIT_DAYS, RT_ANCHOR_TO_DISCHG_DAYS,
  RT_ANCHOR_TO_SURGERY_DAYS are the three anchor-offset variables.
- PCM-T-11: an unguarded `var < 0` in SAS counts missings as negatives. Any counts quoted
  in the report must have come from an `IS NOT MISSING AND var < 0` guarded query.
</critical_facts>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Read PREP-09 report and triage rt_* negatives</name>
  <read_first>
    - logs/03_negtime_md3.txt (the PREP-09 report — the authoritative per-variable negative count)
    - .planning/AMENDMENT-01-timestamp-integrity.md (PREP-08 vs PREP-09 scope, PCM-T-11 guard)
    - .planning/phases/06-variable-reconciliation/06-RESEARCH.md (the PCM-D-10 triage rule, lines 91-110)
    - docs/DECISIONS.md (OWN-03 block — the full rt_* variable list)
  </read_first>
  <what-built>
    Nothing automated yet — this is a human-judgment triage. The executor prepares the
    triage table from logs/03_negtime_md3.txt for human review.
  </what-built>
  <how-to-verify>
    1. Confirm logs/03_negtime_md3.txt exists. If it does NOT exist, STOP — 03-06 (PREP-09)
       has not run yet and PCM-D-10 cannot be triaged. Report this as a blocker.
    2. Read logs/03_negtime_md3.txt. For EACH rt_* variable listed, note its negative count.
    3. Classify each variable into exactly one bucket:
       - BUCKET A (anchor offsets, negatives EXPECTED): rt_ANCHOR_to_ADMIT_days,
         rt_ANCHOR_to_SURGERY_days, rt_ANCHOR_to_DISCHG_days — negatives are legitimate, NO action.
       - BUCKET B (already handled by PREP-08): rt_INCISE_to_DRESS_mins,
         rt_RM_START_to_INCISION_mins, rt_RM_START_to_RM_END_mins — already nulled, NO new action.
       - BUCKET C (zero negatives): any rt_*_mins with count 0 — record "zero negatives confirmed".
       - BUCKET D (NEW negatives found in a duration): any rt_*_mins NOT in bucket B with a
         non-zero guarded negative count — THIS REQUIRES A HUMAN DECISION.
    4. Present the classified table to the human.
    5. If ANY variable falls in BUCKET D: flag loudly that a Phase 3 -> 4 -> 5 re-run may be
       needed (same chain as AMENDMENT-01). The human decides: null (re-run) or retain-with-doc.
    6. If NO variable falls in BUCKET D: state "PCM-D-10 resolves clean — only anchor offsets
       and already-handled durations carry negatives."
  </how-to-verify>
  <action>
    Read logs/03_negtime_md3.txt. Build a triage table with columns: Variable | Negative count
    (guarded) | Bucket (A/B/C/D) | Action. Apply the bucketing rule in how-to-verify verbatim.
    Do NOT modify any dataset in this task. Do NOT null any variable. Present the table and the
    bucket-D determination to the human for sign-off before proceeding to Task 2.
  </action>
  <acceptance_criteria>
    - logs/03_negtime_md3.txt was read (its contents are quoted in the checkpoint output)
    - Every rt_* variable in the report is assigned exactly one bucket (A, B, C, or D)
    - rt_ANCHOR_to_ADMIT_days, rt_ANCHOR_to_SURGERY_days, rt_ANCHOR_to_DISCHG_days are all in bucket A
    - If any bucket-D variable exists, the checkpoint output contains the string "Phase 3 -> 4 -> 5 re-run"
    - Human has signed off on the triage table
  </acceptance_criteria>
  <resume-signal>Type "approved" with the bucket-D decision (null-and-rerun / retain-with-doc / none found), or describe issues</resume-signal>
  <done>Human has reviewed the triage table and made a decision for any bucket-D variables (or confirmed none exist)</done>
</task>

<task type="auto">
  <name>Task 2: Record PCM-D-10 resolution in docs/DECISIONS.md</name>
  <read_first>
    - docs/DECISIONS.md (current state — the pending table row 21 and the "Resolutions -- 2026-08-27" section; DO NOT edit the OWN-03 generated block)
    - logs/03_negtime_md3.txt (to transcribe exact per-variable counts into the entry)
  </read_first>
  <action>
    Two edits to docs/DECISIONS.md, ASCII only (session encoding is not UTF-8):

    EDIT 1 — Update the pending-decisions table row for PCM-D-10 (currently line ~21):
    Change:
      `| PCM-D-10 | Negatives in other rt_* variables | Pending -- needs the PREP-09 report | Gerard |`
    To:
      `| PCM-D-10 | Negatives in other rt_* variables | **Resolved 2026-08-27 -- see entry below** | Gerard |`

    EDIT 2 — Append a new resolution section at the end of the "Resolutions -- 2026-08-27"
    block (immediately before the "## Standing note for the data dictionary" heading, so it
    does NOT land inside the OWN-03 generated block). Use this exact heading and structure,
    filling COUNTS and the BUCKET-D DECISION from Task 1:

    ```
    ### PCM-D-10 -- Negatives in other rt_* variables: TRIAGED FROM PREP-09

    The PREP-09 report (logs/03_negtime_md3.txt) was scanned for negative values in every
    rt_* variable other than the three PREP-08 already nulls. Counts are guarded
    (IS NOT MISSING AND var < 0, per PCM-T-11).

    **Anchor offsets -- negatives are LEGITIMATE, NOT nulled:**
    rt_ANCHOR_to_ADMIT_days, rt_ANCHOR_to_SURGERY_days, rt_ANCHOR_to_DISCHG_days are offsets
    from an anchor date, not durations. A negative value means the event preceded the anchor,
    which is meaningful. Report counts: [transcribe from report]. NO ACTION -- do not null.

    **Duration variables (rt_*_mins) other than the three PREP-08 handles:**
    [transcribe each with its guarded negative count]. Resolution: [none had negatives / the
    following had negatives and the decision was X].

    **Bucket-D decision (if any duration outside PREP-08 showed negatives):**
    [Either: "None found -- PCM-D-10 closes clean." OR the human's decision from Task 1 and
    whether a Phase 3 -> 4 -> 5 re-run was triggered.]

    **Resolved:** 2026-08-27 | Owner: Gerard | Phase 6 Plan 01
    ```
  </action>
  <verify>
    <automated>grep "TRIAGED FROM PREP-09" docs/DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TRIAGED FROM PREP-09" docs/DECISIONS.md` returns a match
    - `grep "rt_ANCHOR_to_ADMIT_days" docs/DECISIONS.md` returns a match (anchor offsets named)
    - `grep "Resolved 2026-08-27 -- see entry below" docs/DECISIONS.md` returns a match (pending-table row updated)
    - The OWN-03 generated block (from `<!-- generated by 02_ownership.sas` to end of file) is byte-for-byte unchanged
    - No non-ASCII characters introduced: `grep -P "[^\x00-\x7F]" docs/DECISIONS.md` returns nothing
  </acceptance_criteria>
  <done>docs/DECISIONS.md contains a PCM-D-10 resolution entry naming the anchor-offset variables and recording the PREP-09 triage; the pending-table row shows resolved; the OWN-03 block is untouched</done>
</task>

</tasks>

<verification>
- logs/03_negtime_md3.txt was read and every rt_* variable triaged into a bucket
- docs/DECISIONS.md has a resolved PCM-D-10 entry
- Anchor-offset variables explicitly named as legitimate-negative, not nulled
- No dataset was modified in this plan
- OWN-03 generated block untouched
</verification>

<success_criteria>
- `grep "TRIAGED FROM PREP-09" docs/DECISIONS.md` matches
- `grep "rt_ANCHOR_to_ADMIT_days" docs/DECISIONS.md` matches
- If a bucket-D duration existed, the human decision (null-and-rerun vs retain) is recorded verbatim
- No non-ASCII bytes in docs/DECISIONS.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-variable-reconciliation/06-01-SUMMARY.md`
</output>
