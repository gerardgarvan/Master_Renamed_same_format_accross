---
phase: 06-variable-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - sas/06_reconcile.sas
  - qc/06_reconcile_summary.txt
  - logs/06_reconcile.log
# NOTE: mixed plan -- Task 1 is auto, Task 2 is a checkpoint:human-verify gate;
# autonomous is false because a human sign-off is required to run the SAS program and commit the QC summary.
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - MRG-05
  - REC-01
  - REC-02
  - REC-03
  - REC-04
must_haves:
  truths:
    - "06_reconcile.sas runs against g.master_data_merged read-only and produces qc/06_reconcile_summary.txt"
    - "All 16 deliberately-separate columns are confirmed present in the merged file (3 mortality + 10 frailty + 3 ISO_SEV — and Emergent + rt_envelope_flag also confirmed)"
    - "Emergent Y/N/missing counts are documented (D-04) using Y/N -- NOT 1/0, which returns zero for both"
    - "Six deliberate columns have their non-missing counts reported, so a present-but-empty column is visible"
    - "rt_envelope_flag = 0 and = 1 distribution is documented, not hard-asserted (MRG-05)"
    - "The program modifies no dataset — reads g.master_data_merged only"
  artifacts:
    - path: "sas/06_reconcile.sas"
      provides: "Read-only reconciliation QC program with column-presence, Emergent, and rt_envelope_flag sections"
      min_lines: 80
      contains: "dictionary.columns"
    - path: "qc/06_reconcile_summary.txt"
      provides: "Committed prose QC summary of the reconciliation phase"
      contains: "rt_envelope_flag"
  key_links:
    - from: "sas/06_reconcile.sas"
      to: "g.master_data_merged"
      via: "PROC SQL read against dictionary.columns and the merged dataset"
      pattern: "g\\.master_data_merged|MASTER_DATA_MERGED"
    - from: "sas/06_reconcile.sas"
      to: "qc/06_reconcile_summary.txt"
      via: "FILE / PUT DATA _NULL_ pattern"
      pattern: "file .*06_reconcile_summary"
---

<objective>
Write and run sas/06_reconcile.sas: a read-only program that confirms the 16 deliberately-separate
columns exist in g.master_data_merged, counts the Emergent distribution (D-04), documents the
rt_envelope_flag distribution (MRG-05), and writes a committed prose QC summary to
qc/06_reconcile_summary.txt.

Purpose: gives Phase 6 an executable artifact that fits 99_run_all.sas, produces a committed QC
report, and proves the keep-separate resolutions (D-01/D-02/D-03) landed as actual columns —
without touching any read-only source or modifying the merged dataset.
Output: sas/06_reconcile.sas, qc/06_reconcile_summary.txt, logs/06_reconcile.log
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-variable-reconciliation/06-RESEARCH.md
@.planning/phases/06-variable-reconciliation/06-VALIDATION.md
@docs/DECISIONS.md
</context>

<interfaces>
The 16 deliberately-separate columns to confirm present (exact names, from docs/DECISIONS.md
OWN-03 block and the Standing Note):

Mortality (D-01): Death_Date_Y_N, IsDead_Y_N, Death
Frailty char (D-02): Feels_Exausted, Low_Physical_Activity, Slow_Walking_Speed,
  Unintended_Weight_Loss, Week_Grip_Strength
Frailty numeric (D-02): Feels_Exausted_Value, Low_Physical_Activity_Value,
  Slow_Walking_Speed_Value, Unintended_Weight_Loss_Value, Week_Grip_Strength_Value
ISO_SEV (D-03): ISO_SEV_Exp_IntraOp_MAC_Average, ISO_SEV_IntraOp_MAC_Average, ISO_SEV_MAC_TOTAL_Exp

Also confirm present and report on:
  Emergent (D-04) — count positive/zero/missing
  rt_envelope_flag (MRG-05) — count = 0 and = 1

NOTE: IsDead_Y_N and Death are NOT in the OWN-03 block by those exact names — IsDead_Y_N is
md6-owned and Death is md7-owned. The presence check IS the point: it proves the keep-separate
merge landed all three. If a column is genuinely absent, the program must abort loudly (that is
a real finding, not a program defect).

Established SAS conventions (from prior phases — replicate exactly):
- `%abort cancel` on any assertion failure
- `select count(*) into :macvar trimmed` — NEVER &SQLOBS (PCM-T-11)
- `IS NOT MISSING and var ...` guard on every numeric comparison (PCM-T-11)
- `dictionary.columns` with `libname='G'` and `upcase(memname)='MASTER_DATA_MERGED'` for schema checks
- `filename ... ; file ...; put ...;` DATA _NULL_ pattern for the summary output
- libname g points at the P: merge tree (same LIBNAME preamble as 05_qc_merge.sas)
</interfaces>

<critical_facts>
- READ ONLY: 06_reconcile.sas must NOT modify g.master_data_merged, must NOT call any prep
  program, must NOT write a SAS dataset to g. It reads and writes only qc/06_reconcile_summary.txt.
- Do NOT use `data X; set X;` (PCM-T-02). Do NOT use PROC SQL UPDATE (PCM-T-01).
- Do NOT hard-assert rt_envelope_flag = 1 count equals 9. That is a current observation, not a
  pipeline invariant (RESEARCH Pitfall 4). Log it; assert nothing on the flagged count.
- Emergent type is unknown (char vs numeric). Read dictionary.columns for its type BEFORE the
  count query, and branch: `= '1'`/`= '0'` for char, `= 1`/`= 0` for numeric.
- Phase 6 reads g.master_data_merged, so it assumes Phase 4 and Phase 5 are CURRENT. If the
  AMENDMENT-01 re-run chain has not completed, the merged file may lack rt_envelope_flag — the
  presence check will catch that.
</critical_facts>

<tasks>

<task type="auto">
  <name>Task 1: Write sas/06_reconcile.sas</name>
  <read_first>
    - sas/05_qc_merge.sas (the reference implementation — copy its SECTION 0 preconditions, LIBNAME g preamble, assert macro style, and FILE/PUT summary pattern)
    - docs/DECISIONS.md (exact 16 column names in the OWN-03 block and Standing Note; Emergent and rt_* rows)
    - .planning/phases/06-variable-reconciliation/06-RESEARCH.md (SECTION-by-SECTION architecture, lines 114-190; code examples, lines 340-391)
  </read_first>
  <action>
    Create sas/06_reconcile.sas with these sections. Match sas/05_qc_merge.sas conventions exactly.

    SECTION 0 — Preconditions:
      - LIBNAME g against the P: merge path (copy the exact preamble from 05_qc_merge.sas).
      - Assert g library accessible; abort cancel if not.
      - Assert g.master_data_merged exists (PROC SQL count against dictionary.tables or
        dictionary.members); abort cancel if not.
      - Define filename for logs and qc output paths.

    SECTION 1 — Column presence checks (D-01, D-02, D-03, and confirm Emergent + rt_envelope_flag):
      Define this macro (from RESEARCH lines 344-358):
        %macro assert_col(lib=, dsn=, col=, label=);
          %local n;
          proc sql noprint;
            select count(*) into :n trimmed
            from dictionary.columns
            where libname = upcase("&lib")
              and upcase(memname) = upcase("&dsn")
              and upcase(name) = upcase("&col");
          quit;
          %if &n = 0 %then %do;
            %put ERROR: &label -- &col absent from &lib..&dsn;
            %abort cancel;
          %end;
          %else %put NOTE: &label PASS -- &col present in &lib..&dsn;
        %mend assert_col;
      Then call it for all 16 columns plus Emergent and rt_envelope_flag, tagging labels:
        D-01: Death_Date_Y_N, IsDead_Y_N, Death
        D-02: Feels_Exausted, Low_Physical_Activity, Slow_Walking_Speed, Unintended_Weight_Loss,
              Week_Grip_Strength, Feels_Exausted_Value, Low_Physical_Activity_Value,
              Slow_Walking_Speed_Value, Unintended_Weight_Loss_Value, Week_Grip_Strength_Value
        D-03: ISO_SEV_Exp_IntraOp_MAC_Average, ISO_SEV_IntraOp_MAC_Average, ISO_SEV_MAC_TOTAL_Exp
        D-04: Emergent
        MRG-05: rt_envelope_flag
      Each %put on success must contain the literal substring "PASS" so the log grep works.

    SECTION 2 — Emergent counts (D-04):
      `Emergent` is `Y` / `N` / blank (PCM-F-06) -- NOT `1` / `0`. It is definitively
      CHARACTER `$1`: md3 owns it and Phase 4's LENGTH block declares it. NO type branching.

      An earlier draft coded `Emergent = '1'` / `= '0'`, which returns 0 for both and writes
      "0 positive / 0 zero" into the summary -- a coding error that reads like a finding.

      ```sas
      proc sql noprint;
        select sum(Emergent is not missing and upcase(Emergent) = 'Y') into :n_emergent_y    trimmed,
               sum(Emergent is not missing and upcase(Emergent) = 'N') into :n_emergent_n    trimmed,
               sum(missing(Emergent))                                  into :n_emergent_miss trimmed
        from g.master_data_merged;
      quit;
      ```
      %put NOTE: REC-04/D-04 -- Emergent: &n_emergent_y Y, &n_emergent_n N, &n_emergent_miss missing;

      Report the counts; assert nothing. There is no expected value: the merged `Emergent`
      comes from md3, whose distribution has never been measured. The 0.05% / 0.09% figures in
      PCM-F-06 are md1 and md8 SOURCE rates and do not describe this column.

    SECTION 3 — rt_envelope_flag distribution (MRG-05):
      Use the guarded pattern (RESEARCH lines 383-390):
        proc sql noprint;
          select sum(rt_envelope_flag = 0) into :n_flag0 trimmed,
                 sum(rt_envelope_flag = 1) into :n_flag1 trimmed
          from g.master_data_merged
          where rt_envelope_flag is not missing;
        quit;
      %put NOTE: MRG-05 -- rt_envelope_flag=0: &n_flag0, rt_envelope_flag=1: &n_flag1;
      Do NOT assert &n_flag1 = 9. Comment: /* observation, not an invariant -- see PCM-D-08 */

    SECTION 3b — Populated check for the deliberate columns (REPORT ONLY, no assertion):
      Presence proves a column EXISTS; it does not prove anything landed in it. A stale merged
      file or a broken keep list produces a column that is present and entirely missing -- the
      one failure mode SECTION 1 structurally cannot see.

      ```sas
      proc sql noprint;
        select sum(Death_Date_Y_N is not missing) into :n_pop_dd trimmed,
               sum(IsDead_Y_N     is not missing) into :n_pop_id trimmed,
               sum(Death          is not missing) into :n_pop_dt trimmed,
               sum(Feels_Exausted is not missing) into :n_pop_fe trimmed,
               sum(Feels_Exausted_Value is not missing) into :n_pop_fev trimmed,
               sum(ISO_SEV_MAC_TOTAL_Exp is not missing) into :n_pop_iso trimmed
        from g.master_data_merged;
      quit;
      ```
      Rough expectations from source row counts -- report, do not assert:
        Death_Date_Y_N ~41,150 (md3-owned)   IsDead_Y_N ~9,462 (md6)   Death ~9,215 (md7)
        Feels_Exausted ~9,215 (md7)          Feels_Exausted_Value ~41,150 (md3)
        ISO_SEV_MAC_TOTAL_Exp ~22,473 (md8)
      **A count of 0 on any of these is a real finding** -- the column landed empty, meaning the
      keep-separate resolution did not actually take. Flag it loudly in the log and the summary,
      but do not abort: the reviewer needs the whole picture, not the first failure.

    SECTION 4 — PCM-D-10 confirmation note (read-only):
      Write a fixed note into the summary that anchor-offset variables (rt_ANCHOR_to_ADMIT_days,
      rt_ANCHOR_to_SURGERY_days, rt_ANCHOR_to_DISCHG_days) legitimately carry negatives and were
      NOT nulled (cross-reference docs/DECISIONS.md PCM-D-10). No data modification.

    SECTION 5 — Write summary and close:
      filename outref "&qc_path.\06_reconcile_summary.txt";
      data _null_; file outref; ... put ...; run;
      The summary MUST contain, as plain lines:
        - Run timestamp (use %sysfunc(datetime()) formatted, or &sysdate9. &systime.)
        - "Column presence: 16 deliberate columns + Emergent + rt_envelope_flag all PASS"
          (or the failing column name)
        - "Emergent: <n_y> Y / <n_n> N / <miss> missing" (Y/N/blank, NOT 1/0)
        - "rt_envelope_flag: <n0> at 0, <n1> at 1 (flagged count is an observation, not asserted)"
        - A "Populated counts" block listing the six spot-checked columns with their non-missing
          counts and the rough expectation, plus the line: "A count of 0 means the column landed
          empty -- the keep-separate resolution did not take. Presence alone cannot detect this."
        - "rt_envelope_flag" appears as a literal token (acceptance grep depends on it)
        - "PCM-D-10: anchor-offset rt_ANCHOR_to_*_days negatives are legitimate, not nulled"
        - The Emergent caveat, worded so it describes THIS column rather than the sources:
          "Emergent caveat (PCM-D-04): the merged value is md3-owned. At source, md1 and md8
           showed 0.05% and 0.09% Y with matching blank shares -- consistent with a field
           clinicians rarely complete rather than a true emergency rate. Patient_Type and
           Admit_Source are likely better urgency proxies. Do not model on Emergent without
           checking the observed rate above first."
          Do NOT write "7 and 21 positives" as though it described the merged column -- those
          were md1 and md8 source counts, and the merged column comes from md3.
  </action>
  <verify>
    <automated>grep -c "dictionary.columns" sas/06_reconcile.sas</automated>
  </verify>
  <acceptance_criteria>
    - sas/06_reconcile.sas exists and is at least 80 lines
    - `grep "%macro assert_col" sas/06_reconcile.sas` matches
    - All 18 presence checks present: `grep -c "%assert_col" sas/06_reconcile.sas` returns 18
    - `grep "rt_envelope_flag" sas/06_reconcile.sas` matches AND the file does NOT contain a hard assertion `n_flag1 = 9` / `assert_eq(9` on the flagged count (`grep -i "flag1.*=.*9\|assert.*9" sas/06_reconcile.sas` returns nothing)
    - `grep "is not missing" sas/06_reconcile.sas` matches (PCM-T-11 guard present)
    - `grep "upcase(Emergent) = 'Y'" sas/06_reconcile.sas` matches AND `grep "Emergent = '1'" sas/06_reconcile.sas` returns NOTHING (Emergent is Y/N/blank, PCM-F-06)
    - `grep "n_pop_dd\|Populated" sas/06_reconcile.sas` matches (SECTION 3b populated check present)
    - `grep "7 and 21 positives" sas/06_reconcile.sas` returns NOTHING (those are md1/md8 source counts, not the merged md3-owned column)
    - File contains NO `set g.master_data_merged` write-back, NO `proc sql` `update`, NO `data X; set X` self-overwrite (`grep -i "update \|proc sql.*update" sas/06_reconcile.sas` returns nothing)
    - `grep "06_reconcile_summary.txt" sas/06_reconcile.sas` matches (writes the summary)
  </acceptance_criteria>
  <done>sas/06_reconcile.sas exists with SECTION 0-5, 18 presence checks, guarded Emergent and rt_envelope_flag counts, FILE/PUT summary; no dataset writes; no hard assertion on the flagged count</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run 06_reconcile.sas and commit the QC summary</name>
  <read_first>
    - sas/06_reconcile.sas (the program to run)
    - .planning/phases/06-variable-reconciliation/06-VALIDATION.md (run commands, lines 20-23)
  </read_first>
  <what-built>
    Task 1 wrote sas/06_reconcile.sas. This task runs it in a fresh SAS session and confirms
    the QC summary was produced clean.
  </what-built>
  <how-to-verify>
    1. Restart the SAS session (a prior program may have ended in `%abort cancel`, which leaves
       an interactive session swallowing the next submit — PCM constraint).
    2. Run: `sas -sysin sas/06_reconcile.sas -log logs/06_reconcile.log`
    3. Confirm no errors: `grep -E "ERROR|ASSERTION FAILED" logs/06_reconcile.log` returns nothing.
    4. Confirm all presence checks passed: `grep -c "PASS" logs/06_reconcile.log` returns at least 18.
    5. Confirm the summary exists: `ls qc/06_reconcile_summary.txt`.
    6. Open qc/06_reconcile_summary.txt and confirm it reports: 16+2 columns present, Emergent
       counts, rt_envelope_flag distribution, and the PCM-D-10 anchor-offset note.
    7. If any column was absent (ERROR in log), that is a real finding — the merged file may be
       stale (AMENDMENT-01 re-run not done) or a keep-separate column did not land. Report it.
  </how-to-verify>
  <action>
    Restart SAS, run the program, inspect the log and summary per how-to-verify. Do NOT commit
    *.sas7bdat, *.xlsx, or *.csv (PHI, gitignored). Commit sas/06_reconcile.sas and
    qc/06_reconcile_summary.txt to git (qc/ report is committed as a reviewable artifact).
  </action>
  <acceptance_criteria>
    - `grep -E "ERROR|ASSERTION FAILED" logs/06_reconcile.log` returns nothing
    - `grep -c "PASS" logs/06_reconcile.log` returns >= 18
    - `ls qc/06_reconcile_summary.txt` succeeds
    - `grep "Emergent" qc/06_reconcile_summary.txt` matches
    - `grep "rt_envelope_flag" qc/06_reconcile_summary.txt` matches
    - `grep "PCM-D-10" qc/06_reconcile_summary.txt` matches
  </acceptance_criteria>
  <resume-signal>Type "approved" if the log is clean and the summary is present, or describe the absent column / error</resume-signal>
  <done>06_reconcile.sas ran without ERROR, all 18 presence checks passed, qc/06_reconcile_summary.txt exists and reports Emergent + rt_envelope_flag + PCM-D-10 note, committed to git</done>
</task>

</tasks>

<verification>
- sas/06_reconcile.sas exists, reads g.master_data_merged read-only, writes only the qc summary
- All 16 deliberate columns + Emergent + rt_envelope_flag confirmed present
- Emergent distribution documented (D-04)
- rt_envelope_flag distribution documented, not hard-asserted (MRG-05)
- logs/06_reconcile.log has no ERROR
- qc/06_reconcile_summary.txt committed
</verification>

<success_criteria>
- `grep -E "ERROR|ASSERTION FAILED" logs/06_reconcile.log` returns nothing
- `grep -c "PASS" logs/06_reconcile.log` >= 18
- qc/06_reconcile_summary.txt contains "Emergent", "rt_envelope_flag", and "PCM-D-10"
- No SAS dataset was written by the program
</success_criteria>

<output>
After completion, create `.planning/phases/06-variable-reconciliation/06-02-SUMMARY.md`
</output>
