---
phase: 06-variable-reconciliation
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/data_dictionary_notes.txt
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-11
  - REC-05
must_haves:
  truths:
    - "docs/data_dictionary_notes.txt exists and documents the five multi-column/flagged concept groups for Phase 8 to consume"
    - "The deferred age-floor decision (D-07) is recorded as inherited by Phase 7"
    - "rt_envelope_flag (D-08 / MRG-05) is documented for downstream users with its meaning"
    - "The dropped inert ceilings (D-09) and closed md3-owns missingness question (D-11) are cross-referenced"
  artifacts:
    - path: "docs/data_dictionary_notes.txt"
      provides: "Prose data-dictionary stub covering the deliberate multi-column concepts, Emergent caveat, rt_envelope_flag, and deferred/closed decisions for Phase 8"
      min_lines: 40
      contains: "rt_envelope_flag"
  key_links:
    - from: "docs/data_dictionary_notes.txt"
      to: "docs/DECISIONS.md"
      via: "each concept group cites its decision ID (PCM-D-01..D-11)"
      pattern: "PCM-D-0[1-9]|PCM-D-11"
---

<objective>
Complete Phase 6 documentation: create docs/data_dictionary_notes.txt — a committed prose stub
that Phase 8's 08_dictionary.sas will consume — documenting the five deliberate concept groups
(mortality, frailty components, ISO_SEV, Emergent caveat, rt_envelope_flag) and recording the
deferred age-floor (D-07) and the closed decisions (D-09, D-11) for downstream users.

Purpose: the keep-separate resolutions (D-01/D-02/D-03) and the flag/defer/close decisions
(D-04/D-07/D-08/D-09/D-11) are already written in docs/DECISIONS.md. This plan surfaces them as
column-level dictionary guidance so anyone consuming the merged file knows the multiple columns
are deliberate, not oversight.
Output: docs/data_dictionary_notes.txt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-variable-reconciliation/06-RESEARCH.md
@docs/DECISIONS.md
</context>

<critical_facts>
- ASCII only (session encoding is not UTF-8). No smart quotes, no em-dashes, no non-ASCII bytes.
- This plan does NOT edit the OWN-03 generated block in docs/DECISIONS.md (machine-generated,
  do-not-hand-edit). All source content is the "Resolutions -- 2026-08-27" section and the
  "Standing note for the data dictionary" table already in DECISIONS.md.
- D-07 is DEFERRED, not resolved: the QC-05 Age_at_Encounter floor of 18 stays a type-sanity
  guard; it must NOT be tightened to 64. Phase 7 inherits the age-floor question.
- rt_envelope_flag = 1 on 9 rows at the 2026-08-27 run, but that is an observation, not an invariant — do
  NOT write "always 9" as a fact; write "flag marks rows where an operative sub-interval exceeds
  the room-occupancy interval; values retained, not nulled (PCM-D-08)".
- Phase 6 does NOT rename, drop, or add any column in g.master_data_merged. Reconciliation here
  is a DOCUMENTATION decision (keep-separate), not a schema change.
</critical_facts>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/data_dictionary_notes.txt</name>
  <read_first>
    - docs/DECISIONS.md ("Resolutions -- 2026-08-27" section lines 59-173 and "Standing note for the data dictionary" table lines 177-187 — the authoritative source content)
    - .planning/ROADMAP.md (Phase 7 entry, lines 111-120 — to cross-reference the inherited age-floor question)
    - .planning/phases/06-variable-reconciliation/06-RESEARCH.md (Open Question 1, lines 397-405 — why this is a prose stub, not the XLSX)
  </read_first>
  <action>
    Create docs/data_dictionary_notes.txt, ASCII only. This is a prose stub for Phase 8's
    08_dictionary.sas to fold into docs/DATA_DICTIONARY.xlsx. Write these sections verbatim in
    structure, filling the exact column names from docs/DECISIONS.md:

    HEADER:
      "PeCAN Master Dataset -- Data Dictionary Notes (Phase 6 stub)"
      "ASCII only. Source: docs/DECISIONS.md. Consumed by: Phase 8 08_dictionary.sas."
      "Phase 6 made NO schema changes -- these notes document deliberate decisions."

    SECTION 1 -- Deliberate multi-column concepts (D-01, D-02, D-03):
      A table or list stating each concept keeps multiple columns BY DECISION:
      - Mortality flag (PCM-D-01, keep separate): Death_Date_Y_N (md1-md5), IsDead_Y_N (md6),
        Death (md7). Three source-specific columns; NOT collapsed because nobody has verified they
        measure the same thing. Analysts use in_md1-in_md8 to determine which applies to whom.
        Note: _30_DAY_MORTALITY and Death_Days_After_Surgery are separate measures, never merge candidates.
      - Frailty components (PCM-D-02, keep separate): five char Y/N (Feels_Exausted,
        Low_Physical_Activity, Slow_Walking_Speed, Unintended_Weight_Loss, Week_Grip_Strength;
        md6/md7) and five numeric _Value columns (same names + _Value; md3/md5). Ten columns for
        five concepts. The width difference ($3 md7 vs $1 md6) is evidence the encodings are not
        interchangeable.
      - ISO_SEV exposure (PCM-D-03, keep separate): ISO_SEV_Exp_IntraOp_MAC_Average (md1-md3),
        ISO_SEV_IntraOp_MAC_Average (md4), ISO_SEV_MAC_TOTAL_Exp (md8). md8's is a TOTAL, not an
        average -- never a naming variant.

    SECTION 2 -- Emergent caveat (D-04, retain despite rarity):
      Emergent is retained in the merged file. Its values are Y / N / blank (NOT 1 / 0).

      IMPORTANT -- attribute the numbers correctly. The merged Emergent column is md3-owned, and
      md3's distribution has never been separately measured; qc/06_reconcile_summary.txt from
      plan 06-02 carries the actual merged counts. The often-quoted "7 positives in md1, 21 in
      md8" are SOURCE-level counts from PCM-F-06 and do NOT describe this column. Write:

      "Emergent (PCM-D-04, retained): values are Y / N / blank. The merged value is md3-owned;
       see qc/06_reconcile_summary.txt for the observed distribution. At source, md1 and md8
       showed 0.05% and 0.09% Y with blank shares matching to within a tenth of a percent --
       consistent across two independent exports, and consistent with a field clinicians rarely
       complete rather than a true emergency rate. Patient_Type and Admit_Source are likely
       better urgency proxies. Do not model on Emergent without checking the observed rate."

    SECTION 3 -- rt_envelope_flag (D-08 / MRG-05):
      rt_envelope_flag = 1 marks rows where an operative sub-interval exceeds the room-occupancy
      interval (rt_RM_START_to_RM_END_mins) that contains it -- an impossible combination. Derived in
      the Phase 4 merge DATA step. Values are RETAINED, not nulled (nothing identifies which of the
      three timestamps is wrong). Analysts should decide how to treat flagged rows. The flagged count
      is an observation (9 rows at the 2026-08-27 run), not a fixed invariant.

    SECTION 4 -- Deferred and closed decisions:
      - Age floor (PCM-D-07, DEFERRED to Phase 7): observed minimum Age_at_Encounter is 64. Not
        investigated in this project. The QC-05 floor of 18 is a type-sanity guard and must NOT be
        tightened to 64. Phase 7 (Cohort and Missingness) inherits this question when defining the
        analytic cohort. See ROADMAP Phase 7.
      - Operative-interval ceilings (PCM-D-09, DROPPED): the three QC-05 ceilings (2000/500/2000)
        never fired and were removed (QC-07). Floors handled at source by PREP-08; containment by QC-06.
      - md3-owns missingness (PCM-D-11, CLOSED): variables owned by md3 inherit md3's missingness;
        for Admit_BMI, Cognitive_Score, Frailty_Score this recovers nothing from other sources
        (PCM-F-17). Where md3 is blank on a variable it owns, no other source has a value for that patient.

    SECTION 5 -- PCM-D-10 pointer:
      Cross-reference: rt_ANCHOR_to_ADMIT_days, rt_ANCHOR_to_SURGERY_days, rt_ANCHOR_to_DISCHG_days
      legitimately carry negative values (offsets from an anchor date, not durations) and were NOT
      nulled. See docs/DECISIONS.md PCM-D-10.
  </action>
  <verify>
    <automated>test -f docs/data_dictionary_notes.txt && grep -c "rt_envelope_flag" docs/data_dictionary_notes.txt</automated>
  </verify>
  <acceptance_criteria>
    - docs/data_dictionary_notes.txt exists and is at least 40 lines
    - `grep "Death_Date_Y_N" docs/data_dictionary_notes.txt` matches (D-01 columns named)
    - `grep "Feels_Exausted_Value" docs/data_dictionary_notes.txt` matches (D-02 numeric frailty named)
    - `grep "ISO_SEV_MAC_TOTAL_Exp" docs/data_dictionary_notes.txt` matches (D-03 md8 TOTAL named)
    - `grep "rt_envelope_flag" docs/data_dictionary_notes.txt` matches (D-08/MRG-05)
    - `grep "PCM-D-07" docs/data_dictionary_notes.txt` matches AND `grep -i "Phase 7" docs/data_dictionary_notes.txt` matches (age floor inherited by Phase 7)
    - `grep "PCM-D-09" docs/data_dictionary_notes.txt` and `grep "PCM-D-11" docs/data_dictionary_notes.txt` both match
    - `grep "Emergent" docs/data_dictionary_notes.txt` matches with its caveat (D-04)
    - `grep "Y / N / blank" docs/data_dictionary_notes.txt` matches (correct value set recorded)
    - `grep "7 positives in md1" docs/data_dictionary_notes.txt` returns NOTHING (source counts must not be presented as the merged column's distribution)
    - No non-ASCII bytes: `grep -P "[^\x00-\x7F]" docs/data_dictionary_notes.txt` returns nothing
    - The "always 9" trap avoided: `grep -i "always 9\|invariant.*9" docs/data_dictionary_notes.txt` returns nothing
  </acceptance_criteria>
  <done>docs/data_dictionary_notes.txt exists, documents the five concept groups with exact column names citing PCM-D-01/02/03/04/08, records D-07 as inherited by Phase 7, cross-references D-09/D-11/D-10, ASCII only</done>
</task>

</tasks>

<verification>
- docs/data_dictionary_notes.txt exists as a committed prose stub for Phase 8
- All five concept groups documented with exact column names and decision IDs
- D-07 recorded as inherited by Phase 7 (age floor stays a guard, not tightened)
- rt_envelope_flag (D-08/MRG-05) documented for downstream users
- D-09 and D-11 cross-referenced
- ASCII only, no schema changes claimed
</verification>

<success_criteria>
- docs/data_dictionary_notes.txt >= 40 lines
- Contains Death_Date_Y_N, Feels_Exausted_Value, ISO_SEV_MAC_TOTAL_Exp, rt_envelope_flag, Emergent
- Contains PCM-D-07 with a Phase 7 reference, plus PCM-D-09 and PCM-D-11
- No non-ASCII bytes; no "always 9" invariant claim
</success_criteria>

<output>
After completion, create `.planning/phases/06-variable-reconciliation/06-03-SUMMARY.md`
</output>
