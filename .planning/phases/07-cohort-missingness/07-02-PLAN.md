---
phase: 07-cohort-missingness
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - docs/DECISIONS.md
autonomous: false
requirements:
  - PCM-D-05
  - PCM-F-11
  - PCM-F-12
must_haves:
  truths:
    - "qc/07_cohort_missingness.txt has been reviewed by the human and the admitted N and complete-case Ns are confirmed correct BEFORE any value is transcribed into DECISIONS.md"
    - "PCM-D-05 entry in docs/DECISIONS.md is updated from 'Pending' to Resolved with the INPATIENT/OBSERVATION rationale stated explicitly"
    - "The admitted-patient N measured in 07-01 is quoted in the DECISIONS.md PCM-D-05 entry"
    - "The INPATIENT/OBSERVATION eligibility rationale (geriatric assessments were never administered to ambulatory patients) is present verbatim"
    - "Every percentage in the PCM-D-05 entry states its direction explicitly (HAVE or LACK) and is transcribed from a measured value, never assumed"
    - "Within-cohort complete-case Ns are reported separately from full-file complete-case Ns, each with its own denominator named"
    - "PCM-D-07 (age floor) disposition is recorded in the PCM-D-05 entry -- no additional age criterion added"
  artifacts:
    - path: "docs/DECISIONS.md"
      provides: "PCM-D-05 resolved entry with rationale, admitted N, and PCM-D-07 age-floor disposition"
      contains: "PCM-D-05"
  key_links:
    - from: "qc/07_cohort_missingness.txt"
      to: "docs/DECISIONS.md PCM-D-05 entry"
      via: "human reads admitted_n and the measured percentages from the QC file and transcribes into DECISIONS.md"
      pattern: "PCM-D-05"
---

<objective>
Verify that sas/07_cohort.sas produced correct output, then update docs/DECISIONS.md to
resolve PCM-D-05 with the INPATIENT/OBSERVATION restriction rationale, the measured
admitted-patient N, the measured missingness figures, and the PCM-D-07 age-floor
disposition.

Purpose: Closes the only remaining open pipeline decision (PCM-D-05). Phase 8 requires
all decisions resolved before producing the final data dictionary.
Output: Resolved PCM-D-05 entry in docs/DECISIONS.md.

TASK ORDER MATTERS. The human-verify checkpoint runs FIRST. Task 2 transcribes measured
values out of qc/07_cohort_missingness.txt, so that file must exist and have been reviewed
before the transcription begins. (In the previous revision of this plan the edit task ran
first and read a QC file that nothing had yet produced.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@docs/DECISIONS.md
@.planning/phases/07-cohort-missingness/07-RESEARCH.md
@.planning/phases/07-cohort-missingness/07-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify SAS run and QC output</name>
  <read_first>
    - qc/07_cohort_missingness.txt (the P: drive QC file produced by sas/07_cohort.sas)
    - The SAS log for 07_cohort.sas (logs/ path on P: drive)
    - .planning/phases/07-cohort-missingness/07-01-SUMMARY.md (the measured values recorded there)
  </read_first>
  <what-built>
    Plan 07-01 created sas/07_cohort.sas and submitted it in SAS. This checkpoint confirms
    before anything is written into a permanent decision record that:
    (a) the program ran to completion with zero SAS ERRORs,
    (b) all four complete-case N assertions passed,
    (c) g.analytic_cohort was created with a sensible row count,
    (d) qc/07_cohort_missingness.txt exists, is complete, and holds plausible values.
  </what-built>
  <how-to-verify>
    1. Open the SAS log for 07_cohort.sas. Search for "ERROR:". There should be none.
       If any exist, return to plan 07-01 and fix sas/07_cohort.sas.
    2. Search the log for "WARNING:". Warnings from the degenerate-admitted-N guard or
       the zero-denominator guard are real findings -- read them before approving.
    3. Confirm these four NOTE lines are present:
       "NOTE: Admit_BMI complete-case N = 12726 (assertion passed)"
       "NOTE: Cognitive_Score complete-case N = 20540 (assertion passed)"
       "NOTE: Frailty_Score complete-case N = 23311 (assertion passed)"
       "NOTE: All-three complete-case N = 6523 (assertion passed)"
    4. Confirm the admitted-patient NOTE is present, in the format
       "NOTE: Admitted-patient N (INPATIENT+OBSERVATION) = NNNNN".
       Sanity-check it: it should be neither 0 nor 41,150, and roughly 24,000 is expected.
       A value of 0 or 41,150 means the Patient_Type filter did not match as intended.
    5. Confirm g.analytic_cohort exists in the g library and its row count equals the
       admitted N from step 4.
    6. Confirm qc/07_cohort_missingness.txt exists in the qc directory. Check specifically
       that there is NOT a stray file named qc07_cohort_missingness.txt one directory up --
       that would be the missing-backslash path bug.
    7. Confirm the QC file contains, in this order: the PROC FREQ Patient_Type table, the
       two PROC MEANS outputs, then the summary key=value lines. If the procedure output is
       absent and only the summary lines remain, ODS LISTING was reopened and truncated the
       file; return to 07-01.
    8. Review the PROC FREQ Patient_Type table. Confirm the values present are the expected
       set (INPATIENT, OBSERVATION, AMBULATORY or similar) with no unexpected categories and
       no blank rows that the MISSING option should have surfaced.
    9. Read these lines out of the QC file and record them -- Task 2 transcribes them:
         merged_n=
         admitted_n=
         BMI_complete_case_n_cohort=
         Cognitive_complete_case_n_cohort=
         Frailty_complete_case_n_cohort=
         all_three_complete_case_n_cohort=
         pct_admitted_HAVE_bmi=
         pct_admitted_LACK_bmi=
   10. Sanity-check the two percentages: they must sum to approximately 100. Historically
       the project shorthand "~53%" referred to the share that HAVE Admit_BMI, so
       pct_admitted_HAVE_bmi near 53 and pct_admitted_LACK_bmi near 47 is the expected
       shape. If they arrive reversed, do NOT approve -- investigate first.
   11. If everything checks out, type "approved" and include the eight values from step 9.
       Otherwise describe the issue.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms zero ERROR lines in the SAS log
    - Human confirms all four assertion NOTE lines are present with correct Ns
    - Human confirms the admitted N is neither 0 nor 41,150
    - Human confirms g.analytic_cohort row count matches the admitted N in the log
    - Human confirms qc/07_cohort_missingness.txt exists at the qc path with procedure output intact
    - Human confirms the two BMI percentages sum to approximately 100 and reports which is which
    - Human supplies the eight key=value figures for transcription
  </acceptance_criteria>
  <resume-signal>Type "approved" followed by the eight key=value figures from step 9, or describe issues found in the log or QC file</resume-signal>
  <done>SAS run clean, all four Ns asserted, g.analytic_cohort created, QC file complete, measured values captured for transcription</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/DECISIONS.md with PCM-D-05 resolution</name>
  <read_first>
    - docs/DECISIONS.md (read the entire file -- locate the pending-table row for PCM-D-05 on
      approximately line 16, and find the correct insertion point for the new resolution section;
      the last resolution section before the OWN-03 generated block is the insertion target)
    - The eight measured values supplied by the human in Task 1
    - .planning/phases/07-cohort-missingness/07-RESEARCH.md (PCM-D-05 rationale text)
    - docs/data_dictionary_notes.txt (PCM-D-07 age floor note -- confirms "do NOT tighten QC-05 floor to 64")
  </read_first>
  <action>
    Two edits to docs/DECISIONS.md (ASCII only; no UTF-8 characters).

    Every bracketed placeholder below is filled from the Task 1 values. Do NOT carry any
    percentage forward from an earlier draft of this plan or from 07-RESEARCH.md -- an
    inverted figure ("53% lack Admit_BMI" where 53% actually HAVE it) was present in an
    earlier revision and must not reach a permanent record. Each percentage is written with
    its direction spelled out.

    EDIT 1 -- Update the pending-decisions table row for PCM-D-05 (currently reads
    "Pending -- Phase 7 | TBD"):
      | PCM-D-05 | Analytic cohort INPATIENT/OBSERVATION restriction | **Resolved 2026-08-27 -- see entry below** | Gerard |

    EDIT 2 -- Append a new PCM-D-05 resolution section. Insert it immediately before the
    "## Standing note for the data dictionary" heading (or, if that heading is absent, before
    the "<!-- generated by 02_ownership.sas" comment that opens the OWN-03 block). Do NOT
    insert inside the OWN-03 generated block.

    ```
    ### PCM-D-05 -- Analytic cohort INPATIENT/OBSERVATION restriction: RESOLVED

    **Decision:** The analytic cohort is defined as patients with
    Patient_Type IN ('INPATIENT','OBSERVATION'). Implemented in sas/07_cohort.sas,
    which produces g.analytic_cohort (N = [ADMITTED_N] rows).

    **Rationale:** Ambulatory patients were never eligible for the geriatric assessments
    that produce Admit_BMI, Cognitive_Score, and Frailty_Score. A restriction based on
    assessment eligibility is pre-specifiable and clinically defensible. A restriction
    based on data availability (keep only patients with non-missing BMI) would introduce
    selection bias on the outcome. The INPATIENT/OBSERVATION criterion is independent of
    missingness.

    **Patient_Type values present (measured by PROC FREQ in SECTION 1 of 07_cohort.sas,
    before any filter was applied):** See qc/07_cohort_missingness.txt for the full
    distribution, including any unexpected categories or blank values captured with the
    MISSING option.

    **Admitted-patient N (measured, not assumed):** [ADMITTED_N] of [MERGED_N] merged rows.

    **Complete-case Ns within g.master_data_merged (asserted in SECTION 4, denominator
    [MERGED_N]):**
    - Admit_BMI non-missing: 12,726 of 41,150 (28,424 missing; unrecoverable, PCM-D-11)
    - Cognitive_Score non-missing: 20,540 of 41,150
    - Frailty_Score non-missing: 23,311 of 41,150
    - All three simultaneously non-missing: 6,523 of 41,150

    **Complete-case Ns within g.analytic_cohort (measured in SECTION 3, denominator
    [ADMITTED_N]):** These are reported separately and are NOT assumed equal to the
    full-file counts -- a non-missing value on an ambulatory row does not enter the cohort.
    - Admit_BMI non-missing: [BMI_N_COHORT] of [ADMITTED_N]
    - Cognitive_Score non-missing: [COG_N_COHORT] of [ADMITTED_N]
    - Frailty_Score non-missing: [FRL_N_COHORT] of [ADMITTED_N]
    - All three simultaneously non-missing: [ALL3_N_COHORT] of [ADMITTED_N]

    **Admit_BMI availability within the cohort (direction stated explicitly):**
    - [PCT_HAVE]% of admitted rows HAVE a non-missing Admit_BMI
    - [PCT_LACK]% of admitted rows LACK it
    The two figures are complements and sum to 100. Earlier project shorthand referred to
    "the ~53% gap"; that figure is the share that HAVE the measurement, not the share that
    lack it. The ambiguous phrasing is retired -- always state the direction.

    **PCM-D-07 (age floor) disposition:** No additional age criterion is applied in the
    analytic cohort. The QC-05 floor of 18 remains a type-sanity guard only. The observed
    minimum Age_at_Encounter is 64; this is a data characteristic, not a cohort criterion.
    Adding an age floor of 64 would be circular (derived from the observed minimum, not from
    clinical eligibility). Decision: do not tighten. See docs/data_dictionary_notes.txt.

    **md3-owns missingness trade-off (PCM-D-11):** Missingness is verified unrecoverable
    for Admit_BMI, Cognitive_Score, and Frailty_Score (coalescing other sources recovers
    zero rows; PCM-F-07). For other md3-owned variables this has NOT been verified and must
    not be stated as a global property. See SECTION 5 of sas/07_cohort.sas and
    qc/07_cohort_missingness.txt.

    **Resolved:** 2026-08-27 | Owner: Gerard | Phase 7 Plan 02
    ```
  </action>
  <verify>
    <automated>grep "PCM-D-05 -- Analytic cohort" docs/DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PCM-D-05 -- Analytic cohort" docs/DECISIONS.md` returns a match
    - `grep "Resolved 2026-08-27 -- see entry below" docs/DECISIONS.md` returns a match for the PCM-D-05 table row (the old "Pending -- Phase 7 | TBD" text is gone)
    - `grep "assessment eligibility" docs/DECISIONS.md` returns a match (rationale present)
    - `grep "g.analytic_cohort" docs/DECISIONS.md` returns a match (output dataset named)
    - `grep "PCM-D-07" docs/DECISIONS.md` returns a match inside the PCM-D-05 section (age floor disposition recorded)
    - `grep "6523" docs/DECISIONS.md` returns a match (all-three complete-case N present)
    - `grep "28,424" docs/DECISIONS.md` returns a match (BMI missing count present)
    - `grep "HAVE a non-missing Admit_BMI" docs/DECISIONS.md` returns a match (direction stated)
    - `grep "LACK it" docs/DECISIONS.md` returns a match (complement stated)
    - `grep -E "\[(ADMITTED_N|MERGED_N|BMI_N_COHORT|COG_N_COHORT|FRL_N_COHORT|ALL3_N_COHORT|PCT_HAVE|PCT_LACK)\]" docs/DECISIONS.md` returns ZERO matches (every placeholder was filled)
    - The two percentages in the entry sum to approximately 100
    - `grep "53% lack" docs/DECISIONS.md` returns ZERO matches (the inverted phrasing must not appear)
    - The OWN-03 generated block (lines starting with "<!-- generated by 02_ownership.sas") is byte-for-byte unchanged -- verify by reading 10 lines before and after the insertion point
    - `grep -Pn "[^\x00-\x7F]" docs/DECISIONS.md` returns nothing (no non-ASCII characters introduced)
  </acceptance_criteria>
  <done>docs/DECISIONS.md has a resolved PCM-D-05 entry with INPATIENT/OBSERVATION rationale, measured admitted N, both full-file and within-cohort complete-case Ns with their denominators named, direction-explicit BMI percentages, PCM-D-07 disposition, and the md3-owns caveat; pending table row updated; OWN-03 block untouched; no unfilled placeholders</done>
</task>

</tasks>

<verification>
- Human verification (Task 1) completed BEFORE the DECISIONS.md edit (Task 2)
- `grep "PCM-D-05 -- Analytic cohort" docs/DECISIONS.md` matches
- `grep "assessment eligibility" docs/DECISIONS.md` matches (rationale present)
- `grep "Resolved 2026-08-27 -- see entry below" docs/DECISIONS.md` matches for PCM-D-05 table row
- No unfilled [PLACEHOLDER] tokens remain in the entry
- Both BMI percentages present with explicit HAVE/LACK direction, summing to approximately 100
- Within-cohort Ns reported separately from full-file Ns, each with its denominator named
- OWN-03 generated block unchanged
- No non-ASCII bytes in docs/DECISIONS.md
</verification>

<success_criteria>
- PCM-D-05 entry in docs/DECISIONS.md shows Resolved with rationale, admitted N, both sets of complete-case Ns, direction-explicit percentages, and PCM-D-07 disposition
- sas/07_cohort.sas ran clean in SAS (zero ERRORs, all four assertions pass) -- confirmed in Task 1
- g.analytic_cohort exists with the admitted-patient N rows
- qc/07_cohort_missingness.txt exists, contains procedure output and "admitted_n="
- No inverted percentage reaches the permanent record
</success_criteria>

<output>
After completion, create `.planning/phases/07-cohort-missingness/07-02-SUMMARY.md`
</output>
