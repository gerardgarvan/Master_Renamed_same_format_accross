---
phase: 08-documentation-handoff
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - sas/99_run_all.sas
  - docs/DECISIONS.md
autonomous: false
requirements:
  - DOC-02
  - DOC-03
must_haves:
  truths:
    - "99_run_all.sas contains a Phase 8 block that includes 08_dictionary.sas"
    - "99_run_all.sas header comment reflects eight phases and lists docs/DATA_DICTIONARY.xlsx as an expected output"
    - "docs/DECISIONS.md contains PCM-D-05 with a resolution (not just Pending)"
    - "docs/DECISIONS.md contains PCM-D-12 documenting the percent-abort-cancel return code on this machine"
  artifacts:
    - path: "sas/99_run_all.sas"
      provides: "Full pipeline runner including Phase 8"
      contains: "08_dictionary.sas"
    - path: "docs/DECISIONS.md"
      provides: "Complete decision log through PCM-D-12"
      contains: "PCM-D-12"
  key_links:
    - from: "sas/99_run_all.sas"
      to: "sas/08_dictionary.sas"
      via: "%include with &sas_path."
      pattern: "08_dictionary.sas"
    - from: "docs/DECISIONS.md"
      to: "PCM-D-05"
      via: "Phase 7 Plan 02 wrote it; this plan verifies it exists"
      pattern: "PCM-D-05"
---

<objective>
Wire 08_dictionary.sas into 99_run_all.sas; verify PCM-D-05 resolution is present in DECISIONS.md; add PCM-D-12 (%abort cancel return code) to DECISIONS.md.

Purpose: DOC-03 requires 99_run_all.sas to run all eight phases without manual steps. DOC-02 requires DECISIONS.md to be complete through all resolved decisions. PCM-D-12 is required before 99_run_all.sas can ever be scheduled on Windows.

Output: Updated sas/99_run_all.sas (Phase 8 block added); updated docs/DECISIONS.md (PCM-D-12 added); human checkpoint for %abort cancel return-code test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Master_Renamed_same_format_accross\.planning\ROADMAP.md
@C:\Master_Renamed_same_format_accross\.planning\phases\08-documentation-handoff\08-01-SUMMARY.md
@C:\Master_Renamed_same_format_accross\sas\99_run_all.sas
@C:\Master_Renamed_same_format_accross\docs\DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 8 block to 99_run_all.sas and update header</name>
  <files>sas/99_run_all.sas</files>

  <read_first>
    - sas/99_run_all.sas (read the FULL file before editing -- see existing Phase 7 block to understand the exact pattern to copy)
  </read_first>

  <action>
Make two targeted edits to sas/99_run_all.sas:

EDIT 1 -- Update the header comment block at the top of the file:

Change "Submits all seven phases in order" to "Submits all eight phases in order".

In the Pipeline section of the header, add after the Phase 7 line:
"    Phase 8  08_dictionary.sas      -- data dictionary (docs/DATA_DICTIONARY.xlsx)"

In the "Expected final output" section, add:
"    docs/DATA_DICTIONARY.xlsx  variable dictionary (gitignored; produced on the analyst machine)"

EDIT 2 -- Insert Phase 8 block immediately before the "PIPELINE COMPLETE" block.
The existing "PIPELINE COMPLETE" comment block starts with:
"/* ========================================================================="
"   PIPELINE COMPLETE"

Insert the following block immediately before that line (copy the pattern of the Phase 7 block exactly):

```sas
/* =========================================================================
   PHASE 8 -- Documentation & Handoff
   Reads g.master_data_merged and qclib.ownership_map (read-only).
   Reads docs/data_dictionary_notes.txt for derivation metadata.
   Writes docs/DATA_DICTIONARY.xlsx.
   ========================================================================= */
%put NOTE: ---- Phase 8: Documentation & Handoff --------------------;
%include "&sas_path.\08_dictionary.sas";
%put NOTE: ---- Phase 8 complete --------------------------------------;
```

EDIT 3 -- Update the PIPELINE COMPLETE log message to reflect Phase 8:
Change:
  "%put NOTE: g.master_data_merged and g.analytic_cohort are ready.;"
To:
  "%put NOTE: g.master_data_merged and g.analytic_cohort are ready.;"
  "%put NOTE: docs/DATA_DICTIONARY.xlsx written to: &docs_path;"

Do NOT change any paths, macro variable names, or other content. Only the three edits above.
  </action>

  <verify>
    <automated>grep -i "08_dictionary.sas" "C:/Master_Renamed_same_format_accross/sas/99_run_all.sas"</automated>
    <automated>grep -i "Phase 8" "C:/Master_Renamed_same_format_accross/sas/99_run_all.sas"</automated>
    <automated>grep -i "eight phases" "C:/Master_Renamed_same_format_accross/sas/99_run_all.sas"</automated>
    <automated>grep -i "DATA_DICTIONARY.xlsx" "C:/Master_Renamed_same_format_accross/sas/99_run_all.sas"</automated>
  </verify>

  <acceptance_criteria>
    - sas/99_run_all.sas contains the string "08_dictionary.sas"
    - sas/99_run_all.sas contains the string "Phase 8" in both the header comment and the %put NOTE line
    - sas/99_run_all.sas contains the string "eight phases" (or "eight" in place of "seven") in the header
    - sas/99_run_all.sas contains the string "DATA_DICTIONARY.xlsx"
    - The Phase 8 %include block appears BEFORE the PIPELINE COMPLETE comment block (verify by reading the file top to bottom)
    - The file still contains all seven original %include lines for phases 1-7 unchanged
  </acceptance_criteria>

  <done>99_run_all.sas includes 08_dictionary.sas as Phase 8, header reflects eight phases, and DATA_DICTIONARY.xlsx is listed in expected outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run %abort cancel return-code test and record PCM-D-12</name>
  <files>docs/DECISIONS.md</files>

  <read_first>
    - docs/DECISIONS.md (read the full file to find the correct location to append PCM-D-12 -- append after the PCM-D-10 section)
  </read_first>

  <what-built>
Task 1 wired Phase 8 into the pipeline runner. Now the %abort cancel return-code behavior must be settled and documented before 99_run_all.sas can be scheduled on Windows (DOC-02, ROADMAP Phase 8 success criterion 5).
  </what-built>

  <how-to-verify>
Step 1 -- Run the return-code test manually in Windows CMD (not SAS, not PowerShell):

1. Open a Windows Command Prompt (cmd.exe).
2. Create a one-line test file. In the CMD window:
   echo %abort cancel; > C:\Temp\test_abort.sas
3. Run it via SAS batch:
   "C:\Program Files\SASHome\SASFoundation\9.4\sas.exe" -sysin C:\Temp\test_abort.sas -log C:\Temp\test_abort.log
   (Adjust the SAS executable path if it differs on this machine.)
4. Immediately after SAS exits, check the OS return code:
   echo Return code: %ERRORLEVEL%
5. Note the number printed. This is PCM-D-12.

Step 2 -- Add PCM-D-12 to docs/DECISIONS.md.
Append the following block at the end of docs/DECISIONS.md (use the actual return code you observed in step 1; replace NN with the number):

```
---

## PCM-D-12 -- %abort cancel return code on Windows batch

**Question:** When 99_run_all.sas is submitted via "sas -sysin ...", what OS return
code does Windows receive when a %abort cancel fires?

**Answer:** Return code = NN (observed on this machine, SAS 9.4M8, Windows 10 Home
10.0.19045, 2026-08-27).

**Test method:** sas -sysin test_abort.sas where test_abort.sas contains only
"%abort cancel;". Return code captured via %ERRORLEVEL% in CMD immediately after SAS exits.

**Consequence for scheduling:** Any Windows Task Scheduler job or CI step running
99_run_all.sas must treat return code NN as a pipeline failure. A return code of 0
from the SAS process means all phases completed without an abort.

**Resolved:** 2026-08-27 | Owner: Gerard | Phase 8 Plan 02
```

IMPORTANT: PCM-D-12 uses ASCII only. Do not paste smart quotes or em-dashes.
Do not change any existing content in DECISIONS.md above the new entry.

Step 3 -- Verify PCM-D-05 is present and resolved.
Read docs/DECISIONS.md and confirm there is a section for PCM-D-05 that does NOT say "Pending".

IMPORTANT -- PCM-D-05 is NOT a Phase 7 formality that will simply arrive. PCM-F-19 voided its
original rationale: the justification for restricting to INPATIENT/OBSERVATION was that only
admitted patients had the geriatric assessments (PCM-F-12), and after MRG-06 that is false --
13,288 of 20,540 cognitive scores and 15,161 of 23,311 frailty scores belong to patients
OUTSIDE the admitted cohort. `Admit_BMI` is what actually forces the restriction (all 12,726
values inside, zero outside). D-05 now needs a decision from Erin about what the cohort is for.

So DOC-02 may block on a stakeholder conversation rather than a program run. Do NOT write
PCM-D-05 here. Report its status, and if it is still open, say so plainly rather than treating
it as a pending formality.

ALSO -- DOC-02 as originally written asks for "D-01 through D-07". The register now runs to
D-11 plus D-12 added by this plan. Verify the FULL range: D-01..D-04 resolved, D-05 open (see
above), D-06..D-11 resolved, D-12 added here. Checking only through D-07 would declare
completeness while ignoring a third of the decisions.
  </how-to-verify>

  <resume-signal>
Type one of:
- "approved NN" where NN is the observed %ERRORLEVEL% value and PCM-D-12 has been added to DECISIONS.md
- "approved NN d05-pending" if PCM-D-12 is added but PCM-D-05 is still Pending (Phase 7 Plan 02 must run first)
- describe any issues encountered
  </resume-signal>

  <acceptance_criteria>
    - docs/DECISIONS.md contains the string "PCM-D-12"
    - docs/DECISIONS.md contains the string "%abort cancel" in the PCM-D-12 section
    - docs/DECISIONS.md contains the string "Return code" in the PCM-D-12 section
    - docs/DECISIONS.md contains the string "PCM-D-05" with text other than "Pending -- Phase 7" (if the Erin decision has been taken)
    - docs/DECISIONS.md contains an entry for every ID from PCM-D-01 to PCM-D-12, and none of D-01..D-04 or D-06..D-12 reads "Pending"
    - No non-ASCII characters were introduced into DECISIONS.md
  </acceptance_criteria>

  <done>
PCM-D-12 recorded in DECISIONS.md with the actual observed return code. PCM-D-05 status verified. DOC-02 is complete if PCM-D-05 is resolved; otherwise flagged for Phase 7 completion.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. grep "08_dictionary.sas" sas/99_run_all.sas -- must return at least one line
2. grep "PCM-D-12" docs/DECISIONS.md -- must return at least one line
3. grep "PCM-D-05" docs/DECISIONS.md -- confirm presence; note whether resolved or still open
   (if open, that is a stakeholder blocker, not a missing program run -- see Step 3)
4. grep -c "PCM-D-" docs/DECISIONS.md -- confirm all twelve IDs D-01..D-12 are present
4. grep "seven phases" sas/99_run_all.sas -- must return nothing (replaced by "eight phases")
</verification>

<success_criteria>
sas/99_run_all.sas includes Phase 8 and has an updated header; docs/DECISIONS.md contains PCM-D-12 with the observed return code; PCM-D-05 status is verified and reported.
</success_criteria>

<output>
After completion, create .planning/phases/08-documentation-handoff/08-02-SUMMARY.md
</output>
