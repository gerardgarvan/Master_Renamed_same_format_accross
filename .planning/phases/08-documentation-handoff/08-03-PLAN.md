---
phase: 08-documentation-handoff
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified: []
autonomous: false
requirements:
  - DOC-01
  - DOC-04
must_haves:
  truths:
    - "docs/DATA_DICTIONARY.xlsx exists on disk with a KEY sheet (leftmost) and a Dictionary sheet"
    - "Dictionary sheet has a row count equal to the PROC CONTENTS count of g.master_data_merged"
    - "99_run_all.sas runs from a clean SAS session through all eight phases without aborting"
    - "git log shows at least one commit per phase (eight reviewable commits)"
  artifacts:
    - path: "docs/DATA_DICTIONARY.xlsx"
      provides: "Variable dictionary -- produced at run time, not in git"
      contains: "KEY sheet and Dictionary sheet (verified by opening the file)"
  key_links:
    - from: "99_run_all.sas"
      to: "docs/DATA_DICTIONARY.xlsx"
      via: "Phase 8 %include block runs 08_dictionary.sas which writes the xlsx"
      pattern: "n/a -- runtime artifact"
---

<objective>
Human-verify the full pipeline run and confirm git history completeness (DOC-01, DOC-04).

Purpose: The final two success criteria for Phase 8 require a human to run 99_run_all.sas in a clean SAS session and confirm DATA_DICTIONARY.xlsx was written correctly, then confirm git log shows one reviewable commit per phase.

Output: No new files created by this plan. The checkpoint confirms DOC-01 (xlsx artifact on disk) and DOC-04 (git history). Any git commits needed to complete the history are identified here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Master_Renamed_same_format_accross\.planning\ROADMAP.md
@C:\Master_Renamed_same_format_accross\.planning\phases\08-documentation-handoff\08-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run full pipeline from clean SAS session and verify DATA_DICTIONARY.xlsx</name>
  <files></files>

  <read_first>
    - sas/99_run_all.sas (read the header for expected outputs and the batch usage command)
  </read_first>

  <what-built>
Plans 08-01 and 08-02 created sas/08_dictionary.sas and wired it into sas/99_run_all.sas as Phase 8. All eight phases are now in the pipeline runner.
  </what-built>

  <how-to-verify>
Step 1 -- Close and reopen SAS completely (fresh session, no carry-over macro symbols or librefs).

Step 2 -- Open sas/99_run_all.sas in SAS Display Manager, select all (Ctrl+A), submit (F8).
  Alternatively, run in batch from CMD:
  "C:\Program Files\SASHome\SASFoundation\9.4\sas.exe" ^
    -sysin "C:\Master_Renamed_same_format_accross\sas\99_run_all.sas" ^
    -log "P:\PeCAN Master Data\Gerard\Master_Renamed_same_format_accross\merge\logs\99_run_all.log"

Step 3 -- After SAS completes (no ERROR messages, no %abort cancel fires), check the SAS log for:
  - The line: "NOTE: ---- Phase 8: Documentation & Handoff ----"
  - The line: "NOTE: ---- Phase 8 complete ----"
  - The line: "NOTE: [08_dictionary] DATA_DICTIONARY.xlsx written to ..."
  - The line: "NOTE: [08_dictionary] Variable count: NNN" (note the number NNN)
  - No ERROR or WARNING lines that indicate a run failure

Step 4 -- Open docs/DATA_DICTIONARY.xlsx in Excel and verify:
  a. KEY sheet is the LEFTMOST tab (first sheet from the left)
  b. Dictionary sheet is the second tab
  c. Dictionary sheet has NNN rows of data (matching the variable count from the log)
  d. Header row has blue (#0021A5) background
  e. Spot-check: PRECEDE_STUDY_ID row exists, source column shows md3 or similar
  f. Spot-check: rt_envelope_flag row exists, derivation column shows "derived at merge (Phase 4)"
  g. Spot-check: Cognitive_Score row exists, derivation column shows "md3 spine, md8 gap-fill (MRG-06)"
  h. Spot-check: rt_ANCHOR_to_ADMIT_days row exists, derivation column mentions "PCM-D-10"
  i. coverage_pct column shows numeric values (not blank) for all rows

Report the variable count NNN in your resume signal.
  </how-to-verify>

  <resume-signal>
Type one of:
- "approved NNN" where NNN is the variable count from the log and all spot-checks passed
- describe any issues: which phase aborted, which spot-check failed, what the log says
  </resume-signal>

  <acceptance_criteria>
    - SAS log contains "NOTE: ---- Phase 8 complete ----" with no ERROR lines after it
    - SAS log contains "NOTE: [08_dictionary] Variable count:" followed by a number greater than 0
    - docs/DATA_DICTIONARY.xlsx exists on disk (file size > 0 bytes)
    - KEY sheet is leftmost in the workbook
    - Dictionary sheet row count matches the variable count from the log
    - Spot-check variables rt_envelope_flag, Cognitive_Score, rt_ANCHOR_to_ADMIT_days are all present with non-blank derivation values
  </acceptance_criteria>

  <done>Full pipeline confirmed clean. DATA_DICTIONARY.xlsx verified. DOC-01 and DOC-03 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify git history has one reviewable commit per phase</name>
  <files></files>

  <read_first>
    - .planning/ROADMAP.md (Phase 8 success criterion 4: "A git history where each phase is a reviewable commit")
  </read_first>

  <what-built>
The pipeline is complete. This checkpoint confirms DOC-04: the git log is reviewable by phase.
  </what-built>

  <how-to-verify>
Step 1 -- In a CMD or Git Bash window from the repo root (C:\Master_Renamed_same_format_accross):
  git log --oneline

Step 2 -- Confirm the log shows commits traceable to each of the eight phases. Exact commit messages will vary, but look for commits referencing:
  - Phase 1 (verify_sources / checksums / SRC)
  - Phase 2 (ownership_map / OWN)
  - Phase 3 (prep / normalization / PREP)
  - Phase 4 (merge / MRG)
  - Phase 5 (qc_merge / QC)
  - Phase 6 (reconcile / variable reconciliation)
  - Phase 7 (cohort / missingness)
  - Phase 8 (dictionary / handoff / DOC)

Step 3 -- If any phase has no commit, identify which files from that phase are uncommitted:
  git status

Step 4 -- If uncommitted SAS programs exist, stage and commit them now with a message like:
  git add sas/08_dictionary.sas sas/99_run_all.sas docs/DECISIONS.md
  git commit -m "docs(08): Phase 8 data dictionary and pipeline finalization"

Do NOT commit any .sas7bdat, .xlsx, .csv, or files under data/ (gitignored by project policy).
  </how-to-verify>

  <resume-signal>
Type one of:
- "approved" if git log shows all eight phases and no uncommitted SAS program files remain
- "committed" if you ran a git commit to add missing phase artifacts -- paste the commit hash
- describe any issues
  </resume-signal>

  <acceptance_criteria>
    - git log --oneline shows at least 8 commits (one per phase; more is acceptable)
    - git status shows no untracked or modified .sas files under sas/
    - docs/DECISIONS.md is committed (git status shows it clean)
    - No .sas7bdat, .xlsx, or .csv files were committed (gitignore is working)
  </acceptance_criteria>

  <done>Git history confirmed reviewable by phase. DOC-04 satisfied. Phase 8 complete.</done>
</task>

</tasks>

<verification>
Final check (run from repo root after both checkpoints pass):
1. git log --oneline | head -20 -- confirm eight phase commits visible
2. git status -- confirm clean working tree (no modified or untracked SAS files)
3. test -f "C:/Master_Renamed_same_format_accross/docs/DATA_DICTIONARY.xlsx" -- file exists (or check in Windows Explorer)
4. grep "PCM-D-12" docs/DECISIONS.md -- returns at least one line
5. grep "08_dictionary.sas" sas/99_run_all.sas -- returns at least one line
</verification>

<success_criteria>
1. docs/DATA_DICTIONARY.xlsx exists on disk with KEY sheet leftmost, correct variable count, and spot-checks passing (DOC-01)
2. docs/DECISIONS.md contains PCM-D-05 resolved and PCM-D-12 with observed return code (DOC-02)
3. 99_run_all.sas ran all eight phases cleanly from a fresh SAS session (DOC-03)
4. git log shows eight reviewable phase commits and working tree is clean (DOC-04)
</success_criteria>

<output>
After completion, create .planning/phases/08-documentation-handoff/08-03-SUMMARY.md
</output>
