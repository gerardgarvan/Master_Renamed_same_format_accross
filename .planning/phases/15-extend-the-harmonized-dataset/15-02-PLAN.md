---
phase: 15-extend-the-harmonized-dataset
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - sas/10b_concept_harmonize.sas
autonomous: false
requirements: [HARM-07, HARM-04]
user_setup: []

must_haves:
  truths:
    - "A written rule in the 10b program header states which pipeline-derived columns are carried and which dropped"
    - "The twelve no-information columns (in_md3 + eleven h_*_src companions) are dropped from g.master_data_harmonized"
    - "An in-program assertion fails the run if any of the twelve remain in the harmonized output"
    - "The src_changed assertion no longer false-fires on in_md3 (dropped by rule, not by redundancy proof)"
    - "g.master_data_merged is confirmed unmodified after the run: 176 columns, 41,150 rows"
    - "Any newly dropped alias is proven redundant in-run (0 rows added, 0 disagreements) by the existing SECTION 4b machinery"
  artifacts:
    - path: "sas/10b_concept_harmonize.sas"
      provides: "HARM-07 rule header, drop_pipeline_noinfo gate, DROP block, HARM-07 assertion, src_changed in_md3 exclusion, post-run merged column-count assertion"
      contains: "drop_pipeline_noinfo"
    - path: "qc/10b_harmonize_report.txt"
      provides: "Regenerated harmonization report after the extended re-run"
      contains: "harmonized_columns"
  key_links:
    - from: "sas/10b_concept_harmonize.sas SECTION 5 DATA step"
      to: "g.master_data_harmonized"
      via: "DROP in_md3 + all h_*_src under drop_pipeline_noinfo gate"
      pattern: "drop in_md3"
    - from: "sas/10b_concept_harmonize.sas SECTION 6 assert_all"
      to: "dictionary.columns for MASTER_DATA_HARMONIZED"
      via: "HARM-07 absence assertion"
      pattern: "n_noinfo_present"
    - from: "sas/10b_concept_harmonize.sas SECTION 6"
      to: "dictionary.columns for MASTER_DATA_MERGED"
      via: "post-run 176-column assertion"
      pattern: "n_merged_cols"
---

<objective>
Satisfy HARM-07: state a rule governing pipeline-derived columns and enforce it in code, then
re-run `10b_concept_harmonize.sas` end-to-end so the extended decisions from Plan 15-01 are
applied and the twelve no-information columns are provably dropped.

Three targeted code changes to `sas/10b_concept_harmonize.sas` (no new program):
  1. A written rule in the header + a `%let drop_pipeline_noinfo = 1;` gate + a DROP block in
     the SECTION 5 DATA step removing `in_md3` and the eleven `h_*_src` companions.
  2. A HARM-07 assertion in SECTION 6 that fails if any of the twelve remain in the output.
  3. A fix to the SECTION 6 `src_changed` assertion so it does NOT false-fire on `in_md3`
     (which is in g.master_data_merged but is dropped by RULE, not by redundancy proof), plus
     a post-run assertion that g.master_data_merged still has exactly 176 columns.

Then a human-verified fresh-session SAS run confirms everything passes.

Note on waves: the two code-edit tasks (Tasks 1-2) do not themselves depend on Plan 15-01,
but Task 3 (the SAS run) applies the decisions Plan 15-01 wrote into concept_decisions.csv,
so this plan runs in Wave 2 after 15-01 completes.

Purpose: HARM-07 requires the rule to be "enforced in code" — a comment satisfies "written"
but not "enforced". The DROP + assertion is the enforcement.
Output: extended sas/10b_concept_harmonize.sas; regenerated g.master_data_harmonized and
qc/10b_harmonize_report.txt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-extend-the-harmonized-dataset/15-RESEARCH.md
@.planning/phases/15-extend-the-harmonized-dataset/15-VALIDATION.md
@sas/10b_concept_harmonize.sas

<interfaces>
<!-- Existing 10b macro variables and structures the HARM-07 changes must use. -->
<!-- Do NOT introduce new names where these already exist. -->

Existing macro variables (global, set before SECTION 5):
  &n_rows     = row count of g.master_data_merged (should be 41150)
  &n_h        = count of distinct harmonized names (harmonized columns)
  &hnames     = space-separated list of harmonized names (e.g. h_death h_cognitive ...)
  &droplist   = space-separated proven-redundant source columns dropped in the DATA step
  &force_src  = 1  (emits every h_*_src companion — LEAVE AS-IS; HARM-07 drops via DROP, not by force_src=0)
  &n_out      = row count of g.master_data_harmonized (asserted == &n_rows)

Existing macros:
  %fail_out(msg=...)   -> %put ERROR + %abort cancel  (use for every HARM-07 failure)

Existing SECTION 5 DATA step (build_harmonized macro) skeleton:
  data g.master_data_harmonized;
    set g.master_data_merged;
    %if %length(&droplist) > 0 %then %do; drop &droplist; %end;   <-- add HARM-07 DROP right after this
    length ... ;
    ...rules...
  run;

Existing SECTION 6 src_changed WHERE clause (the line to extend):
  and a.name not in (select strip(upcase(varname)) from work.drop_ok)

Existing SECTION 6 assert_all macro: add the HARM-07 absence check and the merged-column-count
check inside/alongside it, using %fail_out on violation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HARM-07 rule header, drop_pipeline_noinfo gate, and DROP block</name>
  <read_first>
    - sas/10b_concept_harmonize.sas (SECTION 4b for &drop_redundant placement pattern; SECTION 5 build_harmonized DATA step)
    - .planning/phases/15-extend-the-harmonized-dataset/15-RESEARCH.md (Pattern 2, Example 1, Pitfall 4)
  </read_first>
  <action>
    1. In the program header comment block (near the top, after the CON-09 description), add the
       HARM-07 rule statement verbatim (ASCII only):

       ```
       Pipeline-derived column rule (HARM-07):
         CARRY: in_md1, in_md2, in_md4..in_md8, n_sources, rt_envelope_flag, rt_*
                (these carry information: source membership, row count, clinical timing)
         DROP:  in_md3 (constant -- md3 is the spine; value is 1 for all 41,150 rows)
                h_*_src companions (each a single repeated value -- the redundancy proof
                showed no secondary source ever fires; these columns carry no information)
         ENFORCEMENT: DROP statements in the SECTION 5 DATA step; a SECTION 6 assertion that
                all twelve are absent from g.master_data_harmonized after the DATA step.
                Set drop_pipeline_noinfo=0 to retain them for diagnostic purposes.
       ```

    2. Immediately after the existing `%let force_src = 1;` line in SECTION 5 (do NOT change
       force_src — keep it 1 so the LENGTH declarations remain, per Pitfall 4), add the gate:

       ```
       /* HARM-07: pipeline-derived column rule. See header. in_md3 is constant (md3 is the
          spine); h_*_src companions each hold one repeated value (no secondary ever fires).
          Both classes carry no information and are dropped here. g.master_data_merged is
          untouched. Set to 0 to retain for diagnostics. */
       %let drop_pipeline_noinfo = 1;
       ```

    3. In the build_harmonized macro's DATA step, immediately AFTER the existing
       `%if %length(&droplist) > 0 %then %do; drop &droplist; %end;` block, add the HARM-07
       DROP block. It drops in_md3 and each h_*_src companion, built from &hnames at compile
       time (loop variable &_pi to avoid colliding with the existing &i / &k):

       ```
       %if &drop_pipeline_noinfo = 1 %then %do;
         drop in_md3
         %do _pi = 1 %to &n_h;
           %scan(&hnames, &_pi)._src
         %end;
         ;
       %end;
       ```

       Declare `%local _pi;` alongside the existing `%local i k h emit_src;` in build_harmonized.
       DROP removes columns from OUTPUT, not the PDV, so the priority rules below still read any
       dropped source column correctly. Do NOT touch the LENGTH block or the force_src logic.
  </action>
  <verify>
    <automated>grep -q "drop_pipeline_noinfo" sas/10b_concept_harmonize.sas AND grep -q "drop in_md3" sas/10b_concept_harmonize.sas AND grep -q "Pipeline-derived column rule (HARM-07)" sas/10b_concept_harmonize.sas</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Pipeline-derived column rule (HARM-07)" sas/10b_concept_harmonize.sas` matches (rule stated)
    - `grep "%let drop_pipeline_noinfo = 1" sas/10b_concept_harmonize.sas` matches (gate present)
    - `grep -n "drop in_md3" sas/10b_concept_harmonize.sas` matches inside build_harmonized, AFTER the `drop &droplist` block
    - The DROP block loops `%do _pi = 1 %to &n_h;` emitting `%scan(&hnames, &_pi)._src`
    - `%let force_src = 1;` is UNCHANGED (grep still finds it set to 1)
    - `%local` line in build_harmonized includes `_pi`
    - No non-ASCII bytes introduced
  </acceptance_criteria>
  <done>Header carries the HARM-07 rule; drop_pipeline_noinfo gate set to 1; DROP block for in_md3 + eleven h_*_src companions added after the droplist block, force_src untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add HARM-07 absence assertion, src_changed in_md3 exclusion, and merged 176-column assertion</name>
  <read_first>
    - sas/10b_concept_harmonize.sas (SECTION 6: the src_changed PROC SQL, the assert_all macro, the &n_out/&n_key queries)
    - .planning/phases/15-extend-the-harmonized-dataset/15-RESEARCH.md (Pattern 2 assertion, Example 2, Example 3, Example 4, Pitfall 1, CRITICAL SUBTLETY)
  </read_first>
  <action>
    Three changes in SECTION 6. All failures use the existing %fail_out(msg=...) macro.

    1. src_changed exclusion for in_md3 (Pitfall 1 / CRITICAL SUBTLETY). in_md3 IS a column of
       g.master_data_merged and is now dropped by RULE, not by redundancy proof — without this
       fix, src_changed reports "1 columns vanished ... WITHOUT being proven redundant" for
       IN_MD3. In the existing src_changed WHERE clause, immediately after the line
       `and a.name not in (select strip(upcase(varname)) from work.drop_ok)`
       add:

       ```
       /* HARM-07 exclusion -- in_md3 is dropped by rule, not by redundancy proof */
       %if &drop_pipeline_noinfo = 1 %then %do;
       and a.name ne 'IN_MD3'
       %end;
       ```
       (The h_*_src companions are NOT in g.master_data_merged — they are created by 10b — so
       src_changed never sees them; only in_md3 needs excluding.)

    2. HARM-07 absence assertion. After the existing &n_out / &n_key queries in SECTION 6, add a
       PROC SQL + macro that counts how many of the twelve no-information columns remain in
       g.master_data_harmonized and fails if any do. Place the %if guard so it only runs when
       drop_pipeline_noinfo=1:

       ```
       %if &drop_pipeline_noinfo = 1 %then %do;
         proc sql noprint;
           create table work.noinfo_present as
           select name from dictionary.columns
           where libname='G' and memname='MASTER_DATA_HARMONIZED'
             and ( upcase(name) = 'IN_MD3'
                   %do _pi = 1 %to &n_h;
                   or upcase(name) = upcase("%scan(&hnames,&_pi)_SRC")
                   %end; );
           select count(*) into :n_noinfo_present trimmed from work.noinfo_present;
         quit;
         %if &n_noinfo_present > 0 %then %do;
           %put ERROR: &n_noinfo_present no-information pipeline column(s) still present.;
           %fail_out(msg=HARM-07 violation -- in_md3 or h_*_src columns not dropped);
         %end;
         %put NOTE: [10b] HARM-07 OK -- in_md3 and all h_*_src companions dropped.;
       %end;
       ```
       This block must live inside a macro (PCM rule: every %abort inside a macro; %fail_out
       already satisfies that, but the surrounding %do/%if must also be inside a macro). Put the
       whole block inside the existing assert_all macro, OR a new %macro assert_harm07; ... %mend
       assert_harm07; %assert_harm07; called right after %assert_all. Declare `%local _pi;` in
       whichever macro hosts the %do loop.

    3. Post-run g.master_data_merged 176-column assertion (success criterion 3). Add a query +
       macro confirming the merged file is unmodified:

       ```
       proc sql noprint;
         select count(*) into :n_merged_cols trimmed
         from dictionary.columns
         where libname='G' and memname='MASTER_DATA_MERGED';
       quit;
       %macro assert_merged_unchanged;
         %if &n_merged_cols ne 176 %then %do;
           %fail_out(msg=g.master_data_merged has &n_merged_cols columns -- expected 176 -- it was modified);
         %end;
         %put NOTE: [10b] g.master_data_merged confirmed 176 columns and &n_rows rows -- unmodified.;
       %mend assert_merged_unchanged;
       %assert_merged_unchanged;
       ```
       (Row count of the merged file is already implied by &n_rows and &n_out == &n_rows; this
       adds the column-count guarantee. 41,150 rows is the &n_rows value asserted in SECTION 0.)
  </action>
  <verify>
    <automated>grep -q "n_noinfo_present" sas/10b_concept_harmonize.sas AND grep -q "n_merged_cols" sas/10b_concept_harmonize.sas AND grep -q "a.name ne 'IN_MD3'" sas/10b_concept_harmonize.sas</automated>
  </verify>
  <acceptance_criteria>
    - `grep "a.name ne 'IN_MD3'" sas/10b_concept_harmonize.sas` matches, guarded by `%if &drop_pipeline_noinfo = 1`, inside the src_changed WHERE clause
    - `grep "n_noinfo_present" sas/10b_concept_harmonize.sas` matches; the query counts IN_MD3 plus every `%scan(&hnames,&_pi)_SRC`
    - The HARM-07 assertion calls %fail_out with a message containing "HARM-07 violation" when n_noinfo_present > 0
    - `grep "n_merged_cols ne 176" sas/10b_concept_harmonize.sas` matches; %fail_out fires on any count != 176
    - Every %abort/%fail_out remains inside a macro (no bare open-code %if driving %abort)
    - `%local _pi;` declared in the macro hosting each new %do loop
    - No non-ASCII bytes introduced
  </acceptance_criteria>
  <done>src_changed excludes in_md3 under the gate; HARM-07 absence assertion fails on any of the twelve remaining; post-run assertion confirms g.master_data_merged is 176 columns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fresh-session SAS run and verification (applies 15-01 decisions + HARM-07 rule)</name>
  <read_first>
    - sas/10b_concept_harmonize.sas (the fully extended program)
    - docs/concept_decisions.csv (must already carry the confirmed EXT rows from Plan 15-01)
    - .planning/phases/15-extend-the-harmonized-dataset/15-VALIDATION.md (Per-Task Verification Map)
    - .planning/phases/15-extend-the-harmonized-dataset/15-RESEARCH.md (Pitfalls 1, 2, 3, 5)
  </read_first>
  <action>
    In a fresh SAS 9.4 session (restart SAS first -- %abort cancel leaves an interactive session
    that swallows the next submit), submit sas/10b_concept_harmonize.sas after confirming
    docs/concept_decisions.csv already carries the confirmed EXT rows from Plan 15-01. Inspect
    the SAS log for zero ERROR lines and the required NOTE lines, then run PROC CONTENTS to
    confirm in_md3 and all h_*_src are absent from g.master_data_harmonized, the new h_ columns
    are present, and g.master_data_merged still has 176 columns and 41,150 rows. See
    how-to-verify for the exact log strings and abort signatures.
  </action>
  <what-built>
    The extended 10b_concept_harmonize.sas (HARM-07 rule + drop + assertions) plus the extended
    concept_decisions.csv from Plan 15-01. This checkpoint runs the program end-to-end in a
    fresh SAS 9.4 session — the integration step that both applies the HARM-04 decisions (proving
    them by SECTION 4b redundancy proof) and enforces the HARM-07 rule. This task depends on Plan
    15-01 being complete (concept_decisions.csv already extended and force-committed).
  </what-built>
  <how-to-verify>
    PRECONDITION: confirm docs/concept_decisions.csv already contains the confirmed EXT rows
    (Plan 15-01 Task 3 done). If not, complete 15-01 first.

    1. Restart SAS (fresh session — %abort cancel leaves an interactive session that swallows the
       next submit, per project rule). Submit `sas/10b_concept_harmonize.sas`.
    2. Inspect the SAS log for ZERO ERROR lines and the following NOTE lines:
       - `NOTE: [10b] decision file passed all thirteen validation gates.`
       - `NOTE: [10b] &n_yes confirmed mappings across &n_con_yes concepts.` with n_con_yes
         reflecting the original 11 concepts PLUS the newly confirmed SSDI/CPT1 (and any label)
         concepts.
       - `NOTE: [10b] every observed value of every confirmed column is mapped.` (coverage passed)
       - For each newly dropped alias: `NOTE: [10b] <sv> vs <pv> -- would add 0 rows, 0 disagreements.`
         (redundancy PROVEN in-run — success criterion 4: zero rows added, zero disagreements)
       - `NOTE: [10b] HARM-07 OK -- in_md3 and all h_*_src companions dropped.`
       - `NOTE: [10b] g.master_data_merged confirmed 176 columns and 41150 rows -- unmodified.`
    3. Watch for the failure signatures from RESEARCH pitfalls and abort if any appear:
       - "N concepts are only PARTIALLY confirmed" (Pitfall 2 — a concept was half-confirmed in 15-01)
       - "columns vanished ... WITHOUT being proven redundant" for IN_MD3 (Pitfall 1 — Task 2 fix missing)
       - "harmonized names collide with an existing column" (Pitfall 5 — pick a new h_ name)
       - decision file missing (Pitfall 3 — concept_decisions.csv not on disk / not committed)
    4. Confirm g.master_data_harmonized exists. Run PROC CONTENTS or query dictionary.columns:
       - in_md3 is ABSENT
       - no *_src column is present (all eleven h_*_src companions absent)
       - the new h_ column(s) for the confirmed concepts ARE present
    5. Confirm g.master_data_merged still has 176 columns and 41,150 rows (PROC CONTENTS).
    6. Copy qc/10b_harmonize_report.txt to the repo qc/ folder if commit history is wanted
       (P: qc path is not version-controlled).
  </how-to-verify>
  <acceptance_criteria>
    - SAS log shows zero ERROR lines
    - Log shows "decision file passed all thirteen validation gates"
    - Log shows "HARM-07 OK -- in_md3 and all h_*_src companions dropped"
    - Log shows "g.master_data_merged confirmed 176 columns and 41150 rows -- unmodified"
    - For every newly dropped alias, the redundancy NOTE shows "would add 0 rows, 0 disagreements"
    - PROC CONTENTS of g.master_data_harmonized: in_md3 absent, all h_*_src absent, new h_ columns present
    - PROC CONTENTS of g.master_data_merged: exactly 176 columns, 41,150 rows
  </acceptance_criteria>
  <resume-signal>Type "run passed" with the n_con_yes count and final harmonized column count, or paste the ERROR lines.</resume-signal>
</task>

</tasks>

<verification>
- HARM-07 rule stated in the program header and enforced by DROP + assertion (Tasks 1, 2)
- src_changed no longer false-fires on in_md3; twelve no-information columns provably absent (Tasks 2, 3)
- HARM-04 decisions from 15-01 applied by program and proven redundant in-run (Task 3)
- g.master_data_merged confirmed unmodified: 176 columns, 41,150 rows (Tasks 2, 3)
- Any newly dropped alias proven redundant: 0 rows added, 0 disagreements (Task 3, existing SECTION 4b)
</verification>

<success_criteria>
- HARM-07: written rule enforced in code; in_md3 + eleven h_*_src dropped and asserted absent
- HARM-04: extended decisions applied by 10b, not by hand; SECTION 4b re-proves redundancy
- g.master_data_merged unmodified (176 cols, 41,150 rows) — asserted post-run
- Newly dropped alias(es) proven redundant in the run (zero rows added, zero disagreements)
</success_criteria>

<output>
After completion, create `.planning/phases/15-extend-the-harmonized-dataset/15-02-SUMMARY.md`
recording the final harmonized column count, the concepts applied, the redundancy proof results,
and confirmation that in_md3 + h_*_src are absent and g.master_data_merged is unchanged.
</output>
