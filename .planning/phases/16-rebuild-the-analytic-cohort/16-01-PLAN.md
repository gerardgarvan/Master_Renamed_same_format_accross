---
phase: 16-rebuild-the-analytic-cohort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sas/16b_cohort_rebuild.sas
autonomous: false
requirements: [HARM-10]
must_haves:
  truths:
    - "sas/16b_cohort_rebuild.sas reads g.master_data_harmonized and never writes it"
    - "g.analytic_cohort is rebuilt from g.master_data_harmonized restricted to INPATIENT+OBSERVATION rows (13,890)"
    - "g.analytic_cohort carries all 174 columns including every h_* column and no dropped alias"
    - "Full-file complete-case Ns 12,726 / 20,540 / 23,311 are asserted and pass"
    - "g.master_data_harmonized is confirmed unmodified (174 cols, 41,150 rows) after the run"
  artifacts:
    - path: "sas/16b_cohort_rebuild.sas"
      provides: "Cohort rebuild program reading harmonized, promoting analytic_cohort"
      contains: "g.master_data_harmonized"
      min_lines: 200
  key_links:
    - from: "sas/16b_cohort_rebuild.sas"
      to: "g.master_data_harmonized"
      via: "set statement in work.cohort_candidate DATA step"
      pattern: "set g.master_data_harmonized"
    - from: "sas/16b_cohort_rebuild.sas"
      to: "g.analytic_cohort"
      via: "promote DATA step after all assertions pass"
      pattern: "data g.analytic_cohort"
---

<objective>
Write sas/16b_cohort_rebuild.sas: a standalone SAS 9.4M8 program that reads
g.master_data_harmonized (174 cols, 41,150 rows), applies the INPATIENT+OBSERVATION
Patient_Type filter, validates in WORK, promotes the validated candidate to
g.analytic_cohort (replacing the stale Phase 7 version), measures within-cohort and h_*
Ns, and asserts the source file is unmodified.

Purpose: HARM-10 -- the current g.analytic_cohort is 176 columns built in Phase 7 from
g.master_data_merged, carrying eleven dropped aliases and none of the h_* columns. Any
analysis using it gets pre-harmonisation encodings. This rebuilds it from the harmonized file.
Output: sas/16b_cohort_rebuild.sas (committed), plus at run time g.analytic_cohort (P: drive,
not committed) and two QC files on the P: qc path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-rebuild-the-analytic-cohort/16-CONTEXT.md
@.planning/phases/16-rebuild-the-analytic-cohort/16-RESEARCH.md
@sas/07_cohort.sas
@sas/10b_concept_harmonize.sas
@sas/00_config.sas

<interfaces>
<!-- Confirmed facts the executor must use directly. No exploration needed. -->

Program name: sas/16b_cohort_rebuild.sas
  (16_ prefix is TAKEN by 16_raw_inventory.sas and 16_summary_docx.sas -- use 16b_,
   consistent with the 10b_ precedent. Apply 16b_ to program, log, and QC file names.)

Filter (verbatim from 07_cohort.sas line 182):
  where upcase(strip(Patient_Type)) in ('INPATIENT','OBSERVATION')

Source dataset: g.master_data_harmonized (174 columns, 41,150 rows)
Target dataset: g.analytic_cohort (full 174-column pass-through, admitted rows only)
Expected admitted N: 13,890 (INPATIENT 13,223 + OBSERVATION 667)

Full-file complete-case Ns to ASSERT (queried against g.master_data_harmonized, NOT the cohort):
  Admit_BMI       = 12726
  Cognitive_Score = 20540
  Frailty_Score   = 23311

Within-cohort Ns to MEASURE only (do NOT assert -- they fire by design after MRG-06):
  Cognitive_Score within cohort (expected ~7,252), Frailty_Score within cohort (expected ~8,150)
  All-three within cohort (D-06: was 6,523 -- stale; measure, never assert)
  Within-cohort Admit_BMI (expected 12,726) MAY be asserted per D-01, but prefer measure+report.

h_* columns to MEASURE within cohort (D-07, report only, no assertion). All 12:
  H_DEATH_YN H_DIABETES H_FRAILTY_ACTIVITY H_FRAILTY_EXHAUST H_FRAILTY_GRIP
  H_FRAILTY_WALKING H_FRAILTY_WEIGHT H_HYPERLIPIDEMIA H_HYPERTENSION
  H_MOVEMENT_DISORDER H_SLEEP_APNEA H_SSDI_DEATH

QC / log file names (all 16b_ prefixed):
  &logs_path.\16b_cohort_rebuild.log
  &qc_path.\16b_cohort_missingness.txt   (data _null_ file mod ONLY -- never ODS)
  &qc_path.\16b_cohort_tables.txt        (ODS LISTING FILE= target ONLY)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SECTIONS 0-4 of sas/16b_cohort_rebuild.sas (preconditions, filter, missingness, full-file assertions)</name>
  <read_first>
    - sas/07_cohort.sas (SECTIONS 0-4: copy %fail_out, %check_dir, %check_source_exists,
      %check_vars_present, assert_row_count, %classify_cohort, %assert_cohort_values,
      %bmi_pct, %assert_complete_case_n verbatim, adapting dataset name and row count)
    - sas/00_config.sas (confirm &g_path, &qc_path, &logs_path macro variables)
    - .planning/phases/16-rebuild-the-analytic-cohort/16-RESEARCH.md (Architecture Patterns section)
  </read_first>
  <action>
    Create sas/16b_cohort_rebuild.sas with a header comment block (pure ASCII, no non-ASCII
    chars per PCM-F-10) naming: Program 16b_cohort_rebuild.sas, Phase 16, Purpose (reads
    g.master_data_harmonized never writes it; rebuilds g.analytic_cohort from harmonized),
    Requirements HARM-10 / PCM-D-05, and the PCM violations avoided list (PCM-T-01 no PROC SQL
    UPDATE, PCM-T-02 no in-place rewrite, PCM-T-11 IS NOT MISSING guards, PCM-R-05 every
    %abort cancel inside a named macro, no &SQLOBS, no non-ASCII).

    SECTION 0 (options, paths, preconditions):
    - `options nodate nonumber ps=max ls=200;`
    - `%include "C:\Master_Renamed_same_format_accross\sas\00_config.sas";`
    - `libname g "&g_path";` (g is WRITABLE -- SECTION 5 promotes into it)
    - Copy %fail_out(msg=) verbatim from 07_cohort.sas but change the two put statements to say
      "16b_cohort_rebuild.sas aborted -- &msg".
    - Copy %check_dir verbatim; call %check_dir(path=&qc_path, label=qc) and
      %check_dir(path=&logs_path, label=logs).
    - `proc printto log="&logs_path.\16b_cohort_rebuild.log" new; run;`
    - Adapt %check_source_exists to query dictionary.tables for
      upcase(memname)='MASTER_DATA_HARMONIZED', fail with
      "g.master_data_harmonized not found. Run Phases 3-15 first." (n_tab ne 1).
    - Adapt %check_vars_present: loop the same five vars
      (Patient_Type Admit_BMI Cognitive_Score Frailty_Score Age_at_Encounter) but query
      upcase(memname)='MASTER_DATA_HARMONIZED'.
    - Row count: `select count(*) into :n_harmonized trimmed from g.master_data_harmonized;`
      then %assert_row_count adapted to compare `&n_harmonized ne 41150` and message
      "g.master_data_harmonized has &n_harmonized rows, expected 41150. Re-run Phases 4-15."
    - Open the summary artifact fresh with a `data _null_; file "&qc_path.\16b_cohort_missingness.txt";`
      step (header lines naming the program, harmonized row count, and pointing to
      16b_cohort_tables.txt for tables).

    SECTION 1 (Patient_Type distribution):
    - `ods listing file="&qc_path.\16b_cohort_tables.txt";`
    - PROC FREQ on Patient_Type (tables Patient_Type / missing) on g.master_data_harmonized,
      title "Patient_Type distribution in g.master_data_harmonized (pre-filter) -- PCM-D-05 evidence".

    SECTION 2 (build candidate in WORK):
    - `data work.cohort_candidate; set g.master_data_harmonized;
       where upcase(strip(Patient_Type)) in ('INPATIENT','OBSERVATION'); run;`
    - `select count(*) into :n_admitted trimmed from work.cohort_candidate;`
    - Copy %classify_cohort verbatim but replace every `&n_merged` reference with `&n_harmonized`.

    SECTION 3 (missingness profile + within-cohort measurement):
    - PROC MEANS n nmiss on g.master_data_harmonized (var Admit_BMI Cognitive_Score
      Frailty_Score Age_at_Encounter) titled full-file.
    - PROC MEANS n nmiss on work.cohort_candidate (same vars) titled admitted-only, N=&n_admitted.
    - PROC FREQ Patient_Type / missing on work.cohort_candidate (post-filter).
    - Copy %assert_cohort_values verbatim (asserts 0 rows outside INPATIENT/OBSERVATION).
    - Measure within-cohort Ns into :n_bmi_cohort :n_cog_cohort :n_frl_cohort :n_all3_cohort
      via SELECT COUNT(*) ... INTO ... TRIMMED with `is not missing` guards (copy the SECTION 3
      PROC SQL block from 07_cohort.sas verbatim, source = work.cohort_candidate).
    - Copy %bmi_pct verbatim (uses &n_admitted, &n_bmi_cohort; LACK derived from HAVE).
    - `ods listing;` to reopen the default destination (NOT `ods listing close;`).

    SECTION 4 (full-file assertions):
    - Copy %assert_complete_case_n(var=, expected=, outvar=) verbatim EXCEPT change the FROM
      clause from `g.master_data_merged` to `g.master_data_harmonized`.
    - Call it three times exactly:
        %assert_complete_case_n(var=Admit_BMI,       expected=12726, outvar=n_bmi_harm);
        %assert_complete_case_n(var=Cognitive_Score, expected=20540, outvar=n_cog_harm);
        %assert_complete_case_n(var=Frailty_Score,   expected=23311, outvar=n_frl_harm);
    - Do NOT include %assert_all_three. Instead measure the full-file all-three N into
      :n_all3_harm via a single PROC SQL SELECT COUNT(*) with the three `is not missing` guards
      against g.master_data_harmonized, and %put it (D-06: measure, never assert).
  </action>
  <verify>
    <automated>grep -c "set g.master_data_harmonized" sas/16b_cohort_rebuild.sas</automated>
  </verify>
  <acceptance_criteria>
    - sas/16b_cohort_rebuild.sas exists
    - grep "set g.master_data_harmonized" sas/16b_cohort_rebuild.sas returns a match
    - grep "upcase(strip(Patient_Type)) in ('INPATIENT','OBSERVATION')" sas/16b_cohort_rebuild.sas returns a match
    - grep "expected=12726" sas/16b_cohort_rebuild.sas returns a match
    - grep "expected=20540" sas/16b_cohort_rebuild.sas returns a match
    - grep "expected=23311" sas/16b_cohort_rebuild.sas returns a match
    - grep -c "assert_all_three" sas/16b_cohort_rebuild.sas returns 0
    - grep "16b_cohort_missingness.txt" sas/16b_cohort_rebuild.sas returns a match
    - grep "16b_cohort_tables.txt" sas/16b_cohort_rebuild.sas returns a match
    - grep -c "SQLOBS" sas/16b_cohort_rebuild.sas returns 0
    - grep "proc printto log=\"&logs_path.\\\\16b_cohort_rebuild.log\" new" sas/16b_cohort_rebuild.sas returns a match
  </acceptance_criteria>
  <done>SECTIONS 0-4 written; source dataset is g.master_data_harmonized throughout; three full-file assertions target 12726/20540/23311; all-three is measured not asserted; no &SQLOBS; log/QC files carry the 16b_ prefix.</done>
</task>

<task type="auto">
  <name>Task 2: Write SECTIONS 5-7 (promote, assert-harmonized-unchanged, QC summary, gate) and add h_* measurement</name>
  <read_first>
    - sas/16b_cohort_rebuild.sas (the file being extended -- read SECTIONS 0-4 written in Task 1)
    - sas/07_cohort.sas (SECTIONS 5-6: %verify_promotion, the data _null_ mod QC summary, %gate_on_status)
    - sas/10b_concept_harmonize.sas (SECTION 6 %assert_merged_unchanged pattern to adapt)
    - .planning/phases/16-rebuild-the-analytic-cohort/16-RESEARCH.md (h_* column list and Dataset-Unchanged pattern)
  </read_first>
  <action>
    Append the remaining sections to sas/16b_cohort_rebuild.sas.

    SECTION 5 (promote candidate -> g.analytic_cohort):
    - `data g.analytic_cohort; set work.cohort_candidate; run;` (full pass-through, all 174 columns,
      no KEEP/DROP -- D-09).
    - Copy %verify_promotion verbatim from 07_cohort.sas (compares g.analytic_cohort row count
      to &n_admitted, fails on mismatch).
    - Add a column-count check: PROC SQL count(*) into :n_cohort_cols trimmed from
      dictionary.columns where libname='G' and upcase(memname)='ANALYTIC_COHORT'; fail via
      %fail_out if `&n_cohort_cols ne 174` with message
      "g.analytic_cohort has &n_cohort_cols columns, expected 174 (full pass-through)."
      Put this inside a named macro (e.g. %verify_cohort_cols) per PCM-R-05.

    SECTION 6 (assert source unmodified -- new, adapted from 10b):
    - Define %macro assert_harmonized_unchanged; query :n_cols from dictionary.columns
      (libname='G', memname='MASTER_DATA_HARMONIZED') and :n_rows from g.master_data_harmonized;
      `%if &n_cols ne 174 or &n_rows ne 41150 %then %fail_out(msg=g.master_data_harmonized
      changed: &n_cols cols and &n_rows rows -- expected 174 and 41150);` then a NOTE confirming
      174 columns and 41150 rows unmodified. Call it.

    Also in SECTION 6, measure the 12 h_* within-cohort Ns (D-07, report only). Use a macro that
    loops the twelve names (H_DEATH_YN H_DIABETES H_FRAILTY_ACTIVITY H_FRAILTY_EXHAUST
    H_FRAILTY_GRIP H_FRAILTY_WALKING H_FRAILTY_WEIGHT H_HYPERLIPIDEMIA H_HYPERTENSION
    H_MOVEMENT_DISORDER H_SLEEP_APNEA H_SSDI_DEATH), for each running
    `select count(*) into :hn trimmed from work.cohort_candidate where &hvar is not missing;`
    and writing a `data _null_; file "&qc_path.\16b_cohort_missingness.txt" mod;` line
    `put "h_within_cohort_&hvar=&hn";` (or accumulate into a single data _null_ step). No assertion.

    SECTION 7 (QC summary + gate):
    - `data _null_; file "&qc_path.\16b_cohort_missingness.txt" mod;` writing grep-able
      key=value lines:
        cohort_filter_status=&cohort_status
        harmonized_n=&n_harmonized
        admitted_n=&n_admitted
        cohort_cols=&n_cohort_cols
        BMI_complete_case_n_harmonized=&n_bmi_harm
        Cognitive_complete_case_n_harmonized=&n_cog_harm
        Frailty_complete_case_n_harmonized=&n_frl_harm
        all_three_complete_case_n_harmonized=&n_all3_harm   (measured, not asserted)
        BMI_complete_case_n_cohort=&n_bmi_cohort
        Cognitive_complete_case_n_cohort=&n_cog_cohort
        Frailty_complete_case_n_cohort=&n_frl_cohort
        all_three_complete_case_n_cohort=&n_all3_cohort     (measured baseline -- D-06)
        pct_admitted_HAVE_bmi=&pct_bmi_have
        pct_admitted_LACK_bmi=&pct_bmi_lack
      plus a NOTE line stating the three _harmonized Ns were ASSERTED and passed, and that
      all _cohort values and the all-three N are MEASURED baselines (no expectation established).
    - Copy %gate_on_status verbatim (fails loudly AFTER the QC artifact is complete if
      cohort_status is ZERO_ROWS/ALL_ROWS/QUERY_FAILED).
    - Closing %put NOTE lines naming the two QC files.
    - `proc printto; run;` to restore the log.
  </action>
  <verify>
    <automated>grep -c "assert_harmonized_unchanged\|data g.analytic_cohort\|verify_promotion" sas/16b_cohort_rebuild.sas</automated>
  </verify>
  <acceptance_criteria>
    - grep "data g.analytic_cohort" sas/16b_cohort_rebuild.sas returns a match
    - grep "assert_harmonized_unchanged" sas/16b_cohort_rebuild.sas returns a match
    - grep "n_cols ne 174 or &n_rows ne 41150" sas/16b_cohort_rebuild.sas OR grep "ne 174" AND "ne 41150" both return matches
    - grep "verify_promotion" sas/16b_cohort_rebuild.sas returns a match
    - grep -c "H_SSDI_DEATH" sas/16b_cohort_rebuild.sas returns >= 1
    - grep -c "H_FRAILTY_GRIP" sas/16b_cohort_rebuild.sas returns >= 1
    - grep "all_three_complete_case_n_cohort=" sas/16b_cohort_rebuild.sas returns a match
    - grep "gate_on_status" sas/16b_cohort_rebuild.sas returns a match
    - grep -c "proc printto" sas/16b_cohort_rebuild.sas returns >= 2 (redirect + restore)
    - No non-ASCII bytes: `grep -nP "[^\x00-\x7F]" sas/16b_cohort_rebuild.sas` returns nothing
  </acceptance_criteria>
  <done>Full program complete end to end: promotes g.analytic_cohort with 174-column and row-count verification, asserts g.master_data_harmonized unchanged, measures all 12 h_* within-cohort Ns, writes the grep-able QC summary, gates on degenerate cohort, restores the log.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the SAS run</name>
  <action>Run sas/16b_cohort_rebuild.sas in a fresh SAS 9.4M8 session and confirm the results below before approving.</action>
  <what-built>sas/16b_cohort_rebuild.sas -- reads g.master_data_harmonized, filters to
  INPATIENT+OBSERVATION, promotes g.analytic_cohort (174 cols, ~13,890 rows), asserts full-file
  complete-case Ns, asserts the source unmodified.</what-built>
  <how-to-verify>
  1. Open SAS 9.4M8, start a FRESH session (restart if a prior program ended in %abort cancel).
  2. Submit: `%include "C:\Master_Renamed_same_format_accross\sas\16b_cohort_rebuild.sas";`
  3. Confirm the log at P:\...\logs\16b_cohort_rebuild.log has 0 ERROR lines.
  4. Confirm the log shows: "g.analytic_cohort promoted with 13890 rows" (or the measured N),
     the three complete-case assertions passed (12726 / 20540 / 23311), and
     "g.master_data_harmonized confirmed post-run -- 174 columns and 41150 rows -- unmodified".
  5. Open P:\...\qc\16b_cohort_missingness.txt and confirm admitted_n=13890, cohort_cols=174,
     and the all_three_complete_case_n_cohort=<measured> line is present.
  6. Optionally: PROC CONTENTS on g.analytic_cohort confirms 174 columns including H_* columns.
  Note the measured all-three-within-cohort N and the within-cohort Cognitive/Frailty/h_* Ns --
  Plan 02 records them in SUMMARY.md and STATE.md.
  </how-to-verify>
  <resume-signal>Type "approved" plus the measured all-three-within-cohort N, or describe the errors.</resume-signal>
</task>

</tasks>

<verification>
- Program is standalone SAS, source = g.master_data_harmonized throughout, never writes it except g.analytic_cohort.
- Three full-file assertions (12726/20540/23311) present; all-three and within-cohort Ns measured not asserted.
- g.analytic_cohort promoted only after all assertions pass; 174-column and row-count verification present.
- assert_harmonized_unchanged confirms source untouched (174 cols, 41,150 rows).
- No &SQLOBS; no in-place rewrite; every abort inside a named macro; pure ASCII.
</verification>

<success_criteria>
- sas/16b_cohort_rebuild.sas passes all Task 1 and Task 2 acceptance_criteria greps.
- Human-verified SAS run: 0 ERRORs, g.analytic_cohort ~13,890 rows / 174 cols, source unmodified.
- HARM-10 satisfied: cohort derives from harmonized, carries h_* columns, no dropped aliases.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rebuild-the-analytic-cohort/16-01-SUMMARY.md`
recording: program path, measured admitted N, measured within-cohort Cognitive/Frailty/all-three
Ns, measured h_* within-cohort Ns, and confirmation that g.master_data_harmonized was unmodified.
</output>
