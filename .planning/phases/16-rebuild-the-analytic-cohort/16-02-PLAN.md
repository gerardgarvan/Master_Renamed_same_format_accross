---
phase: 16-rebuild-the-analytic-cohort
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - docs/DECISIONS.md
  - .planning/STATE.md
autonomous: false
requirements: [PCM-D-05, HARM-10]
must_haves:
  truths:
    - "docs/DECISIONS.md PCM-D-05 entry is resolved (not Pending) with attribution to Gerard 2026-09-21 and Price's status named"
    - "The PCM-D-05 entry states the BMI-forces-restriction rationale and voids the old PCM-F-12 rationale"
    - "The entry contains all five Phase 13 population-shift figures including RACE=WHITE 79.8%->87.1%"
    - "The entry contains the BMI HAVE/LACK percentages against the 13,890 admitted denominator"
    - "STATE.md records the measured within-cohort and all-three baselines from the 16-01 run"
  artifacts:
    - path: "docs/DECISIONS.md"
      provides: "Resolved PCM-D-05 decision record with rationale, five figures, attribution"
      contains: "PCM-D-05"
  key_links:
    - from: "docs/DECISIONS.md PCM-D-05 entry"
      to: "16-01 QC output (16b_cohort_missingness.txt)"
      via: "BMI HAVE/LACK percentages and admitted N transcribed from the measured QC values"
      pattern: "13,?890"
---

<objective>
Resolve PCM-D-05 in docs/DECISIONS.md with full attribution, the BMI-forces-restriction
rationale, the BMI HAVE/LACK percentages against the admitted denominator, and all five
Phase 13 population-shift figures. Record the measured baselines from the 16-01 run in STATE.md.

Purpose: PCM-D-05 -- ROADMAP requires the cohort restriction to be "resolved and attributed",
not just resolved. Its original rationale (PCM-F-12) is void; the entry must state the true
rationale and what the restriction actually does to the population.
Output: updated docs/DECISIONS.md (committed), updated .planning/STATE.md (committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-rebuild-the-analytic-cohort/16-CONTEXT.md
@.planning/phases/16-rebuild-the-analytic-cohort/16-RESEARCH.md
@.planning/phases/16-rebuild-the-analytic-cohort/16-01-SUMMARY.md
@docs/DECISIONS.md

<interfaces>
<!-- Exact content required in the PCM-D-05 entry. Transcribe, do not re-measure. -->

Current DECISIONS.md row (to REPLACE):
  | PCM-D-05 | Analytic cohort INPATIENT/OBSERVATION restriction | Pending -- Phase 7 | TBD |

Attribution: Decided by Gerard, 2026-09-21. Price's status: [CHECKPOINT -- Gerard confirms
  "consulted" or "informed after the fact" before commit].

Cohort N: 13,890 (INPATIENT 13,223 + OBSERVATION 667).

BMI HAVE/LACK (denominator = 13,890 admitted, from the 16-01 QC file, NOT the 41,150 full file):
  Expected 91.6% HAVE, 8.4% LACK. Both directions spelled out. Do NOT use the shorthand
  "the ~53% gap".

Five Phase 13 population-shift figures (transcribe from ROADMAP Phase 16 SC-3/SC-4):
  Charlson 0:          60.8% (full) -> 34.5% (cohort)
  General anaesthesia: 57.6% -> 84.2%
  GI service:          18.4% -> 1.7%
  Colonoscopy:          8.5% -> 0.4%
  RACE=WHITE:          79.8% -> 87.1% (7.3-point shift -- required in any methods section)

Rationale: Admit_BMI forces the restriction -- all 12,726 BMI values are inside the admitted
  cohort, zero ambulatory. The OLD rationale (PCM-F-12: ambulatory patients never eligible for
  geriatric assessments) is VOID after MRG-06 / PCM-F-19 -- most cognitive and frailty scores
  belong to ambulatory rows. The restriction selects a clinically DIFFERENT population, not a
  convenience missingness filter.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Confirm Price status on PCM-D-05</name>
  <action>Gerard confirms whether Price was consulted before the decision or is being informed after the fact. The selected word is written into the DECISIONS.md attribution line in Task 1.</action>
  <decision>Price's status on PCM-D-05 for the DECISIONS.md attribution line.</decision>
  <context>ROADMAP requires PCM-D-05 "resolved and attributed". Gerard is the decider
  (2026-09-21). No codebase artifact records whether Price was consulted before the decision
  or will be informed after. The attribution line cannot be written accurately without this.</context>
  <options>
    <option id="consulted">
      <name>Price consulted before the decision</name>
      <pros>Strongest attribution; entry reads "Gerard (decider), Price consulted"</pros>
      <cons>Only valid if it actually happened</cons>
    </option>
    <option id="informed">
      <name>Price to be informed after the fact</name>
      <pros>Accurate if Price was not part of the decision</pros>
      <cons>Weaker sign-off; note it as a follow-up</cons>
    </option>
  </options>
  <resume-signal>Select: consulted or informed (this exact word goes in the attribution line).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Write the resolved PCM-D-05 entry in docs/DECISIONS.md</name>
  <read_first>
    - docs/DECISIONS.md (the file being modified -- find the PCM-D-05 row and the entry format
      used by other resolved decisions, e.g. PCM-D-11, to match structure)
    - .planning/phases/16-rebuild-the-analytic-cohort/16-01-SUMMARY.md (measured BMI HAVE/LACK
      percentages and admitted N from the 16-01 run)
    - .planning/phases/16-rebuild-the-analytic-cohort/16-RESEARCH.md (DECISIONS.md PCM-D-05
      Entry Requirements section -- the five required content items)
  </read_first>
  <action>
    Update the PCM-D-05 status. Change the summary table row from
    "| PCM-D-05 | Analytic cohort INPATIENT/OBSERVATION restriction | Pending -- Phase 7 | TBD |"
    to reflect Resolved status (Resolved -- Phase 16, 2026-09-21, Gerard).

    Add a full PCM-D-05 detail entry (matching the format of other resolved entries such as
    PCM-D-11) containing ALL of the following, using the concrete values from the interfaces block
    and the measured HAVE/LACK from the 16-01 SUMMARY:

    1. Resolution: admitted-only cohort (INPATIENT + OBSERVATION), N = 13,890
       (INPATIENT 13,223 + OBSERVATION 667).
    2. Rationale (NEW): Admit_BMI forces the restriction -- all 12,726 BMI values are inside the
       admitted cohort, zero ambulatory; any BMI analysis requires the restriction. State that
       the OLD rationale (PCM-F-12: ambulatory patients never eligible for geriatric assessments)
       is VOID after MRG-06 / PCM-F-19, because most cognitive and frailty scores belong to
       ambulatory rows.
    3. What the restriction DOES: selects a clinically different population, not a convenience
       missingness filter. Include all five Phase 13 figures verbatim:
         - Charlson 0: 60.8% -> 34.5%
         - General anaesthesia: 57.6% -> 84.2%
         - GI service: 18.4% -> 1.7%
         - Colonoscopy: 8.5% -> 0.4%
         - RACE=WHITE: 79.8% -> 87.1% (7.3-point shift; required in methods sections for generalisability)
    4. BMI availability within cohort: HAVE and LACK percentages (transcribe the measured
       values from 16-01, expected ~91.6% HAVE / ~8.4% LACK), denominator named explicitly as
       the 13,890 admitted cohort. Do NOT use the shorthand "the ~53% gap".
    5. Attribution: Decided by Gerard, 2026-09-21. Price: <consulted|informed> -- use the exact
       word selected in the checkpoint above.
    6. Cross-reference: rebuilt by sas/16b_cohort_rebuild.sas (Phase 16); supersedes the Phase 7
       cohort built from g.master_data_merged.

    Use pure ASCII (no smart quotes, no em-dash characters -- use "--").
  </action>
  <verify>
    <automated>grep -c "PCM-D-05" docs/DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Pending -- Phase 7" docs/DECISIONS.md returns 0 (the old status is gone)
    - grep "79.8% -> 87.1%\|79.8%.*87.1%" docs/DECISIONS.md returns a match (RACE=WHITE shift)
    - grep "60.8%.*34.5%" docs/DECISIONS.md returns a match (Charlson)
    - grep "57.6%.*84.2%" docs/DECISIONS.md returns a match (anaesthesia)
    - grep "18.4%.*1.7%" docs/DECISIONS.md returns a match (GI service)
    - grep "8.5%.*0.4%" docs/DECISIONS.md returns a match (colonoscopy)
    - grep "13,890\|13890" docs/DECISIONS.md returns a match (admitted N)
    - grep -i "PCM-F-12" docs/DECISIONS.md returns a match (old rationale named as void)
    - grep -i "Gerard" docs/DECISIONS.md returns a match near PCM-D-05 (attribution)
    - grep "16b_cohort_rebuild" docs/DECISIONS.md returns a match
    - `grep -nP "[^\x00-\x7F]" docs/DECISIONS.md` returns nothing new introduced by this edit
  </acceptance_criteria>
  <done>PCM-D-05 is Resolved in DECISIONS.md with the BMI-forces-restriction rationale, PCM-F-12 named as void, all five population-shift figures, BMI HAVE/LACK against the 13,890 denominator, and attribution to Gerard with Price's confirmed status.</done>
</task>

<task type="auto">
  <name>Task 2: Record measured Phase 16 baselines in STATE.md</name>
  <read_first>
    - .planning/STATE.md (the file being modified -- find the Performance Metrics table and the
      Established Decisions / Pending Decisions sections)
    - .planning/phases/16-rebuild-the-analytic-cohort/16-01-SUMMARY.md (the measured baseline values)
  </read_first>
  <action>
    Update .planning/STATE.md to record the Phase 16 outcome and the new measured baselines that
    did not exist before (they must live somewhere assertable, not only in a P: QC text file):

    1. In the Performance Metrics table, add rows for the rebuilt cohort:
       - "g.analytic_cohort rows (harmonized) | 13,890 | <measured> | 16b run" 
       - "g.analytic_cohort columns | 174 | <measured> | 16b run"
       - "All-three within-cohort N (baseline) | -- | <measured from 16-01> | 16b_cohort_missingness.txt"
       Transcribe the measured values from 16-01-SUMMARY.md (do not invent -- if the run has not
       produced them, this task is blocked on 16-01's checkpoint).
    2. Add a note under Established Decisions that PCM-D-05 is RESOLVED (admitted-only, BMI-forces-it,
       Gerard 2026-09-21) and that g.analytic_cohort is rebuilt from g.master_data_harmonized by
       sas/16b_cohort_rebuild.sas (HARM-10 satisfied).
    3. In the Pending Decisions section, move PCM-D-05 out of "Still open" -- it is now resolved.
    4. Record the within-cohort Cognitive N, Frailty N, and the 12 h_* within-cohort Ns as the
       newly established baselines (from 16-01-SUMMARY.md), noting these had never been established
       before Phase 16 (D-06, D-07).

    Update the Last Updated / Last Session line to reflect Phase 16 completion. Pure ASCII only.
  </action>
  <verify>
    <automated>grep -c "16b_cohort_rebuild\|13,890\|13890" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "16b_cohort_rebuild" .planning/STATE.md returns a match
    - grep "PCM-D-05" .planning/STATE.md returns a match in an Established/Resolved context (not only "Still open")
    - grep -i "13,890\|13890" .planning/STATE.md returns a match (rebuilt cohort N)
    - grep -i "all-three\|all_three" .planning/STATE.md returns a match (new baseline recorded)
    - grep -c "PCM-D-05.*pending\|pending.*PCM-D-05" .planning/STATE.md is 0 in the "Still open" block (it moved to resolved)
    - `grep -nP "[^\x00-\x7F]" .planning/STATE.md` introduces no new non-ASCII from this edit
  </acceptance_criteria>
  <done>STATE.md records the rebuilt cohort N/columns, PCM-D-05 as resolved, and the measured within-cohort + h_* + all-three baselines from the 16-01 run, so the next phase touching the cohort has values to assert against.</done>
</task>

</tasks>

<verification>
- DECISIONS.md PCM-D-05 is Resolved with attribution, BMI rationale, PCM-F-12 named void, all five figures, BMI HAVE/LACK against 13,890.
- STATE.md records the rebuilt cohort N/columns, PCM-D-05 resolved, and the new measured baselines.
- No new non-ASCII characters introduced in either file.
</verification>

<success_criteria>
- PCM-D-05 requirement satisfied: resolved AND attributed with the required rationale and figures.
- All Task 1 and Task 2 acceptance_criteria greps pass.
- Baselines from the 16-01 run persist in STATE.md, not only in the P: QC file.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rebuild-the-analytic-cohort/16-02-SUMMARY.md`
recording: the PCM-D-05 resolution text summary, Price's confirmed status, and the exact
measured baselines transcribed into STATE.md.
</output>
