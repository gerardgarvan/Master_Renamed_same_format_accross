---
phase: 18-supplemental-raw-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sas/00_config.sas
  - sas/16_raw_inventory.sas
  - sas/macros_raw_import.sas
  - sas/18_supplemental_raw_gap.sas
autonomous: true
requirements: [RAW-08, RAW-12]
user_setup: []

must_haves:
  truths:
    - "raw_path and D15_APPROVED are defined once in 00_config.sas; 16_raw_inventory.sas no longer carries its own %let raw_path"
    - "16_raw_inventory.sas is located on disk and committed; %import_csv / %import_xlsx live in sas/macros_raw_import.sas and are %included by both 16 and 18 (no second copy of the macro code exists)"
    - "The program scaffold sets validvarname=v7 validmemname=extend, assigns libname g, asserts g.analysis_base exists, and routes its log to logs\\18_supplemental_raw_gap.log"
    - "Section A writes qc\\18_id_diagnostic.txt: 5 base IDs not in r9, 5 r9 IDs not in base, 5 r7 numeric IDs rendered with best32., all with lengths; plus length() frequency tables for base-not-in-r9, r9-not-in-base, r7, and the matched IDs as reference"
    - "Section A joins on a derived _k = strip(cats(key)) so the same block works for char $12, char $18 and numeric keys"
    - "Section A never aborts; the only %abort cancel in the program is inside %fail_out"
  artifacts:
    - path: "sas/00_config.sas"
      provides: "raw_path and D15_APPROVED macro variables"
      contains: "%let raw_path"
    - path: "sas/macros_raw_import.sas"
      provides: "%import_csv and %import_xlsx, moved verbatim from 16_raw_inventory.sas"
      contains: "%macro import_csv"
    - path: "sas/18_supplemental_raw_gap.sas"
      provides: "Program scaffold + preconditions + Section A ID diagnostic"
      contains: "%assert_base"
    - path: "qc/18_id_diagnostic.txt"
      provides: "2022 ID mismatch diagnostic (written at runtime)"
  key_links:
    - from: "sas/18_supplemental_raw_gap.sas"
      to: "sas/00_config.sas"
      via: "%include then &raw_path and &D15_APPROVED references"
      pattern: "raw_path"
    - from: "sas/18_supplemental_raw_gap.sas"
      to: "sas/macros_raw_import.sas"
      via: "%include"
      pattern: "macros_raw_import"
    - from: "sas/18_supplemental_raw_gap.sas"
      to: "g.analysis_base"
      via: "libname g + %assert_base"
      pattern: "libname g"
---

<objective>
Establish the Phase 18 foundation and deliver Section A (the 2022 ID mismatch diagnostic).

Wave 0 first: locate `sas/16_raw_inventory.sas` on disk (it ran on 2026-09-16 from
`C:\Master_Renamed_same_format_accross\sas\`; the research glob that reported it missing ran
outside the repo), commit it, and move its two import macros verbatim into a shared
`sas/macros_raw_import.sas` that both 16 and 18 `%include`. Do NOT rebuild the macros from
prose — that is how divergence starts (18-RESEARCH.md Pattern 3, Don't Hand-Roll).

Move `raw_path` into `00_config.sas` so both programs share one definition (RAW-12), and put
`%let D15_APPROVED = 0;` there too — a `%let` inside program 18 is invisible to program 17, so
the gate can only block Phase 17 if the flag lives in config (see plan 02 Task 3).

Scaffold `sas/18_supplemental_raw_gap.sas` following the established PCM program pattern
(config include, session options, log routing, `libname g`, `%assert_base`, named-macro-only
conditionals). Implement Section A: diagnose why the 2022 cohort does not join, printing
samples and length distributions from three sources — the base, r9 (char $12) and r7
(numeric) — to `qc\18_id_diagnostic.txt`. Section A always runs and never aborts
(RAW-08 / PCM-D-16 is diagnosed, never auto-closed).

Purpose: Give Gerard the evidence to decide PCM-D-16, and lay the shared import + precondition
layer that Section B (plan 02) builds on.
Output: Updated `sas/00_config.sas` and `sas/16_raw_inventory.sas`, new
`sas/macros_raw_import.sas`, new `sas/18_supplemental_raw_gap.sas` (scaffold + Section A), and
at runtime `qc\18_id_diagnostic.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-supplemental-raw-inventory/18-CONTEXT.md
@.planning/phases/18-supplemental-raw-inventory/18-RESEARCH.md
@16-supplemental-raw-inventory.md
@sas/00_config.sas
@sas/16_raw_inventory.sas
@sas/17_summary_stats_by_domain.sas

<interfaces>
<!-- Established PCM patterns the executor MUST replicate. Extracted from sas/17_summary_stats_by_domain.sas. -->

Config include (first line of every program), then session options, then the shared macros:
```sas
%include "C:\Master_Renamed_same_format_accross\sas\00_config.sas";
options validvarname=v7 validmemname=extend nofmterr msglevel=i;
%include "&sas_path.\macros_raw_import.sas";
```
00_config.sas defines &sas_path &docs_path &source_path &g_path &qc_path &logs_path and the
&in_pipeline flag. It does NOT issue `libname g` — each program must. After Task 1 it also
defines &raw_path and &D15_APPROVED.

validmemname=extend is REQUIRED: sheet names such as 2018_2019_PRECEDE_DATABASE start with a
digit and can only be referenced as a name literal. validvarname=v7 is REQUIRED so that
"PRECEDE Study ID" (r3) imports as PRECEDE_Study_ID, as plan 02 assumes.

Log routing + fail_out + check_dir (copy this exact shape):
```sas
%macro route_log;
  %if &in_pipeline = 0 %then %do;
    proc printto log="&logs_path.\18_supplemental_raw_gap.log" new; run;
  %end;
%mend route_log;
%macro restore_log;
  %if &in_pipeline = 0 %then %do;
    proc printto; run;
  %end;
%mend restore_log;
%macro fail_out(msg=);
  %put ERROR: &msg;
  ods listing;
  %restore_log;
  %abort cancel;
%mend fail_out;
%macro check_dir(path=, label=);
  %if %sysfunc(fileexist(&path)) = 0 %then %do;
    %fail_out(msg=&label directory not found: &path);
  %end;
%mend check_dir;
```

assert_base pattern (via dictionary.tables, then row count):
```sas
%let n_tab_base = 0;
proc sql noprint;
  select count(*) into :n_tab_base trimmed
  from dictionary.tables where libname='G' and memname='ANALYSIS_BASE';
quit;
%macro assert_base;
  %if &n_tab_base ne 1 %then %do;
    %fail_out(msg=g.analysis_base not found in g library);
  %end;
%mend assert_base;
```

The two import macros, as they exist in 16_raw_inventory.sas (move verbatim; shown so the
executor can recognise them, not retype them):
```sas
%macro import_csv(rid, fname);
    proc import datafile="&raw_path\&fname"
        out=work.&rid dbms=csv replace;
        guessingrows=max;
    run;
    ...
%mend;
%macro import_xlsx(rid, fname);
    libname xin xlsx "&raw_path\&fname" access=readonly;
    ... sheets enumerated from dictionary.tables, copied via call execute into work.&rid._sN ...
    libname xin clear;
%mend;
```
Note their positional signature (rid, fname) and that %import_xlsx imports EVERY sheet as
work.&rid._s1, _s2, ... and records each in work.dslist. Plan 02 calls them with exactly this
signature. If the 16 versions insert into work.dslist, the shared file must create work.dslist
when it does not exist (a small %macro ensure_dslist guarded by %sysfunc(exist())).
</interfaces>

<raw_file_facts>
<!-- From 16-supplemental-raw-inventory.md Scope + Keys tables. -->
raw_path = P:\PeCAN Master Data\Gerard\raw   (root of the 9 supplemental files)

r9 = All_YEARS_LAT_LONG_20231127.csv, 41,150 rows, key PRECEDE_Study_ID char $12,
     31,935 match base, 9,215 raw-only IDs, 9,215 base-only IDs.
r7 = 2022_Education_20240124.csv, 9,215 rows, key PRECEDE_Study_ID NUMERIC, 0 match base.
r9 is CHARACTER and still mismatches, so a cats() numeric cast is NOT the explanation —
the diagnostic must display raw values + lengths from all three sides, never conclude a fix.
r7 is included because it shows the 2022 ID series in its numeric form: if those IDs have
more than 12 digits, that is direct evidence about the $12 base column.
</raw_file_facts>
</context>

<tasks>

<task type="auto">
  <n>Task 0: Locate and commit 16_raw_inventory.sas; move the import macros to a shared file</n>
  <read_first>
    - sas/16_raw_inventory.sas (on disk at C:\Master_Renamed_same_format_accross\sas\ — the %include path inside it proves the location)
    - 18-RESEARCH.md (Pattern 3 and the Runtime State Inventory note that the file was not found by a glob — that glob ran outside the repo)
  </read_first>
  <files>sas/16_raw_inventory.sas, sas/macros_raw_import.sas</files>
  <action>
    1. Confirm sas/16_raw_inventory.sas exists in the repo working tree. If `git status` shows it
       untracked, `git add sas/16_raw_inventory.sas` and commit it as "Phase 16: raw inventory
       program (executed 2026-09-16)". It contains no PHI (code only).
    2. Create sas/macros_raw_import.sas containing, verbatim (cut, not retyped), the %import_csv
       and %import_xlsx macro definitions from 16_raw_inventory.sas, plus a small guard so the
       file is safe to %include from any program:
         %macro ensure_dslist;
           %if %sysfunc(exist(work.dslist)) = 0 %then %do;
             data work.dslist; length rid $3 fname $80 sheet $64 memname $32; stop; run;
           %end;
         %mend ensure_dslist;
         %ensure_dslist
       Add `access=readonly` to the xin libname if it is not already there.
    3. In 16_raw_inventory.sas, replace the two macro definitions with
       `%include "&sas_path.\macros_raw_import.sas";` placed after the config include and the
       options statement. Do not change anything else in 16 except Task 1 item 3 below.
  </action>
  <verify>
    <automated>git ls-files sas/16_raw_inventory.sas; grep -c "%macro import_csv\|%macro import_xlsx" sas/macros_raw_import.sas; grep -c "%macro import_csv\|%macro import_xlsx" sas/16_raw_inventory.sas</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files sas/16_raw_inventory.sas` returns the path (file is tracked)
    - `sas/macros_raw_import.sas` contains exactly one `%macro import_csv` and one `%macro import_xlsx`
    - `sas/16_raw_inventory.sas` contains zero `%macro import_csv` / `%macro import_xlsx` and one `%include "&sas_path.\macros_raw_import.sas";`
  </acceptance_criteria>
  <done>The import macros exist in one place, both 16 and 18 can %include them, and 16 is under version control.</done>
</task>

<task type="auto">
  <n>Task 1: Add raw_path and D15_APPROVED to 00_config.sas; remove the local raw_path from 16</n>
  <read_first>
    - sas/00_config.sas (see current %let path block — add raw_path alongside source_path)
    - 16-supplemental-raw-inventory.md (Scope section confirms the directory: P:\PeCAN Master Data\Gerard\raw)
    - sas/16_raw_inventory.sas (has `%let raw_path = P:\PeCAN Master Data\Gerard\raw;` near the top)
  </read_first>
  <files>sas/00_config.sas, sas/16_raw_inventory.sas</files>
  <action>
    1. In the "Data paths (P: -- NOT in git, PHI lives here)" block of sas/00_config.sas, after the
       existing `%let logs_path = ...;` line, add exactly:

         /* ---- Raw supplemental source path (P: -- NOT in git) ---- */
         %let raw_path = P:\PeCAN Master Data\Gerard\raw;

    2. In a new block after the path block, add:

         /* ---- Phase 18 / PCM-D-15 approval gate ----
            0 = awaiting Gerard review of qc\18_gap_candidates.txt
            1 = approved -- lets 18_supplemental_raw_gap.sas complete and lets
                17_summary_stats_by_domain.sas build work.analysis_base_ext */
         %let D15_APPROVED = 0;

    3. Add matching NOTE lines alongside the existing NOTE block at the bottom:

         %put NOTE: [00_config] raw_path     = &raw_path;
         %put NOTE: [00_config] D15_APPROVED = &D15_APPROVED;

    4. In sas/16_raw_inventory.sas delete the local `%let raw_path = ...;` line so the config
       definition is the only one.

    Do NOT touch any other line of either file. Do NOT add libname statements to config. Keep
    ASCII only, no apostrophes or embedded semicolons inside %put text (PCM rule).
  </action>
  <verify>
    <automated>grep -n "raw_path\|D15_APPROVED" sas/00_config.sas; grep -c "%let raw_path" sas/16_raw_inventory.sas</automated>
  </verify>
  <acceptance_criteria>
    - `sas/00_config.sas` contains `%let raw_path = P:\PeCAN Master Data\Gerard\raw;`
    - `sas/00_config.sas` contains `%let D15_APPROVED = 0;`
    - `sas/00_config.sas` contains both `%put NOTE: [00_config] raw_path` and `%put NOTE: [00_config] D15_APPROVED`
    - No `libname` statement added to 00_config.sas (grep "libname g" sas/00_config.sas returns nothing)
    - `grep -c "%let raw_path" sas/16_raw_inventory.sas` returns 0
  </acceptance_criteria>
  <done>raw_path and D15_APPROVED are defined once in config and echoed as NOTEs; 16 reads raw_path from config.</done>
</task>

<task type="auto">
  <n>Task 2: Scaffold 18_supplemental_raw_gap.sas with preconditions</n>
  <read_first>
    - sas/17_summary_stats_by_domain.sas (lines 155-270 — copy route_log/restore_log/fail_out/check_dir/assert_base shape exactly)
    - sas/00_config.sas (confirm &raw_path and &D15_APPROVED now exist after Task 1)
    - sas/macros_raw_import.sas (the import macros and their positional signature)
    - 16-supplemental-raw-inventory.md (Scope table for file names, Keys table for key column names and types)
  </read_first>
  <files>sas/18_supplemental_raw_gap.sas</files>
  <action>
    Create sas/18_supplemental_raw_gap.sas as a fresh program. Structure:

    SECTION 0 — Header + config include + options + shared macros:
      - Program header comment block (Purpose: two-section diagnostic; Reads g.analysis_base and
        raw files read-only; Writes qc\18_id_diagnostic.txt and qc\18_gap_candidates.txt; the only
        %abort cancel is inside %fail_out; the D15 gate at the end reads &D15_APPROVED from config).
      - `%include "C:\Master_Renamed_same_format_accross\sas\00_config.sas";`
      - `options validvarname=v7 validmemname=extend nofmterr msglevel=i;`
      - `%include "&sas_path.\macros_raw_import.sas";`

    SECTION 1 — Macro library (copy the exact shapes from the <interfaces> block above):
      - %route_log / %restore_log (route to "&logs_path.\18_supplemental_raw_gap.log", guarded by &in_pipeline=0)
      - %fail_out(msg=) — %put ERROR, ods listing, %restore_log, %abort cancel. This is the ONLY macro that may contain %abort cancel.
      - %check_dir(path=, label=)
      - %assert_base (dictionary.tables count on libname='G' memname='ANALYSIS_BASE', then %fail_out if ne 1)
      Do NOT define %import_csv or %import_xlsx here — they come from the shared include.

    SECTION 2 — Preconditions (run before any work, in this order):
      - %check_dir(path=&logs_path, label=logs)   /* logs first, before route */
      - %route_log;
      - libname g "&g_path";
      - %check_dir(path=&qc_path, label=qc)
      - %check_dir(path=&raw_path, label=raw)
      - Build &n_tab_base via the dictionary.tables query, then %assert_base;
      - %put NOTE: ==== Phase 18 supplemental raw gap diagnostic starting ====;

    Leave clearly commented placeholders `/* SECTION A: 2022 ID diagnostic -- Task 3 */`,
    `/* SECTION B: gap counts -- plan 02 */`, `/* SECTION C: D15 gate -- plan 02 */` and a
    final `%restore_log;` at the very bottom.

    RULES (PCM, enforced project-wide — violating any is a defect):
      - No bare open-code %IF/%THEN; all conditional logic inside named macros.
      - %abort cancel only inside %fail_out.
      - No %PUT text containing apostrophes or embedded semicolons.
      - Never write to g.* or any raw file (read-only). g.analysis_base only ever on the RIGHT of set/merge.
      - Use `where type = 'num'` / `'char'` for dictionary.columns filters, never `type = 1` (PCM-T-13).
      - Explicit `SELECT COUNT(*) INTO :mac TRIMMED`, never &SQLOBS.
      - No in-place rewrite `data X; set X;` (PCM-T-02).
  </action>
  <verify>
    <automated>grep -n "macros_raw_import\|validmemname=extend\|libname g \|%assert_base\|18_supplemental_raw_gap.log" sas/18_supplemental_raw_gap.sas; grep -c "%abort cancel" sas/18_supplemental_raw_gap.sas</automated>
  </verify>
  <acceptance_criteria>
    - File `sas/18_supplemental_raw_gap.sas` exists
    - Contains `%include "C:\Master_Renamed_same_format_accross\sas\00_config.sas";`
    - Contains `options validvarname=v7 validmemname=extend`
    - Contains `%include "&sas_path.\macros_raw_import.sas";` and does NOT contain `%macro import_csv` or `%macro import_xlsx`
    - Contains `libname g "&g_path";`
    - Contains `%macro fail_out` and exactly one `%abort cancel` (grep -c returns 1)
    - Contains `18_supplemental_raw_gap.log` in the route_log macro
    - Contains commented placeholders for SECTION A, SECTION B and SECTION C
  </acceptance_criteria>
  <done>Program scaffold compiles conceptually: config and shared macros included, options set, libname g assigned, g.analysis_base asserted, log routed. Only one %abort cancel, inside %fail_out.</done>
</task>

<task type="auto">
  <n>Task 3: Implement Section A — 2022 ID mismatch diagnostic (base, r9, r7)</n>
  <read_first>
    - sas/18_supplemental_raw_gap.sas (the scaffold from Task 2 — fill the SECTION A placeholder)
    - 16-supplemental-raw-inventory.md (2022 mismatch section — r9 char, 9,215 raw-only / 9,215 base-only; r7 numeric, 0 match)
    - 18-RESEARCH.md (Code Examples: ID Diagnostic Anti-Join — adapt to the _k key below; Pitfall 2)
  </read_first>
  <files>sas/18_supplemental_raw_gap.sas</files>
  <action>
    Fill the SECTION A placeholder. Section A ALWAYS runs and NEVER aborts — it only writes a file.

    A-1  Import r9 and r7 read-only with the shared macros (positional signature):
           %import_csv(r9, All_YEARS_LAT_LONG_20231127.csv)
           %import_csv(r7, 2022_Education_20240124.csv)
         These create work.r9 and work.r7.

    A-2  Build one-column key datasets, all on a derived character key so the same code works
         for char $12, char $18 and numeric keys:
           data work.base_ids;  set g.analysis_base(keep=PRECEDE_STUDY_ID);
             length _k $32; _k = strip(cats(PRECEDE_STUDY_ID)); id_length = length(_k);
             if _k ne ''; keep _k id_length; run;
           data work.r9_ids;    set work.r9(keep=PRECEDE_Study_ID);
             length _k $32; _k = strip(cats(PRECEDE_Study_ID)); id_length = length(_k);
             if _k ne ''; keep _k id_length; run;
           data work.r7_ids;    set work.r7(keep=PRECEDE_Study_ID);
             length _k $32 id_best32 $32;
             _k = strip(cats(PRECEDE_Study_ID));
             id_best32 = strip(put(PRECEDE_Study_ID, best32.));   /* full precision rendering */
             id_length = length(id_best32);
             if _k ne ''; keep _k id_best32 id_length; run;
         proc sort nodupkey by _k on all three.

    A-3  Anti-joins on _k:
           work.base_not_in_r9  (in base, not in r9)
           work.r9_not_in_base  (in r9, not in base)
           work.matched_ids     (in both)  -- reference set
         Standard merge-by-_k with in= flags.

    A-4  Sample sets: first 5 rows (obs=5) of base_not_in_r9, r9_not_in_base, and r7_ids.

    A-5  PROC FREQ on id_length for the FULL sets: base_not_in_r9, r9_not_in_base, r7_ids, and
         matched_ids (the last is the reference — expected to be all 12). Output to work datasets.

    A-6  Write qc\18_id_diagnostic.txt via DATA _NULL_ with `file "&qc_path.\18_id_diagnostic.txt"`.
         The file MUST contain, clearly labelled and in this order:
           - Header naming the mismatch: 2022 cohort; base N; r9 N; r7 N; matched N; base-only N; r9-only N
           - "5 BASE IDs NOT IN r9"          value + id_length
           - "5 r9 IDs NOT IN BASE"          value + id_length
           - "5 r7 IDs (numeric, best32.)"   id_best32 + id_length
           - "LENGTH DISTRIBUTION -- base IDs not in r9"
           - "LENGTH DISTRIBUTION -- r9 IDs not in base"
           - "LENGTH DISTRIBUTION -- r7 IDs (numeric key rendered best32.)"
           - "LENGTH DISTRIBUTION -- matched IDs (reference)"
           - A closing line: "PCM-D-16 remains open -- no cast or fix applied by this program"
         Use only ASCII, no apostrophes or embedded semicolons in put text.

    Do NOT attempt any cast/fix, and do NOT %abort. Section A is diagnosis only (PCM-D-16 open).
  </action>
  <verify>
    <automated>grep -n "import_csv(r9\|import_csv(r7\|18_id_diagnostic.txt\|base_not_in_r9\|r9_not_in_base\|matched_ids\|id_best32\|id_length" sas/18_supplemental_raw_gap.sas; grep -c "%abort cancel" sas/18_supplemental_raw_gap.sas</automated>
  </verify>
  <acceptance_criteria>
    - Section A imports r9 and r7 via `%import_csv(r9, All_YEARS_LAT_LONG_20231127.csv)` and `%import_csv(r7, 2022_Education_20240124.csv)`
    - All key datasets derive `_k = strip(cats(...))` with an explicit `length _k $32`
    - Contains `work.base_not_in_r9`, `work.r9_not_in_base` and `work.matched_ids` anti-join/merge data steps by `_k`
    - Contains `put(PRECEDE_Study_ID, best32.)` for r7
    - Contains PROC FREQ on `id_length` for all four sets
    - Contains `file "&qc_path.\18_id_diagnostic.txt"`
    - Total `%abort cancel` count in file remains 1
  </acceptance_criteria>
  <done>Section A produces qc\18_id_diagnostic.txt with three 5-ID sample blocks (base, r9, r7 best32) and four length-frequency tables; program continues without aborting.</done>
</task>

</tasks>

<verification>
- `git ls-files sas/16_raw_inventory.sas` returns the path; `sas/macros_raw_import.sas` holds the only copy of the import macros.
- `grep "raw_path\|D15_APPROVED" sas/00_config.sas` shows both %lets and both NOTEs; 16 has no local raw_path.
- `grep -c "%abort cancel" sas/18_supplemental_raw_gap.sas` returns exactly 1 (inside %fail_out only).
- `sas/18_supplemental_raw_gap.sas` contains the options line, shared-macro include, libname g, %assert_base, and Section A.
- On a SAS run (manual): `qc\18_id_diagnostic.txt` exists and contains three sample blocks with lengths plus four length-frequency tables; the program does not abort in Section A.
- `grep -i "ERROR\|ABORT" logs/18_supplemental_raw_gap.log` shows no ERROR from Section A (log on P:; check manually).
</verification>

<success_criteria>
- RAW-12: raw_path (and D15_APPROVED) live in 00_config.sas and are referenced by 18_supplemental_raw_gap.sas; the import macros exist in exactly one file.
- RAW-08: 2022 ID diagnostic written to qc\18_id_diagnostic.txt with base / r9 / r7 samples and four length distributions; no abort.
- Shared import macros and preconditions are in place for plan 02 Section B.
- All PCM rules honored: single %abort cancel in %fail_out, no bare open-code %IF, read-only on g.* and raw\, ASCII-only %put.
</success_criteria>

<o>
After completion, create `.planning/phases/18-supplemental-raw-inventory/18-01-SUMMARY.md`
</o>
