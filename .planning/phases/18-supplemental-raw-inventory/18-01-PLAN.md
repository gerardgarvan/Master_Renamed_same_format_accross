---
phase: 18-supplemental-raw-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sas/00_config.sas
  - sas/18_supplemental_raw_gap.sas
autonomous: true
requirements: [RAW-08, RAW-12]
user_setup: []

must_haves:
  truths:
    - "raw_path is defined once in 00_config.sas and read by 18_supplemental_raw_gap.sas"
    - "The program scaffold assigns libname g, asserts g.analysis_base exists, and routes its log to logs\\18_supplemental_raw_gap.log"
    - "%import_csv and %import_xlsx macros are reconstructed and can import a raw file read-only"
    - "Section A writes qc\\18_id_diagnostic.txt: 5 base IDs not in r9 beside 5 r9 IDs not in base with lengths, plus length() frequency tables for both non-matching sets"
    - "Section A never aborts; the only %abort cancel in the program is the D15 gate (added in plan 02)"
  artifacts:
    - path: "sas/00_config.sas"
      provides: "raw_path macro variable"
      contains: "%let raw_path"
    - path: "sas/18_supplemental_raw_gap.sas"
      provides: "Program scaffold + import macros + Section A ID diagnostic"
      contains: "%macro import_csv"
    - path: "qc/18_id_diagnostic.txt"
      provides: "2022 ID mismatch diagnostic (written at runtime)"
  key_links:
    - from: "sas/18_supplemental_raw_gap.sas"
      to: "sas/00_config.sas"
      via: "%include then &raw_path reference"
      pattern: "raw_path"
    - from: "sas/18_supplemental_raw_gap.sas"
      to: "g.analysis_base"
      via: "libname g + %assert_base"
      pattern: "libname g"
---

<objective>
Establish the Phase 18 foundation and deliver Section A (the 2022 ID mismatch diagnostic).

Move `raw_path` into `00_config.sas` so both `16` and `18` share one definition (RAW-12). Scaffold `sas/18_supplemental_raw_gap.sas` following the established PCM program pattern (config include, log routing, `libname g`, `%assert_base`, named-macro-only conditionals). Reconstruct the `%import_csv` / `%import_xlsx` macros — the Phase 16 program `16_raw_inventory.sas` was never committed to git, so the macros must be rebuilt from the documented pattern, not imported. Implement Section A: diagnose why the 2022 cohort (r9 raw-only / base-only IDs) does not join, printing samples and length distributions to `qc\18_id_diagnostic.txt`. Section A always runs and never aborts (RAW-08 / PCM-D-16 is diagnosed, never auto-closed).

Purpose: Give Gerard the evidence to decide PCM-D-16, and lay the reusable import + precondition layer that Section B (plan 02) builds on.
Output: Updated `sas/00_config.sas`, new `sas/18_supplemental_raw_gap.sas` (scaffold + macros + Section A), and at runtime `qc\18_id_diagnostic.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-supplemental-raw-inventory/18-CONTEXT.md
@.planning/phases/18-supplemental-raw-inventory/18-RESEARCH.md
@16-supplemental-raw-inventory.md
@sas/00_config.sas
@sas/17_summary_stats_by_domain.sas

<interfaces>
<!-- Established PCM patterns the executor MUST replicate. Extracted from sas/17_summary_stats_by_domain.sas. -->

Config include (first line of every program):
```sas
%include "C:\Master_Renamed_same_format_accross\sas\00_config.sas";
```
00_config.sas defines &sas_path &docs_path &source_path &g_path &qc_path &logs_path and the
&in_pipeline flag. It does NOT issue `libname g` — each program must. raw_path is NOT yet in config.

Log routing + fail_out + check_dir (copy this exact shape):
```sas
%macro route_log;
  %if &in_pipeline = 0 %then %do;
    proc printto log="&logs_path.\18_supplemental_raw_gap.log" new; run;
  %end;
%mend route_log;
%macro restore_log;
  %if &in_pipeline = 0 %then %do;
    proc printto; run;
  %end;
%mend restore_log;
%macro fail_out(msg=);
  %put ERROR: &msg;
  ods listing;
  %restore_log;
  %abort cancel;
%mend fail_out;
%macro check_dir(path=, label=);
  %if %sysfunc(fileexist(&path)) = 0 %then %do;
    %fail_out(msg=&label directory not found: &path);
  %end;
%mend check_dir;
```

assert_base pattern (via dictionary.tables, then row count):
```sas
%let n_tab_base = 0;
proc sql noprint;
  select count(*) into :n_tab_base trimmed
  from dictionary.tables where libname='G' and memname='ANALYSIS_BASE';
quit;
%macro assert_base;
  %if &n_tab_base ne 1 %then %do;
    %fail_out(msg=g.analysis_base not found in g library);
  %end;
%mend assert_base;
```
</interfaces>

<raw_file_facts>
<!-- From 16-supplemental-raw-inventory.md Scope + Keys tables. -->
raw_path = P:\PeCAN Master Data\Gerard\raw   (root of the 9 supplemental files)

r9 = All_YEARS_LAT_LONG_20231127.csv, 41,150 rows, key PRECEDE_STUDY_ID char $12,
     31,935 match base, 9,215 raw-only IDs, 9,215 base-only IDs.
r9 is CHARACTER and still mismatches, so a cats() numeric cast is NOT the explanation —
the diagnostic must display raw values + lengths, never conclude a fix.
</raw_file_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add raw_path to 00_config.sas</name>
  <read_first>
    - sas/00_config.sas (see current %let path block — add raw_path alongside source_path)
    - 16-supplemental-raw-inventory.md (line 15 confirms the directory: P:\PeCAN Master Data\Gerard\raw)
  </read_first>
  <files>sas/00_config.sas</files>
  <action>
    In the "Data paths (P: -- NOT in git, PHI lives here)" block of sas/00_config.sas, after the
    existing `%let logs_path = ...;` line, add exactly:

      /* ---- Raw supplemental source path (P: -- NOT in git) ---- */
      %let raw_path = P:\PeCAN Master Data\Gerard\raw;

    Then add a matching NOTE line alongside the existing NOTE block at the bottom:

      %put NOTE: [00_config] raw_path    = &raw_path;

    Do NOT touch any other line. Do NOT add libname statements. Keep ASCII only, no apostrophes
    or embedded semicolons inside %put text (PCM rule).
  </action>
  <verify>
    <automated>grep -n "raw_path" sas/00_config.sas</automated>
  </verify>
  <acceptance_criteria>
    - `sas/00_config.sas` contains `%let raw_path = P:\PeCAN Master Data\Gerard\raw;`
    - `sas/00_config.sas` contains `%put NOTE: [00_config] raw_path`
    - No `libname` statement added to 00_config.sas (grep "libname g" sas/00_config.sas returns nothing)
  </acceptance_criteria>
  <done>raw_path is defined once in config and echoed as a NOTE; no other config line changed.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold 18_supplemental_raw_gap.sas with reconstructed import macros and preconditions</name>
  <read_first>
    - sas/17_summary_stats_by_domain.sas (lines 155-270 — copy route_log/restore_log/fail_out/check_dir/assert_base shape exactly)
    - sas/00_config.sas (confirm &raw_path now exists after Task 1)
    - 16-supplemental-raw-inventory.md (Scope table for file names, Keys table for key column names and types)
    - .planning/phases/18-supplemental-raw-inventory/18-RESEARCH.md (Pattern 3 import reuse; note 16_raw_inventory.sas is NOT in git)
  </read_first>
  <files>sas/18_supplemental_raw_gap.sas</files>
  <action>
    Create sas/18_supplemental_raw_gap.sas as a fresh program. Structure:

    SECTION 0 — Header + config include:
      - Program header comment block (Purpose: two-section diagnostic; Reads g.analysis_base and
        raw files read-only; Writes qc\18_id_diagnostic.txt and qc\18_gap_candidates.txt; the only
        %abort cancel is the D15 gate at the very end).
      - `%include "C:\Master_Renamed_same_format_accross\sas\00_config.sas";`

    SECTION 1 — Macro library (copy the exact shapes from the <interfaces> block above):
      - %route_log / %restore_log (route to "&logs_path.\18_supplemental_raw_gap.log", guarded by &in_pipeline=0)
      - %fail_out(msg=) — %put ERROR, ods listing, %restore_log, %abort cancel. This is the ONLY macro that may contain %abort cancel.
      - %check_dir(path=, label=)
      - %assert_base (dictionary.tables count on libname='G' memname='ANALYSIS_BASE', then %fail_out if ne 1)

      Then reconstruct the two import macros (16_raw_inventory.sas is not committed, so build them):

      %macro import_csv(file=, out=);
        /* read-only CSV import, guessingrows=max so wide/typed columns are inferred fully */
        proc import datafile="&raw_path.\&file"
          out=&out dbms=csv replace;
          guessingrows=max;
        run;
      %mend import_csv;

      %macro import_xlsx(file=, sheet=, out=);
        /* read-only XLSX import via the XLSX engine libname, one sheet at a time */
        libname xin xlsx "&raw_path.\&file";
        data &out;
          set xin."&sheet"n;
        run;
        libname xin clear;
      %mend import_xlsx;

    SECTION 2 — Preconditions (run before any work, in this order):
      - %check_dir(path=&logs_path, label=logs)   /* logs first, before route */
      - %route_log;
      - libname g "&g_path";
      - %check_dir(path=&qc_path, label=qc)
      - %check_dir(path=&raw_path, label=raw)
      - Build &n_tab_base via the dictionary.tables query, then %assert_base;
      - %put NOTE: ==== Phase 18 supplemental raw gap diagnostic starting ====;

    Leave a clearly commented placeholder `/* SECTION A: 2022 ID diagnostic -- Task 3 */` and
    `/* SECTION B + D15 gate -- plan 02 */` and a final `%restore_log;` at the very bottom for now
    (Section A fills in Task 3; the gate is added in plan 02, which will move %restore_log below it).

    RULES (PCM, enforced project-wide — violating any is a defect):
      - No bare open-code %IF/%THEN; all conditional logic inside named macros.
      - %abort cancel only inside %fail_out.
      - No %PUT text containing apostrophes or embedded semicolons.
      - Never write to g.* or any raw file (read-only). g.analysis_base only ever on the RIGHT of set/merge.
      - Use `where type = 'num'` / `'char'` for dictionary.columns filters, never `type = 1` (PCM-T-13).
      - Explicit `SELECT COUNT(*) INTO :mac TRIMMED`, never &SQLOBS.
  </action>
  <verify>
    <automated>grep -n "%macro import_csv\|%macro import_xlsx\|libname g \|%assert_base\|18_supplemental_raw_gap.log" sas/18_supplemental_raw_gap.sas</automated>
  </verify>
  <acceptance_criteria>
    - File `sas/18_supplemental_raw_gap.sas` exists
    - Contains `%include "C:\Master_Renamed_same_format_accross\sas\00_config.sas";`
    - Contains `%macro import_csv` and `%macro import_xlsx`
    - Contains `guessingrows=max`
    - Contains `libname g "&g_path";`
    - Contains `%macro fail_out` and exactly one `%abort cancel` (grep -c "%abort cancel" returns 1)
    - Contains `18_supplemental_raw_gap.log` in the route_log macro
    - Contains a commented placeholder for SECTION A
  </acceptance_criteria>
  <done>Program scaffold compiles conceptually: config included, import macros defined, libname g assigned, g.analysis_base asserted, log routed. Only one %abort cancel, inside %fail_out.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Section A — 2022 ID mismatch diagnostic</name>
  <read_first>
    - sas/18_supplemental_raw_gap.sas (the scaffold from Task 2 — fill the SECTION A placeholder)
    - 16-supplemental-raw-inventory.md (lines 83-85: 2022 mismatch — r9 char, 9,215 raw-only / 9,215 base-only; diagnostic instruction)
    - .planning/phases/18-supplemental-raw-inventory/18-RESEARCH.md (Code Examples: ID Diagnostic Anti-Join, Section A-2)
  </read_first>
  <files>sas/18_supplemental_raw_gap.sas</files>
  <action>
    Fill the SECTION A placeholder in sas/18_supplemental_raw_gap.sas. Section A ALWAYS runs and
    NEVER aborts — it only writes a file. Implement exactly:

    A-1  Import r9 read-only:
         %import_csv(file=All_YEARS_LAT_LONG_20231127.csv, out=work.raw_r9)

    A-2  Extract distinct key IDs from both sides. The r9 key column is PRECEDE_STUDY_ID (char $12);
         the base key is PRECEDE_STUDY_ID. Compare on strip(cats()) to be type-safe:
           proc sort data=g.analysis_base(keep=PRECEDE_STUDY_ID) out=work.base_ids nodupkey; by PRECEDE_STUDY_ID; run;
           proc sort data=work.raw_r9(keep=PRECEDE_STUDY_ID)     out=work.r9_ids   nodupkey; by PRECEDE_STUDY_ID; run;

    A-3  Anti-join both directions, capturing id length:
           data work.base_not_in_r9;
             merge work.base_ids(in=inb) work.r9_ids(in=inr);
             by PRECEDE_STUDY_ID;
             if inb and not inr;
             id_length = length(strip(PRECEDE_STUDY_ID));
           run;
           data work.r9_not_in_base;
             merge work.r9_ids(in=inr) work.base_ids(in=inb);
             by PRECEDE_STUDY_ID;
             if inr and not inb;
             id_length = length(strip(PRECEDE_STUDY_ID));
           run;

    A-4  Take the first 5 rows of each anti-join (obs=5) as the sample sets.

    A-5  Build length() frequency tables for the FULL non-matching sets (not just the 5 samples):
         PROC FREQ on id_length for work.base_not_in_r9 and for work.r9_not_in_base, output to work datasets.

    A-6  Write qc\18_id_diagnostic.txt using a DATA _NULL_ with `file "&qc_path.\18_id_diagnostic.txt"`.
         The file MUST contain, clearly labelled:
           - A header line naming the mismatch (2022 cohort, r9 raw-only and base-only counts)
           - "5 BASE IDs NOT IN r9" with each id value and its id_length
           - "5 r9 IDs NOT IN BASE" with each id value and its id_length
           - "LENGTH DISTRIBUTION -- base IDs not in r9" (id_length, count) from the PROC FREQ output
           - "LENGTH DISTRIBUTION -- r9 IDs not in base" (id_length, count) from the PROC FREQ output
         Print the two 5-sample sets side by side if practical; otherwise one block then the other.
         Use only ASCII, no apostrophes or embedded semicolons in put text.

    Do NOT attempt any cast/fix, and do NOT %abort. Section A is diagnosis only (PCM-D-16 open).
  </action>
  <verify>
    <automated>grep -n "import_csv(file=All_YEARS_LAT_LONG\|18_id_diagnostic.txt\|base_not_in_r9\|r9_not_in_base\|id_length" sas/18_supplemental_raw_gap.sas</automated>
  </verify>
  <acceptance_criteria>
    - Section A imports r9 via `%import_csv(file=All_YEARS_LAT_LONG_20231127.csv`
    - Contains both `work.base_not_in_r9` and `work.r9_not_in_base` anti-join data steps
    - Contains `id_length = length(strip(PRECEDE_STUDY_ID))` (or equivalent length of the key) in both anti-joins
    - Contains a PROC FREQ on `id_length` for both non-matching sets
    - Contains `file "&qc_path.\18_id_diagnostic.txt"` (or `&qc_path.\18_id_diagnostic.txt` in a filename= / file statement)
    - No `%abort cancel` added in Section A (total %abort cancel count in file remains 1)
  </acceptance_criteria>
  <done>Section A produces qc\18_id_diagnostic.txt with 5+5 sample IDs (with lengths) and length-frequency tables for both non-matching sets; program continues without aborting.</done>
</task>

</tasks>

<verification>
- `grep "raw_path" sas/00_config.sas` shows the new %let and NOTE.
- `grep -c "%abort cancel" sas/18_supplemental_raw_gap.sas` returns exactly 1 (inside %fail_out only).
- `sas/18_supplemental_raw_gap.sas` contains import macros, libname g, %assert_base, and Section A.
- On a SAS run (manual): `qc\18_id_diagnostic.txt` exists and contains two 5-ID sample blocks with lengths plus two length-frequency tables; the program does not abort in Section A.
- `grep -i "ERROR\|ABORT" logs/18_supplemental_raw_gap.log` shows no unexpected ERROR (log on P:; check manually).
</verification>

<success_criteria>
- RAW-12: raw_path lives in 00_config.sas and is referenced by 18_supplemental_raw_gap.sas.
- RAW-08: 2022 ID diagnostic written to qc\18_id_diagnostic.txt with 5+5 samples and length distributions; no abort.
- Reusable import macros and preconditions are in place for plan 02 Section B.
- All PCM rules honored: single %abort cancel in %fail_out, no bare open-code %IF, read-only on g.* and raw\, ASCII-only %put.
</success_criteria>

<output>
After completion, create `.planning/phases/18-supplemental-raw-inventory/18-01-SUMMARY.md`
</output>
