---
phase: 18-supplemental-raw-inventory
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - sas/18_supplemental_raw_gap.sas
autonomous: true
requirements: [RAW-09, RAW-10, RAW-11]
user_setup: []

must_haves:
  truths:
    - "Section B computes, on matched IDs only, per-column gap counts for r1, r2, r3, r4, r5, r6, r9 (r7 and r8 excluded)"
    - "IN_BASE columns report n_matched, n_fillable, n_equal, n_conflict; NEW columns report n_raw_populated"
    - "r2 dCDT and LINUS families are reported as rollups (family, n_cols, median n_raw_populated), not 3,885 individual rows"
    - "r2 section-divider columns are identified and excluded from the NEW candidate metric, listed as 'section divider -- excluded'"
    - "The program ends with %let D15_APPROVED=0 and a gate macro that aborts (via %fail_out) until the flag is set to 1"
  artifacts:
    - path: "sas/18_supplemental_raw_gap.sas"
      provides: "Section B gap counts + r2 rollups + D15 gate"
      contains: "%let D15_APPROVED"
    - path: "qc/18_gap_candidates.txt"
      provides: "Per-column per-file gap candidate table (written at runtime)"
  key_links:
    - from: "sas/18_supplemental_raw_gap.sas"
      to: "g.analysis_base"
      via: "merge on strip(cats()) key for matched-ID gap counts"
      pattern: "analysis_base"
    - from: "sas/18_supplemental_raw_gap.sas gate"
      to: "downstream Phase 17"
      via: "%let D15_APPROVED=0 + %gate_d15 as last executable line"
      pattern: "D15_APPROVED"
---

<objective>
Deliver Section B (the gap-fill candidate table) and close the program with the PCM-D-15 approval gate.

On matched IDs only, for each file that has matched IDs (r1, r2, r3, r4, r5, r6, r9 — r7 and r8 are
excluded until PCM-D-16 is resolved), count per column how much value the raw file could add:
for IN_BASE columns the fillable / equal / conflict split, and for NEW columns the raw-populated
count. r2's 3,885 NEW columns are dominated by dCDT (3,341) and LINUS (251) feature blocks — these
are rolled up as families so the clinically interesting candidates are findable without reading
4,000 rows. The program ends with `%let D15_APPROVED=0` and a gate macro (identical mechanics to
Phase 17's `DOMAIN_MAP_APPROVED`) that blocks any re-run past that line until Gerard reviews
`qc\18_gap_candidates.txt` and sets the flag.

Purpose: Produce the exact table PCM-D-15 needs, and hard-gate the downstream Phase 17 gap-fill on
Gerard's approval.
Output: Section B + D15 gate appended to `sas/18_supplemental_raw_gap.sas`; at runtime `qc\18_gap_candidates.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-supplemental-raw-inventory/18-CONTEXT.md
@.planning/phases/18-supplemental-raw-inventory/18-RESEARCH.md
@16-supplemental-raw-inventory.md
@sas/18_supplemental_raw_gap.sas
@sas/17_summary_stats_by_domain.sas

<interfaces>
<!-- The D15 gate pattern, copied exactly from sas/17_summary_stats_by_domain.sas. -->

Gate (last executable lines, before %restore_log):
```sas
%let D15_APPROVED = 0;   /* Gerard sets to 1 after reviewing qc\18_gap_candidates.txt */
%macro gate_d15;
  %if &D15_APPROVED ne 1 %then %do;
    %fail_out(msg=PCM-D-15 awaiting approval -- review 18_gap_candidates.txt then set D15_APPROVED=1);
  %end;
%mend gate_d15;
%gate_d15;
%restore_log;
```
%fail_out already exists from plan 01 and contains the only %abort cancel.

Type-safe equality (from 18-RESEARCH.md Pattern 2):
```sas
length _r $200 _b $200;
_r = strip(cats(raw_val));
_b = strip(cats(base_val));
fillable = (missing(base_val) and not missing(raw_val));
equal    = (not missing(raw_val) and not missing(base_val) and _r = _b);
conflict = (not missing(raw_val) and not missing(base_val) and _r ne _b);
```
</interfaces>

<gap_scope_facts>
<!-- From 18-CONTEXT.md D-02 and 16-supplemental-raw-inventory.md overlap/keys tables. -->
Files to process (matched-ID counts) and their key columns:
  r1  2018_2019_2020_Induction_Emergent20231121.csv   key PRECEDE_Study_ID $18   22,472 matched   IN_BASE cols: 3 (rt_RM_START_to_INDUCTION / EMERGENCE etc.), NEW: 0
  r2  2018_2019_Precede_Database.xlsx  sheet 2018_2019_PRECEDE_DATABASE   key studyid $12   14,778 matched   IN_BASE: 102, NEW: 3,885
  r3  2018_2022_COLONOSCOPY_20240118.xlsx  sheet Sheet1   key "PRECEDE Study ID" $12   2,688 matched   IN_BASE: 8, NEW: 0
  r4  2020_Precede_Database_Edu.xlsx  sheet 2020_PRECEDE_DATABASE_EDU   key studyid $12   7,695 matched   IN_BASE: 0, NEW: 3 (Edu_Years, edu_categorical)
  r5  2021_Education_20240124.csv   key PRECEDE_Study_ID $12   9,462 matched   IN_BASE: 2, NEW: 0
  r6  2021_Frailty_20240123.csv   key PRECEDE_Study_ID $12   9,462 matched   IN_BASE: 1, NEW: 5 (five frailty components)
  r9  All_YEARS_LAT_LONG_20231127.csv   key PRECEDE_STUDY_ID $12   31,935 matched   IN_BASE: 4, NEW: 0

EXCLUDED: r7 (2022_Education), r8 (2022_RES) — numeric key, 0 matches, gated on PCM-D-16.

Key aliases: r2/r4 use `studyid`; r3 uses `PRECEDE Study ID` (space, becomes PRECEDE_Study_ID on import);
r1 uses PRECEDE_Study_ID ($18); r5/r6/r9 use PRECEDE_STUDY_ID/PRECEDE_Study_ID. ALWAYS join on
strip(cats(key)) so $18 vs $12 and any char/num difference does not break the match.

r2 section-divider columns (100% missing, NOT data — exclude from NEW metric, list as dividers):
  IDR Variables Only, Bloods, LINUS, garvan_added_variables, ron_extra_for_sabya,
  DigitalClockData, PeCANers, Other Variables

r2 large NEW families to ROLL UP (family, n_cols, median n_raw_populated):
  COM_dCDT (~2,082), COPY_dCDT (~1,259), LINUS (~251). Collectively these are the dCDT/LINUS blocks.
</gap_scope_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Section B — per-column gap-fill counts for r1, r3, r4, r5, r6, r9</name>
  <read_first>
    - sas/18_supplemental_raw_gap.sas (the SECTION B placeholder left by plan 01, and the import macros / %fail_out already defined)
    - 16-supplemental-raw-inventory.md (Keys table for key column names/types; overlap table for IN_BASE/NEW counts per file)
    - .planning/phases/18-supplemental-raw-inventory/18-RESEARCH.md (Pattern 2 type-aware comparison; Code Examples: Type-Safe Column Comparison)
  </read_first>
  <files>sas/18_supplemental_raw_gap.sas</files>
  <action>
    Fill the SECTION B placeholder for the six SMALL files (r1, r3, r4, r5, r6, r9); r2 is handled
    in Task 2. Build a generalized per-file, per-column pass. Recommended structure — a macro
    %gap_file(rid=, file=, sheet=, key=, out=) that:

      1. Imports the raw file read-only (%import_csv for CSV, %import_xlsx with the given sheet for XLSX).
      2. Derives a normalized join key: _k = strip(cats(<raw key column>)) on the raw side, and the
         base side uses _k = strip(cats(PRECEDE_STUDY_ID)). Sort both by _k.
      3. Determines, from dictionary.columns, which non-key raw columns are IN_BASE (name matches a
         g.analysis_base column, case-insensitive on normalized SAS name) vs NEW.
      4. Restricts to MATCHED IDs (inner join / merge with in= on both, keep only rows in both).
      5. For each IN_BASE non-key column, computes with the type-safe rule (from <interfaces>):
           n_matched  = matched row count for the file,
           n_fillable = base missing AND raw populated,
           n_equal    = both populated AND strip(cats()) equal,
           n_conflict = both populated AND strip(cats()) differ.
      6. For each NEW column, computes n_raw_populated = count of matched rows where raw value is not missing.
      7. Emits one observation per column into a stacked results dataset work.gap_results with columns:
           rid, file, column, bucket (IN_BASE / NEW), n_matched, n_fillable, n_equal, n_conflict, n_raw_populated.

    Call %gap_file for r1, r3, r4, r5, r6, r9 with the file names, sheets, and key columns from the
    <gap_scope_facts> block above. Do NOT call it for r2 (Task 2), r7, or r8.

    Because column counts vary and up to 8 IN_BASE columns exist per small file, iterate columns
    programmatically (PROC TRANSPOSE to long, or a call-execute loop over dictionary.columns), NOT
    by hand-enumerating each column name.

    RULES: no bare open-code %IF (all logic in named macros); read-only on g.* and raw files
    (g.analysis_base only on the RIGHT of set/merge); use `where type='char'`/`'num'` for
    dictionary.columns (PCM-T-13); explicit SELECT COUNT(*) INTO :mac TRIMMED, never &SQLOBS;
    ASCII-only %put, no apostrophes/semicolons in put text; do NOT add any %abort cancel.
  </action>
  <verify>
    <automated>grep -n "gap_file\|gap_results\|n_fillable\|n_conflict\|n_raw_populated\|strip(cats" sas/18_supplemental_raw_gap.sas</automated>
  </verify>
  <acceptance_criteria>
    - Section B defines a per-file gap macro invoked for r1, r3, r4, r5, r6, r9 (grep shows the six file names: 2018_2019_2020_Induction, 2018_2022_COLONOSCOPY, 2020_Precede_Database_Edu, 2021_Education, 2021_Frailty, All_YEARS_LAT_LONG)
    - Contains computation of `n_fillable`, `n_equal`, `n_conflict` (IN_BASE) and `n_raw_populated` (NEW)
    - Contains `strip(cats(` used for the key join and/or value equality
    - Does NOT process r7 or r8 (no `2022_Education` and no `2022_RES` file references in Section B)
    - Total `%abort cancel` count in file is still 1
  </acceptance_criteria>
  <done>work.gap_results holds one row per column for r1/r3/r4/r5/r6/r9 with bucket and all five counts, computed on matched IDs with type-safe comparison.</done>
</task>

<task type="auto">
  <name>Task 2: r2 gap counts with dCDT/LINUS family rollups and divider exclusion, then write qc\18_gap_candidates.txt</name>
  <read_first>
    - sas/18_supplemental_raw_gap.sas (Section B from Task 1 — reuse %gap_file / work.gap_results)
    - 16-supplemental-raw-inventory.md (r2 structure table lines 103-123: families, col counts, section dividers, genuine new clinical columns)
    - .planning/phases/18-supplemental-raw-inventory/18-RESEARCH.md (Pattern 4 r2 Family Rollup; Pitfall 4 section dividers; Pitfall 5 unreadable 3,885 rows)
  </read_first>
  <files>sas/18_supplemental_raw_gap.sas</files>
  <action>
    Add r2 processing and the final report writer.

    r2 handling:
      1. Import r2 read-only: %import_xlsx(file=2018_2019_Precede_Database.xlsx,
         sheet=2018_2019_PRECEDE_DATABASE, out=work.raw_r2). Re-assert the row ceiling is not hit
         (row count 14,807 << 1,048,575) with a %put NOTE, not an abort.
      2. Compute IN_BASE gap counts (n_fillable/n_equal/n_conflict) for r2's 102 IN_BASE columns
         using the same type-safe pass as Task 1 (studyid is the key, join on strip(cats(studyid))).
         Append these rows to work.gap_results with rid=r2.
      3. EXCLUDE the 8 section-divider columns from the NEW metric. Identify them by name — the
         SAS-imported forms of: IDR Variables Only, Bloods, LINUS, garvan_added_variables,
         ron_extra_for_sabya, DigitalClockData, PeCANers, Other Variables. Emit these to a separate
         work.r2_dividers dataset (column name + "section divider -- excluded"), NOT to gap_results.
      4. For r2 NEW columns, classify each into a FAMILY by name prefix/pattern: COM_dCDT, COPY_dCDT,
         LINUS -> family "dCDT/LINUS"; everything else -> family "r2 other new". Roll up the
         dCDT/LINUS family to one row: family, n_cols, median(n_raw_populated) via PROC MEANS/SUMMARY
         (median). Store rollup rows in work.r2_family_rollup.
      5. For the "r2 other new" columns (the genuinely new clinical content — Edu_Years,
         edu_categorical, Frailty_Score, frailty_score_category, mmse_3word_repeat, mmse_3word_recall,
         GripStrength_trial_1..3, handedness, age, Primary_Payer, Insurance, Payer_categ,
         SSDI_Death_Y_N, average_pain, Death_Indicator, blood_* labs), emit ONE row per column into
         work.gap_results with bucket=NEW and n_raw_populated on matched IDs. These are the review candidates.

    Final report writer — write qc\18_gap_candidates.txt via DATA _NULL_ (file "&qc_path.\18_gap_candidates.txt"):
      - TOP: a per-file summary block (rid, file, n_matched, count of IN_BASE cols, count of NEW cols).
      - IN_BASE detail: all IN_BASE rows across all files, sorted by n_fillable DESCENDING.
      - NEW detail: all NEW rows (small files + r2 "other new" candidates), sorted by n_raw_populated DESCENDING.
      - r2 FAMILY ROLLUP block: dCDT/LINUS family rows (family, n_cols, median n_raw_populated).
      - APPENDIX: the r2 section-divider list (name + "section divider -- excluded").
      Sort within blocks per D-04: IN_BASE by n_fillable desc, NEW by n_raw_populated desc.
      ASCII only, no apostrophes/semicolons in put text.

    RULES: same as Task 1 (named-macro conditionals, read-only, PCM-T-13 type filter, no &SQLOBS,
    no new %abort cancel).
  </action>
  <verify>
    <automated>grep -n "2018_2019_Precede_Database\|r2_dividers\|r2_family_rollup\|18_gap_candidates.txt\|median\|dCDT" sas/18_supplemental_raw_gap.sas</automated>
  </verify>
  <acceptance_criteria>
    - Imports r2 via `%import_xlsx(file=2018_2019_Precede_Database.xlsx` with sheet `2018_2019_PRECEDE_DATABASE`
    - Contains a section-divider exclusion list including `Bloods`, `LINUS`, `garvan_added_variables`, `Other Variables`
    - Contains a family rollup producing `n_cols` and a `median` of n_raw_populated for the dCDT/LINUS family
    - Writes `&qc_path.\18_gap_candidates.txt`
    - Report has a per-file summary block, an IN_BASE detail block, a NEW detail block, a family rollup block, and a divider appendix
    - Total `%abort cancel` count in file is still 1
  </acceptance_criteria>
  <done>qc\18_gap_candidates.txt lists per-file summary, IN_BASE columns sorted by n_fillable desc, NEW columns sorted by n_raw_populated desc, dCDT/LINUS as family rollups, and section dividers in an appendix.</done>
</task>

<task type="auto">
  <name>Task 3: Add the PCM-D-15 approval gate and finalize log restore</name>
  <read_first>
    - sas/18_supplemental_raw_gap.sas (end of file — the placeholder for the gate + the final %restore_log)
    - sas/17_summary_stats_by_domain.sas (lines 145, 196-200: DOMAIN_MAP_APPROVED gate mechanics)
  </read_first>
  <files>sas/18_supplemental_raw_gap.sas</files>
  <action>
    At the END of sas/18_supplemental_raw_gap.sas, AFTER Section B writes qc\18_gap_candidates.txt
    and BEFORE the final %restore_log, add exactly (copy the shape from <interfaces>):

      %let D15_APPROVED = 0;   /* Gerard sets to 1 after reviewing qc\18_gap_candidates.txt */

      %macro gate_d15;
        %if &D15_APPROVED ne 1 %then %do;
          %fail_out(msg=PCM-D-15 awaiting approval -- review 18_gap_candidates.txt then set D15_APPROVED=1);
        %end;
      %mend gate_d15;

      %gate_d15;

    Ensure %restore_log is called AFTER %gate_d15 only on the success path — note that %fail_out
    already calls %restore_log itself, so when the gate fires the log is restored inside %fail_out.
    On the pass path (D15_APPROVED=1), %gate_d15 does nothing and the trailing %restore_log runs.

    Confirm the gate uses the existing %fail_out (the only %abort cancel). Do NOT add a second
    %abort cancel. ASCII only, no apostrophes/semicolons in the %fail_out message text.
  </action>
  <verify>
    <automated>grep -n "D15_APPROVED\|gate_d15\|%restore_log" sas/18_supplemental_raw_gap.sas</automated>
  </verify>
  <acceptance_criteria>
    - Contains `%let D15_APPROVED = 0;`
    - Contains `%macro gate_d15` whose body calls `%fail_out` when `&D15_APPROVED ne 1`
    - Contains `%gate_d15;` invoked after the gap-candidates file write
    - `%gate_d15;` appears after the `18_gap_candidates.txt` write in file order
    - Total `%abort cancel` count in the file is exactly 1 (grep -c returns 1)
  </acceptance_criteria>
  <done>Program ends with the D15 gate: with D15_APPROVED=0 the run aborts via %fail_out after writing both QC files; setting the flag to 1 lets it complete. No second %abort cancel introduced.</done>
</task>

</tasks>

<verification>
- `grep -c "%abort cancel" sas/18_supplemental_raw_gap.sas` returns exactly 1.
- `grep "D15_APPROVED" sas/18_supplemental_raw_gap.sas` shows the %let, the gate macro, and the invocation.
- Section B references r1, r2, r3, r4, r5, r6, r9 and does NOT reference r7 (2022_Education) or r8 (2022_RES).
- On a SAS run (manual): with D15_APPROVED=0 the program writes qc\18_id_diagnostic.txt AND qc\18_gap_candidates.txt, then aborts at the gate. qc\18_gap_candidates.txt shows the per-file summary, IN_BASE (sorted by n_fillable desc), NEW (sorted by n_raw_populated desc), dCDT/LINUS family rollups, and a divider appendix.
- `grep -i "ERROR" logs/18_supplemental_raw_gap.log` shows only the expected PCM-D-15 gate ERROR line (log on P:; check manually).
</verification>

<success_criteria>
- RAW-09: qc\18_gap_candidates.txt holds per-column per-file gap counts (n_matched, n_fillable, n_equal, n_conflict for IN_BASE; n_raw_populated for NEW) on matched IDs for r1/r2/r3/r4/r5/r6/r9; r7/r8 excluded.
- RAW-10: r2 dCDT/LINUS reported as family rollups; section dividers excluded and listed as such.
- RAW-11: %let D15_APPROVED=0 gate present; program aborts (via %fail_out) until the flag is set to 1, blocking downstream Phase 17.
- All PCM rules honored: single %abort cancel in %fail_out, no bare open-code %IF, read-only on g.* and raw\, type-aware comparison, no &SQLOBS.
</success_criteria>

<output>
After completion, create `.planning/phases/18-supplemental-raw-inventory/18-02-SUMMARY.md`
</output>
