---
phase: 19-raw-directory-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - sas/19_raw_dir_inventory.sas
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05, INV-06]

must_haves:
  truths:
    - "Every file under raw appears in FILES with path, size, last-modified date, and SHA-256 checksum"
    - "Every readable data file (csv, xlsx/xls, sas7bdat) receives row count, column count, and per-variable rows"
    - "Every variable in every readable file appears in VARIABLES with pct_missing and pct_sentinel as separate columns"
    - "Every key-column naming variant for PRECEDE_STUDY_ID, ENCRYPTED_MRN, ENCRYPTED_ENCOUNTER is detected per PCM-T-12"
    - "Every file has exactly one of three statuses: profiled, listed-not-profiled, read-failed"
    - "Each md1-md8 master extract is asserted present before profiling begins; missing any one aborts"
    - "All md1-md8 and r1-r9 files are flagged known; any other file is flagged NEW"
    - "FAMILIES full-join assertion passes: per-file column totals in FAMILIES match VARIABLES"
    - "INV-06 three-term assertion passes: n_profiled + n_listed_not_profiled + n_read_failed = n_total_files"
  artifacts:
    - path: "sas/19_raw_dir_inventory.sas"
      provides: "Complete SAS program implementing Sections 0-14 of the inventory pipeline"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INV-07 updated to list KEY, FILES, SHEETS, VARIABLES, KEY_COLUMNS, RECONCILIATION, FAMILIES"
  key_links:
    - from: "sas/19_raw_dir_inventory.sas Section 4"
      to: "md1-md8 presence assertion"
      via: "%abort cancel inside %fail_out if any named extract absent"
      pattern: "fail_out.*md[1-8].*not found"
    - from: "sas/19_raw_dir_inventory.sas Section 6"
      to: "work.files_out status column"
      via: "&syserr check after each import attempt"
      pattern: "syserr.*read-failed"
    - from: "sas/19_raw_dir_inventory.sas Section 9"
      to: "FAMILIES full-join assertion"
      via: "%assert_families macro with FULL JOIN and coalesce"
      pattern: "full join.*coalesce"
---

<objective>
Update REQUIREMENTS.md to add FAMILIES and KEY to INV-07, then write the complete
`sas/19_raw_dir_inventory.sas` program (all 14 sections): header and config, utility
macros, preconditions, directory traversal, presence assertions, SHA-256 checksums, import
loop with error trapping, variable profiling, key-column detection, FAMILIES assignment,
RECONCILIATION, all embedded assertions, and PROC EXPORT for the Phase 20 CSV handoff.
The ODS Excel output assembly (Section 13) is included in this plan to keep the program
complete as a single deliverable.

Purpose: Phase 19 delivers a complete, checksummed, variable-level inventory of the raw
directory tree. Plan 01 produces the full working program; Plan 02 runs it and verifies
the outputs.

Output: `.planning/REQUIREMENTS.md` (INV-07 updated); `sas/19_raw_dir_inventory.sas`
(complete program, self-contained, runnable in a clean SAS batch session).
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-raw-directory-inventory/19-CONTEXT.md
@.planning/phases/19-raw-directory-inventory/19-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs. All verified from existing project code. -->

From sas/00_config.sas (defines all path macros):
  &raw_path   — P:\PeCAN Master Data\Gerard\raw
  &qc_path    — P: qc directory
  &logs_path  — P: logs directory
  &sas_path   — C:\Master_Renamed_same_format_accross\sas
  &g_path     — P: g library root
  &in_pipeline — 0 (standalone) or 1 (called from runner)

From sas/macros_raw_import.sas:
  %import_csv(rid, filename)   — PROC IMPORT guessingrows=max; appends rid to &dslist
  %import_xlsx(rid, filename)  — XLSX libname; enumerates sheets via dictionary.tables;
                                  copies each sheet to work.&rid_&sheetname via call execute
  %ensure_dslist               — verifies &dslist is non-empty; %fail_out if not

From sas/16_raw_inventory.sas (copy these macros verbatim into Section 1):
  %macro route_log   — proc printto log="&logs_path.\16_raw_inventory.log" new if &in_pipeline=0
  %macro restore_log — proc printto; run; if &in_pipeline=0
  %macro fail_out(msg=) — %put ERROR: &msg; ods listing; %restore_log; %abort cancel;
  %macro check_dir(path=, label=) — %fail_out if %sysfunc(fileexist(&path))=0

Key-detection block pattern (from sas/16_raw_inventory.sas):
  Sweep dictionary.columns for all PRECEDE_STUDY_ID variants:
    upcase(name) in ('PRECEDE_STUDY_ID','PRECEDESTUDYID','PRECEDE STUDY ID',
                     'STUDYID','STUDY_ID')
  For ENCRYPTED_MRN variants:
    upcase(name) in ('ENCRYPTED_MRN','ENCRYPTEDMRN','ENCRYPTED MRN','MRN')
  For ENCRYPTED_ENCOUNTER variants:
    upcase(name) in ('ENCRYPTED_ENCOUNTER','ENCRYPTEDENCOUNTER','ENCRYPTED ENCOUNTER',
                     'ENCOUNTER','ENCOUNTER_ID')
  Also check VARnn columns: if label matches a variant string (XLSX engine renames
  space-containing headers to VARnn; the original header becomes the label)

ODS Excel KEY-first sheet order (from sas/17_summary_stats_by_domain.sas):
  ods excel file="&qc_path.\19_raw_inventory.xlsx" style=styles.pearl
      options(embedded_titles='yes');
  ods excel options(sheet_name='KEY');   /* written first = leftmost */
  proc print data=work.key_legend noobs; run;
  ods excel options(sheet_name='FILES');
  ...
  ods excel close;

UF blue header color: #0021A5 (applied via PROC TEMPLATE or column header style override)

SELECT COUNT(*) INTO pattern (no &SQLOBS anywhere):
  proc sql noprint;
    select count(*) into :macvar trimmed from ... ;
  quit;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md INV-07 to add FAMILIES and KEY sheets</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (current INV-07 text — must see exact wording before editing)
    - .planning/phases/19-raw-directory-inventory/19-CONTEXT.md (D-04 FAMILIES sheet decision, D-05 KEY-leftmost decision, the note at top of domain section about INV-07 needing update)
  </read_first>

  <action>
    Find the INV-07 line in .planning/REQUIREMENTS.md. Currently it reads:
      "User can open `qc/19_raw_inventory.xlsx` with KEY sheet leftmost, UF blue (#0021A5)
       headers, and sheets FILES, SHEETS, VARIABLES, KEY_COLUMNS, RECONCILIATION"

    Replace the sheet list to include FAMILIES and make KEY explicit as a data sheet in
    the enumeration. The corrected INV-07 text must read:

      "**INV-07** — User can open `qc/19_raw_inventory.xlsx` with KEY sheet leftmost, UF
       blue (#0021A5) column headers, and the following sheets in order: KEY (legend),
       FILES, SHEETS, VARIABLES, KEY_COLUMNS, RECONCILIATION, FAMILIES; the KEY sheet is
       a column-by-column legend describing every field in every other sheet, including a
       note that `type` in VARIABLES reflects the SAS-imported type (not the source system
       type); the FAMILIES sheet lists known wide-column families with per-file column
       counts and missingness range (min, median, max)"

    Also update the INV-06 requirement text. Currently INV-06 reads:
      "assertion holds: files listed = files profiled + files listed-not-profiled, zero
       files of unknown status"

    Replace with the three-term form that matches D-06:
      "assertion holds: n_profiled + n_listed_not_profiled + n_read_failed = n_total_files,
       zero files of unknown status; `read-failed` covers any readable-extension file whose
       import failed (locked, corrupt, or XLSX sheet-count shortfall)"

    Write the updated file.
  </action>

  <verify>
    Read .planning/REQUIREMENTS.md and confirm:
    - INV-07 line contains 'FAMILIES' and 'KEY (legend)'
    - INV-07 line lists sheets in order: KEY, FILES, SHEETS, VARIABLES, KEY_COLUMNS, RECONCILIATION, FAMILIES
    - INV-06 line contains 'read-failed' and 'n_read_failed'
  </verify>

  <acceptance_criteria>
    - grep "FAMILIES" .planning/REQUIREMENTS.md returns a match on the INV-07 line
    - grep "KEY (legend)" .planning/REQUIREMENTS.md returns a match on the INV-07 line
    - grep "read-failed" .planning/REQUIREMENTS.md returns a match on the INV-06 line
    - grep "n_read_failed" .planning/REQUIREMENTS.md returns a match on the INV-06 line
  </acceptance_criteria>

  <done>
    REQUIREMENTS.md INV-07 lists all seven sheets (KEY, FILES, SHEETS, VARIABLES,
    KEY_COLUMNS, RECONCILIATION, FAMILIES) and INV-06 references the three-status model
    with read-failed included.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write sas/19_raw_dir_inventory.sas — complete program (all 14 sections)</name>
  <files>sas/19_raw_dir_inventory.sas</files>

  <read_first>
    - sas/16_raw_inventory.sas (copy %route_log, %restore_log, %fail_out, %check_dir,
      key-detection block verbatim; reference program structure and PCM compliance header)
    - sas/macros_raw_import.sas (understand %import_csv and %import_xlsx interfaces before
      calling them; do not redefine)
    - sas/17_summary_stats_by_domain.sas (ODS Excel KEY-first sheet ordering pattern,
      UF blue header approach)
    - sas/00_config.sas (confirm exact macro variable names: &raw_path, &qc_path,
      &logs_path, &sas_path, &g_path, &in_pipeline)
    - .planning/phases/19-raw-directory-inventory/19-CONTEXT.md (all locked decisions
      D-01 through D-07 with exact code patterns)
    - .planning/phases/19-raw-directory-inventory/19-RESEARCH.md (all eight patterns with
      verbatim SAS code; anti-patterns to avoid; pitfalls checklist)
  </read_first>

  <action>
    Create sas/19_raw_dir_inventory.sas with 14 numbered sections. Every design choice
    below is LOCKED in CONTEXT.md; do not substitute alternatives.

    -----------------------------------------------------------------------
    SECTION 0 — Header, %include, options
    -----------------------------------------------------------------------
    PCM compliance header block (same style as 16_raw_inventory.sas lines 1-27):
      Program: 19_raw_dir_inventory.sas
      Purpose: Recursive inventory of &raw_path; seven-sheet workbook +
               machine-readable CSV handoff for Phase 20
      Reads: everything under &raw_path (read-only)
      Writes: qc/19_raw_inventory.xlsx, qc/19_raw_files.csv,
              logs/19_raw_dir_inventory.log
      Does NOT write to any g.* dataset
      XCMD required: certutil is called via FILENAME PIPE; the batch session
        must have XCMD enabled (shared constraint with RUN-01 in Phase 21)
      PCM compliance notes: same as 16_raw_inventory.sas header

    Then:
      %include "C:\Master_Renamed_same_format_accross\sas\00_config.sas";
      options validvarname=v7 validmemname=extend nofmterr msglevel=i;
      options nosyntaxcheck noerrorabend;   /* MUST precede any PROC IMPORT */
      %include "&sas_path.\macros_raw_import.sas";

    -----------------------------------------------------------------------
    SECTION 1 — Utility macros (copy verbatim from 16_raw_inventory.sas)
    -----------------------------------------------------------------------
    Copy %route_log, %restore_log exactly as they appear in 16_raw_inventory.sas
    lines 35-45, but change the log filename to 19_raw_dir_inventory.log.

    Copy %fail_out exactly from 16_raw_inventory.sas lines 47-52, then ADD
    `ods excel close;` on the line before `%restore_log;` so that an abort
    during ODS Excel does not leave the destination open.

    Copy %check_dir exactly from 16_raw_inventory.sas lines 54-58.

    -----------------------------------------------------------------------
    SECTION 2 — Preconditions
    -----------------------------------------------------------------------
    %check_dir(path=&logs_path, label=logs);
    %route_log;
    %check_dir(path=&qc_path, label=qc);
    %check_dir(path=&raw_path, label=raw);
    %put NOTE: ==== Phase 19 raw directory inventory starting ====;

    NOTE: Phase 19 does NOT read g.* datasets. Do NOT include a libname g or
    %assert_base call — this program is standalone.

    -----------------------------------------------------------------------
    SECTION 3 — Directory traversal
    -----------------------------------------------------------------------
    Use FILENAME PIPE with dir /s /b /a-d (the /a-d flag excludes subdirectory
    name lines — without it, folder paths appear as FILES rows, certutil fails
    on them, and the INV-06 assertion trips):

      filename dirpipe pipe "dir /s /b /a-d ""&raw_path""";
      data work.files_raw;
        infile dirpipe truncover lrecl=500;
        length full_path $500 filename $200 ext $20;
        input full_path $500.;
        full_path = strip(full_path);
        if lengthn(strip(full_path)) = 0 then delete;
        filename  = scan(full_path, -1, '\');
        ext       = lowcase(scan(filename, -1, '.'));
        file_id   + 1;
      run;
      filename dirpipe clear;

    Then get file metadata (size, last-modified) using FOPEN/FINFO per file:
      data work.files_raw;
        set work.files_raw;
        length fsize 8 fdate $30;
        fid = fopen(full_path);
        if fid > 0 then do;
          fsize = input(finfo(fid, 'File Size (bytes)'), best32.);
          fdate = finfo(fid, 'Last Modified');
          fid   = fclose(fid);
        end;
        else do;
          fsize = .;
          fdate = 'UNKNOWN';
        end;
      run;

    -----------------------------------------------------------------------
    SECTION 4 — D-02b presence assertion (md1-md8 must all be present)
    -----------------------------------------------------------------------
    Define a DATALINES table of the eight expected master extract filenames
    (exact names as they appear under raw\master; use the same names as
    referenced in sas/macros_raw_import.sas and sas/16_raw_inventory.sas):

      data work.required_files;
        length req_filename $200;
        infile datalines dsd;
        input req_filename $;
        datalines;
      2018_2019_2020_Induction_Emergent20231121.csv
      2018_2019_Precede_Database.xlsx
      2018_2022_COLONOSCOPY_20240118.xlsx
      2018_2022_Induction_Emergent20240402.xlsx
      2018_2022_X_MASTER_DATASET_20240402.csv
      2018_2022_X_MASTER_DATASET_20240402.xlsx
      2020_2022_PRECEDE_DATABASE_20240402.csv
      2020_2022_PRECEDE_DATABASE_20240402.xlsx
      ;

    NOTE: The exact filenames above must be verified by reading sas/16_raw_inventory.sas
    and sas/macros_raw_import.sas for the actual names used. Adjust if different.

    Then for each required file, assert presence using PCM-T-12 (enumerate each
    expected item individually — do not rely on a count):

      %macro assert_required_files;
        %local n_req i req_name n_found;
        /* Count required files */
        %let n_req = 0;
        proc sql noprint;
          select count(*) into :n_req trimmed from work.required_files;
        quit;
        /* For each required file, check it appears in work.files_raw */
        %do i = 1 %to &n_req;
          %let req_name = ;
          proc sql noprint;
            select req_filename into :req_name trimmed
            from work.required_files(firstobs=&i obs=&i);
          quit;
          %let n_found = 0;
          proc sql noprint;
            select count(*) into :n_found trimmed
            from work.files_raw
            where upcase(filename) = upcase("&req_name");
          quit;
          %if &n_found = 0 %then %do;
            %fail_out(msg=D-02b ABORT -- required master extract not found: &req_name);
          %end;
        %end;
        %put NOTE: D-02b assertion passed -- all 8 required master extracts found;
      %mend assert_required_files;
      %assert_required_files;

    -----------------------------------------------------------------------
    SECTION 5 — SHA-256 checksums (D-01)
    -----------------------------------------------------------------------
    Write a macro %get_sha256(fpath=, outdsn=, rowid=) that:
    1. Opens FILENAME ck PIPE "certutil -hashfile ""&fpath"" SHA256"
    2. Reads each line with INFILE ck truncover lrecl=200
    3. Compresses the line (removes all spaces) to handle Windows builds
       that print spaces between byte pairs
    4. Tests: lengthn(compressed) = 64 and notxdigit(compressed) = 0
    5. When found, writes the hash to a work dataset row matching rowid
    6. If no hash line found, writes 'FAILED' (log a WARNING, do not abort)
    7. Clears filename ck

    Loop over work.files_raw via a macro do-loop to call %get_sha256 per file.
    Join the hash results back to work.files_raw to produce work.files_ck
    (all columns from files_raw plus sha256 column).

    -----------------------------------------------------------------------
    SECTION 6 — Import loop with error trapping (D-06)
    -----------------------------------------------------------------------
    Classify each file by extension:
      readable: csv, xlsx, xls, sas7bdat
      not-profiled: anything else (pdf, docx, zip, txt, etc.)

    For each readable-extension file:
      1. Derive a safe SAS dataset name (dsname) from the file_id:
           dsname = cats('inv_', put(file_id, z5.));
      2. Pre-delete stale dataset (CRITICAL — stale dataset trap):
           proc datasets lib=work nolist; delete &dsname; quit;
      3. Attempt import:
           - csv: %import_csv(&dsname, &full_path)
             (Note: %import_csv takes a rid and filename; adapt the call or
              call PROC IMPORT directly for a full path input — read
              macros_raw_import.sas to see if full-path is supported; if not,
              call PROC IMPORT directly with datafile="&full_path")
           - xlsx/xls: assign a libname to the file, enumerate sheets via
             dictionary.tables where libname=upcase("&xlibname"), copy each
             sheet to work.&dsname_&sheetnum via DATA step or call execute
           - sas7bdat: assign a libname to the directory; if successful,
             profile via dictionary.columns; if libname fails, status=listed-not-profiled
      4. After import, check &syserr:
           %let import_ok = 0;
           %if &syserr = 0 %then %let import_ok = 1;
           %if &import_ok = 0 %then %do;
             /* Record fail_reason = &syserr value; status = read-failed */
             %let syscc = 0;  /* reset so error does not propagate */
           %end;
      5. For XLSX success verification: count datasets produced in WORK for
         this rid against sheet count from dictionary.tables. Any shortfall
         means status = read-failed.
      6. If profiled: collect nobs via PROC SQL on dictionary.tables,
         ncols via dictionary.columns. Record in work.file_stats.

    For not-profiled files: set status = listed-not-profiled; no import.

    Build work.files_out with columns:
      full_path, filename, ext, fsize, fdate, sha256, status,
      nobs (. for not-profiled/read-failed), ncols (. for not-profiled/read-failed),
      fail_reason ($200, blank unless read-failed), file_id

    For XLSX files, also build work.sheets_out with columns:
      full_path, filename, sheet_name, nobs, ncols

    -----------------------------------------------------------------------
    SECTION 7 — Variable profiling (INV-03, D-03)
    -----------------------------------------------------------------------
    For each profiled dataset (status='profiled') in work.files_out:

    First get variable metadata from dictionary.columns for that dataset:
      var_name, var_type (char 'char'/'num' — PCM-T-13, never compare to 1/2),
      var_length, var_label, var_pos (varnum)

    Then run the one-pass DATA step (MANDATORY — PROC MEANS cannot count -999):

      data work.miss_&dsname (keep=var_name n_miss_num n_sent_num
                                    n_miss_chr n_sent_chr nobs);
        set work.&dsname end=_eof;
        array _num  {*} _numeric_;
        array _char {*} _character_;
        array n_miss_n {*} _temporary_;
        array n_sent_n {*} _temporary_;
        array n_miss_c {*} _temporary_;
        array n_sent_c {*} _temporary_;
        if _n_ = 1 then do;
          do _i = 1 to dim(_num);  n_miss_n{_i}=0; n_sent_n{_i}=0; end;
          do _i = 1 to dim(_char); n_miss_c{_i}=0; n_sent_c{_i}=0; end;
        end;
        do _i = 1 to dim(_num);
          if missing(_num{_i})    then n_miss_n{_i} + 1;
          else if _num{_i} = -999 then n_sent_n{_i} + 1;
        end;
        do _i = 1 to dim(_char);
          if missing(_char{_i})                         then n_miss_c{_i} + 1;
          else if upcase(strip(_char{_i})) = 'NULL'     then n_sent_c{_i} + 1;
        end;
        if _eof then do;
          nobs = _n_;
          /* Output one row per numeric variable */
          do _i = 1 to dim(_num);
            var_name = vname(_num{_i});
            n_miss_num = n_miss_n{_i};
            n_sent_num = n_sent_n{_i};
            n_miss_chr = .;
            n_sent_chr = .;
            output;
          end;
          /* Output one row per character variable */
          do _i = 1 to dim(_char);
            var_name = vname(_char{_i});
            n_miss_num = .;
            n_sent_num = .;
            n_miss_chr = n_miss_c{_i};
            n_sent_chr = n_sent_c{_i};
            output;
          end;
        end;
        stop;
      run;

    Join dictionary.columns metadata with the missingness counts.
    Compute pct_missing = coalesce(n_miss_num, n_miss_chr) / nobs * 100
            pct_sentinel = coalesce(n_sent_num, n_sent_chr) / nobs * 100

    Append to work.variables with columns:
      source_file ($500), var_name ($32), var_type ($4), var_length (8),
      var_label ($256), var_pos (8), nobs (8),
      n_missing (8), pct_missing (8), n_sentinel (8), pct_sentinel (8)

    NOTE for r2 (3,987 columns, 14,807 rows): the one-pass DATA step is the
    ONLY correct approach. Do not attempt PROC MEANS or per-variable queries.

    -----------------------------------------------------------------------
    SECTION 8 — Key-column detection (INV-04, PCM-T-12)
    -----------------------------------------------------------------------
    Sweep work.variables for all naming variants of the three key columns.
    PCM-T-12 requires enumerating ALL variants — do not rely on a count or
    pattern match alone.

    PRECEDE_STUDY_ID variants (upcase match):
      'PRECEDE_STUDY_ID', 'PRECEDESTUDYID', 'PRECEDE STUDY ID',
      'STUDYID', 'STUDY_ID'
    Also check: if var_type='char' and var_label matches any of the above
    variants (catches VARnn columns from XLSX engine where spaces in the
    original header caused renaming — the original header becomes the label).

    ENCRYPTED_MRN variants (upcase match):
      'ENCRYPTED_MRN', 'ENCRYPTEDMRN', 'ENCRYPTED MRN', 'MRN'
    Also check var_label for the same variants.

    ENCRYPTED_ENCOUNTER variants (upcase match):
      'ENCRYPTED_ENCOUNTER', 'ENCRYPTEDENCOUNTER', 'ENCRYPTED ENCOUNTER',
      'ENCOUNTER', 'ENCOUNTER_ID'
    Also check var_label for the same variants.

    Build work.key_columns with columns:
      source_file ($500), var_name ($32), var_label ($256), var_type ($4),
      key_column_type ($30): one of 'PRECEDE_STUDY_ID', 'ENCRYPTED_MRN',
                             'ENCRYPTED_ENCOUNTER'
      match_basis ($20): 'name' or 'label'

    -----------------------------------------------------------------------
    SECTION 9 — FAMILIES assignment (D-04, longest-match)
    -----------------------------------------------------------------------
    Define the DATALINES lookup table with columns (prefix $50, family_name $50).
    Sort by descending prefix length in code (not by DATALINES row order, which
    is fragile). Upcase both sides before matching.

    CRITICAL — COM scouting: Before locking DATALINES, the executor must
    print all distinct var_name values from work.variables where
    upcase(var_name) =: 'COM' (starts-with operator) and
    upcase(var_name) NOT IN (select upcase(var_name) from work.variables
    where upcase(var_name) =: 'COMP10_' or upcase(var_name)='COMPLICATION_SUM').
    Log this list to the SAS log with %put. Inspect it. If dCDT columns share
    a longer common prefix than 'COM' (e.g., 'CLOCK_'), update DATALINES to
    use that longer prefix instead of 'COM' and remove 'COM'. The scouting
    output appears in logs/19_raw_dir_inventory.log and must be reviewed
    before treating the FAMILIES sheet as final.

    Seed DATALINES (extend after scouting):
      prefix, family_name
      COMPLICATION_SUM, complications
      COMP10_, complications
      LINUS, LINUS
      COM, dCDT

    Sort this lookup by descending length(prefix) in code:
      proc sort data=work.prefix_lookup;
        by descending prefix_len;
      run;

    For each row in work.variables, apply longest-match assignment:
      - Upcase var_name and each prefix before comparison
      - First matching prefix (longest wins) determines family_name
      - If no prefix matches: family_name = 'unassigned'

    Build work.families with columns:
      family_name ($50), source_file ($500), n_cols (8),
      pct_missing_min (8), pct_missing_median (8), pct_missing_max (8)
    (one row per family_name x source_file combination)

    Then run %assert_families (FULL JOIN — see CONTEXT.md D-04 for verbatim SQL):
      %macro assert_families;
        %local n_bad;
        %let n_bad = 0;
        proc sql noprint;
          select count(*) into :n_bad trimmed
          from (
            select coalesce(f.source_file, v.source_file) as src
            from (select source_file, sum(n_cols) as fam_total from work.families
                  group by source_file) f
            full join
                 (select source_file, count(*) as var_total from work.variables
                  group by source_file) v
              on f.source_file = v.source_file
            where coalesce(f.fam_total, 0) ne coalesce(v.var_total, 0)
          );
        quit;
        %if &n_bad > 0 %then %do;
          %fail_out(msg=FAMILIES column totals do not match VARIABLES for &n_bad source files);
        %end;
        %put NOTE: assert_families passed;
      %mend assert_families;
      %assert_families;

    -----------------------------------------------------------------------
    SECTION 10 — RECONCILIATION (D-07)
    -----------------------------------------------------------------------
    Define a DATALINES block of known filenames (17 total: md1-md8 plus r1-r9
    Phase 18 supplemental files). Exact filenames must match how they appear
    in raw — read sas/16_raw_inventory.sas and 18-CONTEXT.md for the r1-r9
    names used in Phase 18.

    Known md1-md8 (raw\master):
      2018_2019_2020_Induction_Emergent20231121.csv
      2018_2019_Precede_Database.xlsx
      2018_2022_COLONOSCOPY_20240118.xlsx
      2018_2022_Induction_Emergent20240402.xlsx
      2018_2022_X_MASTER_DATASET_20240402.csv
      2018_2022_X_MASTER_DATASET_20240402.xlsx
      2020_2022_PRECEDE_DATABASE_20240402.csv
      2020_2022_PRECEDE_DATABASE_20240402.xlsx

    Known r1-r9 Phase 18 supplemental (verify exact names from 18-CONTEXT.md):
      2018_2019_2020_Induction_Emergent20231121.csv  (r1, also md1)
      2018_2019_Precede_Database.xlsx                (r2, also md2 or separate?)
      (Read .planning/phases/18-supplemental-raw-inventory/18-CONTEXT.md for
       the exact list of r1-r9 filenames before coding this DATALINES block.)

    Match by upcase(filename) = upcase(known_filename). Records matching any
    known entry → status_known = 'known'. All others → status_known = 'NEW'.

    Build work.reconciliation by joining work.files_out with the known-file
    lookup (left join; all files_out rows preserved; status_known = 'NEW' for
    non-matching rows).

    -----------------------------------------------------------------------
    SECTION 11 — INV-06 three-term assertion (D-06)
    -----------------------------------------------------------------------
      %macro assert_inv06;
        %local n_bad n_total n_prof n_listed n_failed;
        %let n_bad = 0;
        /* Check for any file with unrecognized status */
        proc sql noprint;
          select count(*) into :n_bad trimmed
          from work.files_out
          where status not in ('profiled', 'listed-not-profiled', 'read-failed');
        quit;
        %if &n_bad > 0 %then %do;
          %fail_out(msg=INV-06 violated -- &n_bad files have unrecognized status);
        %end;
        /* Log the three-term counts for the log record */
        proc sql noprint;
          select count(*) into :n_total trimmed from work.files_out;
          select count(*) into :n_prof   trimmed from work.files_out where status='profiled';
          select count(*) into :n_listed trimmed from work.files_out where status='listed-not-profiled';
          select count(*) into :n_failed trimmed from work.files_out where status='read-failed';
        quit;
        %put NOTE: INV-06 assertion -- total=&n_total profiled=&n_prof listed=&n_listed failed=&n_failed;
        %put NOTE: assert_inv06 passed;
      %mend assert_inv06;
      %assert_inv06;

    -----------------------------------------------------------------------
    SECTION 12 — PROC EXPORT → qc/19_raw_files.csv
    (Write BEFORE ODS Excel opens — per CONTEXT.md Specifics and Pitfall 9)
    -----------------------------------------------------------------------
    Write work.files_out (path, filename, sha256, status, nobs, ncols,
    fsize, fdate, fail_reason) to a CSV before ODS Excel opens:

      proc export data=work.files_out
        outfile="&qc_path.\19_raw_files.csv"
        dbms=csv replace;
      run;

    Log: %put NOTE: qc/19_raw_files.csv written -- Phase 20 handoff file ready;

    -----------------------------------------------------------------------
    SECTION 13 — ODS Excel assembly (D-05, KEY sheet first = leftmost)
    -----------------------------------------------------------------------
    Build work.key_legend first — one row per column in each sheet, with
    columns: sheet_name ($20), column_name ($50), description ($200),
    notes ($200). Include the note: "type in VARIABLES reflects the SAS-imported
    type (char or num), not the source system type. ENCRYPTED_MRN may import as
    numeric in one file and character in another depending on PROC IMPORT
    guessingrows inference."

    Apply UF blue (#0021A5) headers using a PROC TEMPLATE style override or
    ODS TAGSETS options — use the same approach as sas/17_summary_stats_by_domain.sas.

    Sheet order (KEY written first via ods excel options(sheet_name=) call order):
      ods excel file="&qc_path.\19_raw_inventory.xlsx"
          style=styles.pearl options(embedded_titles='yes');

      ods excel options(sheet_name='KEY');
      proc print data=work.key_legend noobs label; run;

      ods excel options(sheet_name='FILES');
      proc print data=work.files_out(drop=file_id) noobs; run;

      ods excel options(sheet_name='SHEETS');
      proc print data=work.sheets_out noobs; run;

      ods excel options(sheet_name='VARIABLES');
      proc print data=work.variables(rename=(source_file=file)) noobs; run;

      ods excel options(sheet_name='KEY_COLUMNS');
      proc print data=work.key_columns noobs; run;

      ods excel options(sheet_name='RECONCILIATION');
      proc print data=work.reconciliation noobs; run;

      ods excel options(sheet_name='FAMILIES');
      proc print data=work.families noobs; run;

      ods excel close;

    FALLBACK (D-05): If ODS Excel is too slow for VARIABLES (40,000+ rows),
    write VARIABLES to qc/19_raw_variables.xlsx separately and keep all other
    sheets in qc/19_raw_inventory.xlsx. This is a separate-file split, not a
    drop-in swap; the VARIABLES sheet is removed from the primary workbook
    in that case.

    -----------------------------------------------------------------------
    SECTION 14 — Output verification and log restore
    -----------------------------------------------------------------------
    Verify that qc/19_raw_inventory.xlsx was created:
      %macro verify_output;
        %if %sysfunc(fileexist("&qc_path.\19_raw_inventory.xlsx")) = 0 %then %do;
          %fail_out(msg=OUTPUT MISSING -- qc/19_raw_inventory.xlsx was not created);
        %end;
        %if %sysfunc(fileexist("&qc_path.\19_raw_files.csv")) = 0 %then %do;
          %fail_out(msg=OUTPUT MISSING -- qc/19_raw_files.csv was not created);
        %end;
        %put NOTE: Phase 19 outputs verified;
      %mend verify_output;
      %verify_output;

    %put NOTE: ==== Phase 19 raw directory inventory complete ====;
    %restore_log;

    -----------------------------------------------------------------------
    GLOBAL CONSTRAINTS (apply throughout the entire program):
    -----------------------------------------------------------------------
    - options nosyntaxcheck noerrorabend; appears in Section 0, before any PROC IMPORT
    - No bare open-code %IF/%THEN — all conditional logic inside named macros (PCM-R-05)
    - No %PUT with apostrophes or embedded semicolons in the text
    - Every %abort cancel inside %fail_out only
    - No &SQLOBS anywhere — use explicit SELECT COUNT(*) INTO :macvar TRIMMED
    - dictionary.columns.type is char 'char'/'num' — never compare to numeric 1/2 (PCM-T-13)
    - No PROC SQL UPDATE; no `data X; set X;` rewrite patterns (PCM-T-01, PCM-T-02)
    - ASCII only in all program text (SAS session encoding is not UTF-8)
    - Paths with spaces: always double-quote in pipe/certutil calls
    - All %abort cancel through %fail_out (which closes ODS Excel before restoring log)
  </action>

  <verify>
    Review the created file for:
    - options nosyntaxcheck noerrorabend appears before any PROC IMPORT call
    - %fail_out macro includes ods excel close before %restore_log
    - certutil pipe uses ""&fpath"" (double-double-quotes for spaces in paths)
    - compress(line, ' ') applied before lengthn=64 test
    - PROC DATASETS delete before each import attempt
    - &syserr checked after each import; %let syscc=0 after failure
    - One-pass DATA step with _numeric_ and _character_ arrays present
    - pct_missing and pct_sentinel are separate columns in the VARIABLES dataset build
    - FAMILIES assertion uses FULL JOIN with coalesce
    - No &SQLOBS anywhere (grep for SQLOBS returns nothing)
    - INV-06 assertion uses SELECT COUNT(*) INTO :n_bad TRIMMED
    - PROC EXPORT for 19_raw_files.csv appears before ods excel file= statement
    - ODS Excel sheet_name='KEY' is the FIRST sheet written
    - All %abort cancel inside named macros only
  </verify>

  <acceptance_criteria>
    - sas/19_raw_dir_inventory.sas exists (file is created)
    - grep -i "nosyntaxcheck" sas/19_raw_dir_inventory.sas returns a match
    - grep -i "noerrorabend" sas/19_raw_dir_inventory.sas returns a match
    - grep -i "SQLOBS" sas/19_raw_dir_inventory.sas returns NO matches
    - grep -i "compress.*line" sas/19_raw_dir_inventory.sas returns a match (certutil parse guard)
    - grep -i "full join" sas/19_raw_dir_inventory.sas returns a match (FAMILIES assertion)
    - grep -i "coalesce" sas/19_raw_dir_inventory.sas returns a match (FAMILIES assertion)
    - grep -i "pct_sentinel" sas/19_raw_dir_inventory.sas returns a match
    - grep -i "pct_missing" sas/19_raw_dir_inventory.sas returns a match
    - grep -i "syscc.*0" sas/19_raw_dir_inventory.sas returns a match (error code reset after read-failed)
    - grep -i "nosyntaxcheck" sas/19_raw_dir_inventory.sas appears before first "proc import" line (order matters)
    - grep -i "19_raw_files.csv" sas/19_raw_dir_inventory.sas returns a match (CSV handoff)
    - grep -i "sheet_name='KEY'" sas/19_raw_dir_inventory.sas returns a match
    - grep -i "sheet_name='FAMILIES'" sas/19_raw_dir_inventory.sas returns a match
    - grep -i "abort cancel" sas/19_raw_dir_inventory.sas returns NO match outside of macro definitions (all aborts inside fail_out)
    - grep -i "ods excel close" sas/19_raw_dir_inventory.sas returns at least 2 matches (one in %fail_out, one at end of Section 13)
  </acceptance_criteria>

  <done>
    sas/19_raw_dir_inventory.sas is complete and contains all 14 sections. The program
    is self-contained: submit it in a clean SAS batch session with XCMD enabled and it
    produces qc/19_raw_inventory.xlsx (seven sheets, KEY leftmost) and
    qc/19_raw_files.csv (Phase 20 handoff). All PCM compliance constraints are met.
    All locked decisions D-01 through D-07 are implemented exactly as specified.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- .planning/REQUIREMENTS.md INV-07 lists FAMILIES and KEY sheets
- .planning/REQUIREMENTS.md INV-06 references read-failed and n_read_failed
- sas/19_raw_dir_inventory.sas exists with all 14 sections
- The program passes the acceptance_criteria grep checks above
- No &SQLOBS in the program
- options nosyntaxcheck noerrorabend appears before any PROC IMPORT
- PROC EXPORT for 19_raw_files.csv precedes the ods excel file= statement
</verification>

<success_criteria>
- .planning/REQUIREMENTS.md updated: INV-07 lists KEY, FILES, SHEETS, VARIABLES,
  KEY_COLUMNS, RECONCILIATION, FAMILIES; INV-06 includes read-failed status
- sas/19_raw_dir_inventory.sas created, complete, and PCM-compliant
- Program implements all locked decisions: D-01 (certutil PIPE), D-02 (full profiling
  + presence assertion), D-03 (one-pass missingness + sentinel separation), D-04
  (FAMILIES FULL JOIN assertion + longest-match), D-05 (ODS Excel KEY-first + UF blue),
  D-06 (three-status model + error trapping), D-07 (DATALINES known-file list)
- All acceptance_criteria grep checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-raw-directory-inventory/19-01-SUMMARY.md`
</output>
