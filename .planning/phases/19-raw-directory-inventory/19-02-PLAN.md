---
phase: 19-raw-directory-inventory
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - sas/19_raw_dir_inventory.sas
autonomous: false
requirements: [INV-07]

must_haves:
  truths:
    - "User can open qc/19_raw_inventory.xlsx with KEY as the leftmost sheet"
    - "User can see seven sheets: KEY, FILES, SHEETS, VARIABLES, KEY_COLUMNS, RECONCILIATION, FAMILIES"
    - "UF blue (#0021A5) column headers appear on every sheet"
    - "qc/19_raw_files.csv exists and contains a row for every file in raw including the md3 source CSV"
    - "SAS log for program 19 shows 0 ERROR lines and all assertion macros report NOTE: ...passed"
    - "COM scouting COM_SCOUT: log lines have been reviewed and FAMILIES DATALINES prefix list confirmed complete (R2-B-03)"
  artifacts:
    - path: "sas/19_raw_dir_inventory.sas"
      provides: "Final runnable program (any fixes from test run applied)"
    - path: "qc/19_raw_inventory.xlsx"
      provides: "Seven-sheet deliverable workbook (produced by the program, not committed to git)"
    - path: "qc/19_raw_files.csv"
      provides: "Machine-readable FILES dataset for Phase 20 PID-01 checksum verification"
  key_links:
    - from: "qc/19_raw_files.csv"
      to: "Phase 20 PID-01"
      via: "CSV contains sha256 column and md3 source row"
      pattern: "2018_2022_X_MASTER_DATASET_20240402"
    - from: "qc/19_raw_inventory.xlsx KEY sheet"
      to: "user inspection"
      via: "KEY is leftmost sheet; opened in Excel"
      pattern: "KEY"
---

<objective>
Run sas/19_raw_dir_inventory.sas in a SAS batch session, inspect the log for ERRORs
and assertion results, fix any bugs found, then have the user open the output workbook
to confirm KEY is leftmost, all seven sheets are present with UF blue headers, and
qc/19_raw_files.csv exists with a row for the md3 source file. The user also reviews
the COM_SCOUT: log lines from the COM scouting step before approving the FAMILIES sheet (R2-B-03).

Purpose: Validate that the program executes without error and produces the required
deliverables. Any bugs found during the test run are fixed in this plan before the
phase is considered complete.

Output: Verified sas/19_raw_dir_inventory.sas (with any fixes applied);
qc/19_raw_inventory.xlsx (seven sheets, KEY leftmost, UF blue headers);
qc/19_raw_files.csv (md3 checksum row present).
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-raw-directory-inventory/19-CONTEXT.md
@.planning/phases/19-raw-directory-inventory/19-RESEARCH.md
@.planning/phases/19-raw-directory-inventory/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run program 19 and fix any errors found in the log</name>
  <files>sas/19_raw_dir_inventory.sas</files>

  <read_first>
    - sas/19_raw_dir_inventory.sas (read the full program before attempting to run it;
      confirm nosyntaxcheck/noerrorabend are present and PROC EXPORT precedes ods excel)
    - .planning/phases/19-raw-directory-inventory/19-01-SUMMARY.md (what was built in Plan 01)
    - .planning/phases/19-raw-directory-inventory/19-RESEARCH.md (pitfalls checklist —
      review before inspecting the log)
  </read_first>

  <action>
    Step 1 — Pre-flight check before running.
    Read sas/19_raw_dir_inventory.sas and confirm:
    - options nosyntaxcheck noerrorabend; appears before any PROC IMPORT call
    - %fail_out macro includes ods excel close; before %restore_log;
    - PROC EXPORT for 19_raw_files.csv appears before the ods excel file= statement
    - No &SQLOBS anywhere in the program
    - Every %abort cancel is inside a named macro (no bare open-code abort)
    - &syserr le 4 used as success threshold (not = 0)
    - Temporary arrays have explicit sizes (not {*} _temporary_)
    - No stop; inside the main loop body of the one-pass DATA step
    - filename('_fr_', full_path) / fopen('_fr_') pattern for file metadata
    - fileexist() calls have no double-quotes inside %sysfunc()
    Fix any pre-flight issues before running.

    Step 2 — Submit the program.
    Run the program in a SAS batch session:
      sas "C:\Master_Renamed_same_format_accross\sas\19_raw_dir_inventory.sas"
    Or submit interactively if batch is not available — IMPORTANT: restart the SAS
    session first so no prior session state affects the run (PCM-C-05: restart SAS
    between programs; %abort cancel in a prior run leaves an interactive session that
    swallows the next submit without executing).

    Step 3 — Inspect the log.
    Read logs/19_raw_dir_inventory.log (or the interactive log if batch was not used).
    Look for:
    - Lines matching /^ERROR/ — any ERROR is a bug to fix
    - Lines matching /NOTE:.*assert.*passed/ — confirm each assertion macro logged its pass note
    - Lines matching /NOTE:.*Phase 19.*complete/ — confirms program ran to end
    - Lines matching /NOTE:.*qc\/19_raw_files.csv written/ — confirms CSV handoff was written
    - Lines matching /WARNING: FAILED/ in the SHA-256 section — if any file hash is FAILED,
      investigate (likely a path-with-spaces quoting issue in the certutil pipe call)
    - Lines matching /OBS=0/ anywhere after an import attempt — indicates nosyntaxcheck
      did not prevent syntax-check mode; fix and rerun
    - Lines matching /COM_SCOUT:/ — the COM scouting DATA _null_ step writes each
      var_name to the log with a "COM_SCOUT:" prefix (R2-B-03: replaces PROC PRINT
      which goes to ODS output, not the log); record the full list for Task 2 review

    Step 4 — Fix errors.
    For each ERROR found, fix the root cause in sas/19_raw_dir_inventory.sas.
    Common fixes to apply (reference 19-RESEARCH.md pitfalls):
    - Pitfall 1 (syntax-check mode): Confirm options nosyntaxcheck noerrorabend is
      before ALL PROC IMPORT calls; move it if needed
    - Pitfall 2 (stale dataset): Confirm PROC DATASETS delete appears before each import
    - Pitfall 3 (certutil parse failure): Confirm compress(line, ' ') is applied before
      lengthn=64 test
    - Pitfall 4 (dir /a-d): Confirm /a-d flag in the dir pipe command
    - Pitfall 5/6 (FAMILIES assertion): If n_bad > 0, look for files with VARIABLES rows
      but no FAMILIES rows; add them to the lookup or check the FULL JOIN SQL
    - Pitfall 9 (PROC EXPORT order): If 19_raw_files.csv is missing, PROC EXPORT
      may have been inside the ODS block — move it before ods excel file= line
    - PCM-T-13: If ERROR about type comparison, check dictionary.columns.type in ('char','num')
      is using character comparison not numeric

    After fixes, re-run and re-inspect until log shows 0 ERROR lines and all assertion
    NOTE: ...passed messages appear.

    Step 5 — Verify CSV handoff.
    After a clean run, read the first few lines of qc/19_raw_files.csv (or list it) to
    confirm:
    - File exists at &qc_path\19_raw_files.csv
    - It contains a header row with columns including full_path, filename, sha256, status
    - It contains a row where filename matches '2018_2022_X_MASTER_DATASET_20240402.csv'
      (the md3 source CSV; Phase 20 PID-01 depends on this row existing with a sha256 value)
    - The sha256 value for that row is 64 hex characters (not 'FAILED')
  </action>

  <verify>
    - logs/19_raw_dir_inventory.log contains no lines beginning with ERROR
    - logs/19_raw_dir_inventory.log contains "NOTE: assert_families passed"
    - logs/19_raw_dir_inventory.log contains "NOTE: assert_inv06 passed"
    - logs/19_raw_dir_inventory.log contains "NOTE: assert_masters_profiled passed"
    - logs/19_raw_dir_inventory.log contains "NOTE: Phase 19 raw directory inventory complete"
    - logs/19_raw_dir_inventory.log contains "NOTE: qc/19_raw_files.csv written"
    - qc/19_raw_files.csv exists (fileexist check or dir listing)
    - qc/19_raw_files.csv contains a row with '2018_2022_X_MASTER_DATASET_20240402'
    - qc/19_raw_inventory.xlsx exists
    - COM scouting output is present in the log as COM_SCOUT: lines (for review in Task 2)
  </verify>

  <acceptance_criteria>
    - Log file contains "NOTE: Phase 19 raw directory inventory complete" (program reached end)
    - Log file contains "NOTE: assert_families passed" (FAMILIES full-join assertion passed)
    - Log file contains "NOTE: assert_inv06 passed" (INV-06 three-term assertion passed)
    - Log file contains "NOTE: assert_masters_profiled passed" (D-06 required-file assertion passed)
    - Log file contains zero lines beginning with "ERROR"
    - qc/19_raw_files.csv exists and contains the string '2018_2022_X_MASTER_DATASET_20240402'
    - qc/19_raw_inventory.xlsx exists
    - sas/19_raw_dir_inventory.sas still contains "nosyntaxcheck" (fix did not remove it)
    - sas/19_raw_dir_inventory.sas grep for "SQLOBS" returns no matches
    - Log file contains at least one line starting with "COM_SCOUT:" (R2-B-03: DATA _null_ log output confirms scouting step ran)
  </acceptance_criteria>

  <done>
    The program runs to completion in a SAS batch session with 0 ERRORs, all embedded
    assertion macros log their passed notes (assert_families, assert_inv06,
    assert_masters_profiled), qc/19_raw_inventory.xlsx and qc/19_raw_files.csv both
    exist, the md3 source CSV row is present in the CSV with a 64-character SHA-256 hash,
    and COM_SCOUT: log lines from the COM scouting step are present for Task 2 review (R2-B-03).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies workbook, CSV deliverables, and COM scouting output</name>

  <what-built>
    sas/19_raw_dir_inventory.sas ran successfully and produced:
    - qc/19_raw_inventory.xlsx — seven-sheet inventory workbook
    - qc/19_raw_files.csv — machine-readable FILES dataset for Phase 20

    The SAS log shows 0 ERRORs and all assertion macros passed.
    The COM scouting DATA _null_ step ran and wrote COM_SCOUT: lines to
    logs/19_raw_dir_inventory.log (R2-B-03: PROC PRINT was replaced with DATA _null_
    PUT so that scouting output actually appears in the batch log).
  </what-built>

  <how-to-verify>
    1. Open qc/19_raw_inventory.xlsx in Excel.
       Confirm the tab order from left to right is exactly:
         KEY | FILES | SHEETS | VARIABLES | KEY_COLUMNS | RECONCILIATION | FAMILIES
       If KEY is not leftmost, this is a bug — report it.

    2. On the KEY sheet:
       Confirm it is a legend describing the columns of each other sheet.
       Confirm it contains a note about `type` in VARIABLES reflecting the
       SAS-imported type (not the source system type).

    3. On the FILES sheet:
       Confirm every row has a non-blank sha256 value (no 'FAILED' entries,
       or if any exist, note the filename for investigation).
       Confirm every file has one of three statuses: profiled, listed-not-profiled,
       or read-failed (no blank or other status values).
       Confirm the md3 source CSV appears:
         filename = 2018_2022_X_MASTER_DATASET_20240402.csv with status = profiled

    4. On the VARIABLES sheet:
       Confirm it has both pct_missing and pct_sentinel columns (two separate columns,
       not one combined column).
       Confirm it has a sheet_name column (so variables from different sheets of the
       same XLSX workbook are distinguishable).
       Spot-check a known wide file (e.g., r2 / 2018_2019_Precede_Database.xlsx)
       to confirm it has many variable rows (expected ~3,987 rows for r2).

    5. On the KEY_COLUMNS sheet:
       Confirm PRECEDE_STUDY_ID, ENCRYPTED_MRN, and ENCRYPTED_ENCOUNTER are listed
       for files that are known to carry them (md3 CSV should have PRECEDE_STUDY_ID
       and ENCRYPTED_MRN).
       Confirm the sheet has a match_basis column ('name' or 'label').
       Confirm the sheet has a key_column_type column and that plain MRN columns
       (if any) appear with key_column_type = 'UNENC_MRN', not 'ENCRYPTED_MRN'.

    6. On the RECONCILIATION sheet:
       Confirm that the md1-md8 filenames (e.g., 2018_2022_X_MASTER_DATASET_20240402.csv,
       ALL_AIM2_MASTER_DATASET_20210917.xlsx) and all r1-r9 supplemental filenames
       (e.g., 2018_2019_2020_Induction_Emergent20231121.csv, All_YEARS_LAT_LONG_20231127.csv)
       appear with status_known = 'known'.
       Confirm that any files present in the raw directory that are NOT in the known list
       appear with status_known = 'NEW'. If no NEW files exist, confirm the column is
       present and all rows show 'known'.
       Report any md1-md8 or r1-r9 file that unexpectedly shows status_known = 'NEW'
       (indicates a filename mismatch in the DATALINES block).

    7. On the FAMILIES sheet:
       Confirm the family_name column contains at least: complications, dCDT, LINUS,
       unassigned. Confirm pct_missing_min, pct_missing_median, pct_missing_max are
       all present (three separate columns).

    8. Confirm column headers are UF blue (#0021A5) throughout all sheets.
       (If headers appear in a different color, note it — this is a style bug.)

    9. Confirm qc/19_raw_files.csv opens and the first column is a file path
       and includes a row for 2018_2022_X_MASTER_DATASET_20240402.csv with a
       64-character SHA-256 hash in the sha256 column.

    10. COM scouting review (P02-01 — REQUIRED before approving FAMILIES):
        Open logs/19_raw_dir_inventory.log and search for lines beginning with
        "COM_SCOUT:" (R2-B-03: the scouting step uses DATA _null_ PUT, not
        PROC PRINT, so results appear as log lines, not ODS output).
        Review the full list of var_name values written there.
        Confirm that all names listed are dCDT clock columns (or are already
        captured by a longer prefix in the FAMILIES lookup: COMPLICATION_SUM,
        COMP10_, LINUS).
        If any name in the list is NOT a dCDT clock column (e.g., COMORBID_*,
        COMMENT_*, or another family entirely):
          - Report those names so the DATALINES prefix list can be updated
            with a longer/more specific prefix for those columns before Phase 19
            is considered complete.
        If all names in the list are confirmed dCDT clock columns:
          - State this explicitly in your approval message.
        The FAMILIES sheet cannot be approved until this review is complete.
  </how-to-verify>

  <resume-signal>
    Type "approved" if all checks pass AND the COM scouting output has been reviewed
    and confirmed (state: "COM scouting confirmed — all residual COM* columns are dCDT
    clock columns" or list any names requiring DATALINES updates).
    Or describe any issues found (e.g., "KEY not leftmost", "pct_sentinel column missing",
    "sha256 shows FAILED for 3 files", "RECONCILIATION shows r2 as NEW — filename mismatch",
    "COM scouting shows COMORBID_* columns need a separate family entry") so they can be fixed.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 19 is complete when:
- sas/19_raw_dir_inventory.sas runs with 0 ERRORs in SAS batch
- assert_families, assert_inv06, and assert_masters_profiled all log "passed"
- qc/19_raw_inventory.xlsx exists with KEY leftmost and all seven sheets
- qc/19_raw_files.csv exists with md3 row and 64-char sha256
- COM scouting output reviewed and FAMILIES DATALINES prefix list confirmed complete
- Human has opened the workbook and typed "approved"
- .planning/REQUIREMENTS.md INV-07 lists all seven sheets
</verification>

<success_criteria>
- SAS log: 0 ERROR lines; "NOTE: Phase 19 raw directory inventory complete"
- SAS log: "NOTE: assert_families passed", "NOTE: assert_inv06 passed",
  "NOTE: assert_masters_profiled passed"
- qc/19_raw_inventory.xlsx: KEY leftmost; FILES, SHEETS, VARIABLES, KEY_COLUMNS,
  RECONCILIATION, FAMILIES all present; UF blue headers; pct_missing and pct_sentinel
  as separate VARIABLES columns; sheet_name column in VARIABLES; match_basis and
  UNENC_MRN in KEY_COLUMNS; RECONCILIATION shows md1-md8 and r1-r9 as status_known='known'
- qc/19_raw_files.csv: md3 row with 64-char sha256; status=profiled
- COM scouting COM_SCOUT: log lines reviewed: FAMILIES DATALINES confirmed complete
- Human checkpoint: "approved" with COM scouting confirmation
- INV-07 now satisfied with all seven sheets documented
</success_criteria>

<output>
After completion, create `.planning/phases/19-raw-directory-inventory/19-02-SUMMARY.md`
</output>
