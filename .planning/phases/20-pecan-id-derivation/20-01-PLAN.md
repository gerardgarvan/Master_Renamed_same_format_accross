---
phase: 20-pecan-id-derivation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sas/19_raw_dir_inventory.sas
  - sas/00_config.sas
  - sas/20_pecan_id.sas
  - docs/DECISIONS.md
autonomous: true
requirements: [PID-01, PID-02, PID-03, PID-04, PID-07, PID-08]
must_haves:
  truths:
    - "Program 19 writes three additional CSV handoffs (key_columns, sheets, variables_md3) alongside 19_raw_files.csv"
    - "Program 20 checksums the md3 source CSV and aborts if it does not match the Phase 19 record"
    - "Program 20 asserts every PRECEDE_STUDY_ID maps to exactly one ENCRYPTED_MRN and aborts otherwise"
    - "Program 20 builds append-only crosswalk g.pecan_id_xwalk and writes a dated backup outside git"
    - "Program 20 writes qc/20_linkage_reach.txt with a YES/NO PCM-D-16 line for r7/r8/r9"
    - "PCM-D-17 and PCM-D-18 are recorded in DECISIONS.md attributed to Gerard Garvan dated 2026-09-23"
  artifacts:
    - path: "sas/20_pecan_id.sas"
      provides: "PID-01 through PID-04, PID-07, PID-08 program logic"
      contains: "build_or_append_xwalk"
    - path: "g.pecan_id_xwalk"
      provides: "ENCRYPTED_MRN to pecan_ID crosswalk (permanent SAS dataset)"
      contains: "pecan_ID"
    - path: "qc/20_linkage_reach.txt"
      provides: "PID-07 linkage reach report"
    - path: "sas/00_config.sas"
      provides: "xwalk_backup_path macro variable"
      contains: "xwalk_backup_path"
    - path: "docs/DECISIONS.md"
      provides: "PCM-D-17 and PCM-D-18 resolved entries"
      contains: "PCM-D-17"
  key_links:
    - from: "sas/20_pecan_id.sas"
      to: "qc/19_raw_files.csv"
      via: "PROC IMPORT + checksum lookup"
      pattern: "19_raw_files.csv"
    - from: "sas/20_pecan_id.sas"
      to: "g.master_data_merged"
      via: "crosswalk build source and D-14 cross-check"
      pattern: "master_data_merged"
    - from: "sas/20_pecan_id.sas"
      to: "g.pecan_id_xwalk"
      via: "PROC APPEND append-only build"
      pattern: "proc append"
---

<objective>
Amend program 19 to emit the three additional CSV handoffs Phase 20 consumes, then
build the new program `sas/20_pecan_id.sas` that verifies the md3 source checksum,
cross-checks the CSV against `g.master_data_merged`, audits MRN cardinality, builds
the append-only crosswalk `g.pecan_id_xwalk` with a dated backup, tests r7/r8/r9
linkage reach, and records PCM-D-17/PCM-D-18 in DECISIONS.md.

Purpose: Produce the patient linkage key and all its supporting audits. Program 20
owns everything EXCEPT attachment (attachment lives in 10b/16b -- Plan 02).
Output: sas/19 amendment (3 new CSVs), sas/00_config.sas (backup path macro var),
sas/20_pecan_id.sas, g.pecan_id_xwalk, dated backup, qc/20_linkage_reach.txt,
DECISIONS.md entries.

This plan is Wave 1: the crosswalk MUST exist before Plan 02's 10b/16b joins run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-pecan-id-derivation/20-CONTEXT.md
@.planning/phases/20-pecan-id-derivation/20-RESEARCH.md
@sas/00_config.sas
@sas/19_raw_dir_inventory.sas
@sas/03_prep_md3.sas
@sas/18_supplemental_raw_gap.sas

<interfaces>
<!-- Key structures the executor needs; extracted from the codebase. Use directly. -->

Program 19 in-memory datasets available at SECTION 12 (before ODS opens):
- work.key_columns  : source_file, sheet_name, var_name, var_label, var_type,
                      key_column_type ($30: PRECEDE_STUDY_ID / ENCRYPTED_MRN /
                      UNENC_MRN / ENCRYPTED_ENCOUNTER / UNENC_ENCOUNTER), match_basis ($20)
- work.sheets_rpt   : full_path, filename, sheet_seq, sheet_name, sheet_status, nobs, ncols
                      (built AFTER SECTION 12 in current code -- see Task 1 note)
- work.sheets_out   : file_id, sheet_seq, sheet_name, sheet_ds, sheet_status, nobs, ncols
- work.variables    : source_file, sheet_name, var_name, var_type, var_length,
                      var_label, var_pos, nobs, n_missing, pct_missing, n_sentinel, pct_sentinel
- work.files_out    : full_path, filename, ext, fsize, fdate, sha256, status, nobs,
                      ncols, fail_reason, import_warning, file_id

Existing Phase 19 CSV export (SECTION 12, line ~831):
  proc export data=work.files_out outfile="&qc_path.\19_raw_files.csv" dbms=csv replace; run;

md3 normalization rules (03_prep_md3.sas, authoritative for D-14):
- ENCRYPTED_MRN declared $40 (line 108); PRECEDE_STUDY_ID declared $12 (line 107)
- NULL sentinel test: strip(upcase(col)) = 'NULL' (lines 61-62)
- md3 asserts n_sent = 0 -- md3 has NO NULL sentinels; no other transformation on
  ENCRYPTED_MRN beyond strip()

certutil checksum pattern (19 SECTION 5, lines 180-194) -- reuse verbatim structure.
PUT-to-fileref pattern (18_supplemental_raw_gap.sas) -- reuse for qc/20_linkage_reach.txt.

00_config.sas convention: %let in the "Data paths (P:)" block + a matching %put NOTE line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend program 19 to write three additional CSV handoffs</name>
  <read_first>
    - sas/19_raw_dir_inventory.sas (SECTION 12 lines 827-835 = existing PROC EXPORT;
      SECTION 13 lines 838-848 = where work.sheets_rpt is built; SECTION 8 = work.key_columns;
      SECTION 7 = work.variables)
  </read_first>
  <files>sas/19_raw_dir_inventory.sas</files>
  <action>
    In SECTION 12 (immediately after the existing `proc export ... 19_raw_files.csv`
    at line ~834, before SECTION 13's ODS block), add THREE new PROC EXPORT steps.
    Note: work.sheets_rpt is not built until SECTION 13; export work.sheets_out
    (which exists from SECTION 6) instead, joining to work.files_meta for full_path
    so the sheet CSV carries the file path Phase 20 needs for targeted import.

    Add exactly these three exports plus %put NOTE confirmations:

    1. Key columns handoff:
       proc export data=work.key_columns
         outfile="&qc_path.\19_raw_key_columns.csv" dbms=csv replace;
       run;
       %put NOTE: qc/19_raw_key_columns.csv written -- Phase 20 PID-07 handoff ready;

    2. Sheets handoff (join sheets_out to files_meta for full_path + filename):
       proc sql noprint;
         create table work.sheets_csv as
         select f.full_path, f.filename, s.file_id, s.sheet_seq, s.sheet_name,
                s.sheet_ds, s.sheet_status, s.nobs, s.ncols
         from work.sheets_out s
         inner join work.files_meta f on s.file_id = f.file_id
         order by f.full_path, s.sheet_seq;
       quit;
       proc export data=work.sheets_csv
         outfile="&qc_path.\19_raw_sheets.csv" dbms=csv replace;
       run;
       %put NOTE: qc/19_raw_sheets.csv written -- Phase 20 PID-07 handoff ready;

    3. md3 variables handoff (VARIABLES rows for the md3 source CSV only). The md3
       source is the file whose filename is 2018_2022_X_MASTER_DATASET_20240402.csv
       under raw\master. Filter work.variables where the source_file path ends in
       that filename:
       proc sql noprint;
         create table work.variables_md3 as
         select * from work.variables
         where upcase(scan(source_file, -1, '\')) =
               '2018_2022_X_MASTER_DATASET_20240402.CSV'
           and upcase(source_file) contains '\MASTER\';
       quit;
       proc export data=work.variables_md3
         outfile="&qc_path.\19_raw_variables_md3.csv" dbms=csv replace;
       run;
       %put NOTE: qc/19_raw_variables_md3.csv written -- Phase 20 D-11 handoff ready;

    Then extend the SECTION 14 %verify_output macro (lines 954-963) to check all three
    new CSVs with %sysfunc(fileexist(...)) = 0 -> %fail_out, enumerated individually
    (PCM-T-12): one %if block per file, each naming the specific path.

    Follow all program-19 constraints already in its header: no PROC SQL UPDATE,
    no in-place rewrite, ASCII only, no apostrophes/embedded semicolons in %PUT text,
    period-terminated macro var in the outfile path (&qc_path.\...).
  </action>
  <verify>
    <automated>grep -c "19_raw_key_columns.csv\|19_raw_sheets.csv\|19_raw_variables_md3.csv" sas/19_raw_dir_inventory.sas</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "19_raw_key_columns.csv" sas/19_raw_dir_inventory.sas` returns >= 2 matches (export + verify)
    - `grep -n "19_raw_sheets.csv" sas/19_raw_dir_inventory.sas` returns >= 2 matches
    - `grep -n "19_raw_variables_md3.csv" sas/19_raw_dir_inventory.sas` returns >= 2 matches
    - `grep -n "2018_2022_X_MASTER_DATASET_20240402.CSV" sas/19_raw_dir_inventory.sas` returns >= 1 match (md3 filter)
    - The three new PROC EXPORT statements appear AFTER the existing 19_raw_files.csv export and BEFORE `ods excel file=` (SECTION 13); verify by line-number ordering in grep -n output
    - `grep -c "'" sas/19_raw_dir_inventory.sas` shows no new apostrophes introduced in %PUT text
    - No `proc sql` ... `update` statement anywhere in the file
  </acceptance_criteria>
  <done>Program 19 emits all four CSV handoffs and verifies all four exist before completing.</done>
</task>

<task type="auto">
  <name>Task 2: Add crosswalk backup path macro variable to 00_config.sas</name>
  <read_first>
    - sas/00_config.sas (lines 18-25 = Data paths P: block; lines 55-62 = %put NOTE block)
  </read_first>
  <files>sas/00_config.sas</files>
  <action>
    In the "Data paths (P:)" block (after line 22, the logs_path %let), add:

    /* ---- Phase 20 / PCM-D-17: crosswalk dated-backup path ----
       Outside qc/ and outside git. ENCRYPTED_MRN (PHI) lives here. */
    %let xwalk_backup_path = P:\PeCAN Master Data\Gerard\Master_Renamed_same_format_accross\merge\xwalk_backup;

    Then add a matching %put NOTE line in the SECTION at lines 55-62, following the
    exact existing convention:
    %put NOTE: [00_config] xwalk_backup_path = &xwalk_backup_path;

    ASCII only. Do not change any existing path value.
  </action>
  <verify>
    <automated>grep -c "xwalk_backup_path" sas/00_config.sas</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "xwalk_backup_path" sas/00_config.sas` returns exactly 2 matches (one %let, one %put)
    - The %let value points to a P: path containing "merge\xwalk_backup" (outside qc/, outside git tree)
    - `git status --short sas/00_config.sas` shows the file modified; the existing six path %let values are unchanged (verify by grep: sas_path, docs_path, source_path, g_path, qc_path, logs_path, raw_path all still present)
  </acceptance_criteria>
  <done>00_config.sas defines &xwalk_backup_path and echoes it, matching existing conventions.</done>
</task>

<task type="auto">
  <name>Task 3: Write sas/20_pecan_id.sas (PID-01 through PID-04, PID-07, PID-08)</name>
  <read_first>
    - sas/19_raw_dir_inventory.sas (SECTION 0-1 header/options/route_log/fail_out macros;
      SECTION 5 certutil pattern lines 180-194)
    - sas/18_supplemental_raw_gap.sas (PUT-to-fileref text output pattern)
    - sas/03_prep_md3.sas (lines 59-93 = NULL sentinel rule; lines 107-108 = MRN/PRECEDE lengths)
    - sas/00_config.sas (path macro vars incl. new &xwalk_backup_path from Task 2)
    - .planning/phases/20-pecan-id-derivation/20-RESEARCH.md ("Recommended Program Structure",
      Patterns 1, 2, 3, 6, 7)
  </read_first>
  <files>sas/20_pecan_id.sas</files>
  <action>
    Create the new program with the SECTION structure from RESEARCH (SECTION 0 through
    SECTION 10). Follow every program-19 constraint (named-macro %abort cancel only via
    a %fail_out macro; explicit SELECT COUNT(*) INTO :macvar TRIMMED, never &SQLOBS;
    ASCII only; no apostrophes/semicolons in %PUT text; no PROC SQL UPDATE; no
    `data X; set X;`; period-terminated macro vars in paths; route_log/restore_log
    guarded by &in_pipeline).

    SECTION 0 -- %include 00_config.sas; options validvarname=v7 nofmterr msglevel=i;
      options nosyntaxcheck noerrorabend (before any PROC IMPORT). Define utility macros
      route_log (proc printto log="&logs_path.\20_pecan_id.log" new when in_pipeline=0),
      restore_log, and fail_out(msg=) (%put ERROR; %restore_log; %abort cancel). Call
      %check on &logs_path/&qc_path/&g_path existence then %route_log.

    SECTION 1 -- Precondition: assert all FOUR Phase 19 CSVs exist, ENUMERATED
      INDIVIDUALLY (PCM-T-12), each its own %if %sysfunc(fileexist(&qc_path.\<name>))=0
      then %fail_out naming that file: 19_raw_files.csv, 19_raw_key_columns.csv,
      19_raw_sheets.csv, 19_raw_variables_md3.csv. Also assert the md3 source CSV exists:
      &raw_path.\master\2018_2022_X_MASTER_DATASET_20240402.csv (D-09 exact folder).

    SECTION 2 -- PID-01 checksum. Run certutil on the md3 source CSV using the SECTION-5
      INFILE PIPE FILEVAR= pattern (build _cmd = 'certutil -hashfile "' || strip(path)
      || '" SHA256'; scan for the 64-hex line). PROC IMPORT qc/19_raw_files.csv (dbms=csv
      guessingrows=max) into work.files19; SELECT sha256 INTO :expected_sha TRIMMED WHERE
      upcase(strip(filename)) = '2018_2022_X_MASTER_DATASET_20240402.CSV'
      AND upcase(full_path) contains '\MASTER\' (exact-folder guard -- matches Phase 19
      presence check so a stray copy elsewhere under raw\ does not shadow this record). Compare
      computed to expected inside a named macro; %fail_out on mismatch. %put NOTE both values.

    SECTION 3 -- PID-01 cont (D-11). PROC IMPORT qc/19_raw_variables_md3.csv into
      work.inv_vars_md3. Read the var_type and var_length recorded for ENCRYPTED_MRN and
      for PRECEDE_STUDY_ID into macro vars (SELECT ... INTO ... TRIMMED). %put NOTE both.
      Set a macro flag &precede_is_num = 1 if the CSV recorded PRECEDE_STUDY_ID as num,
      else 0 -- used by the SECTION 5 cross-check cast (D-14, Pattern 6).

    SECTION 4 -- PID-01 cont (D-12 truncation guard). PROC IMPORT the md3 source CSV
      into work.md3csv with guessingrows=max (scans every row, sets column length to the
      longest observed value -- no truncation on import). Then via PROC SQL:
        SELECT max(lengthn(strip(ENCRYPTED_MRN))) INTO :max_mrn_len TRIMMED
        FROM work.md3csv;
      Assert &max_mrn_len <= 40 inside a named macro; %fail_out with message "source
      holds MRN longer than 40 -- merged file would silently truncate" if violated.
      %put NOTE the measured max length. Do NOT use a positional INFILE approach: it
      breaks on quoted fields containing commas and is unnecessary when guessingrows=max
      is used.

    SECTION 5 -- D-14 cross-check. Join work.md3csv to g.master_data_merged on
      PRECEDE_STUDY_ID after normalization. Apply to the CSV side EXACTLY the md3 prep
      rules confirmed in 03_prep_md3.sas: strip() on ENCRYPTED_MRN and PRECEDE_STUDY_ID;
      treat strip(upcase(ENCRYPTED_MRN)) = 'NULL' as blank. Cast PRECEDE_STUDY_ID to the
      same type on both sides: if &precede_is_num = 1 (CSV imported PRECEDE as numeric),
      compare numerically -- on the merged side compute
      _pid_key_num = input(strip(PRECEDE_STUDY_ID), best32.) and join on the raw numeric
      CSV column directly. Do NOT use put(csv_num, best32.) to convert the CSV side to
      char: put() drops leading zeros, so a merged value "000123" would never equal CSV
      "123". Assert _pid_key_num conversion produces zero missing values (%fail_out if
      > 0 -- means a non-numeric PRECEDE exists in the merged file). Assert (each its own
      named-macro gate, %fail_out on failure):
        (a) every matched row has equal normalized ENCRYPTED_MRN
        (b) the set of PRECEDE_STUDY_IDs is identical on both sides (0 orphans either way)
      %put NOTE counts for matched rows and orphan counts.

    SECTION 6 -- PID-02 source audit on g.master_data_merged. Using the exclusion rule
      (missing(ENCRYPTED_MRN) OR strip(upcase(ENCRYPTED_MRN))='NULL'), report via
      PROC SQL SELECT ... INTO ... TRIMMED and %put NOTE: count blank MRN, count
      placeholder MRN (separately, D-15), count distinct included MRN, count distinct
      PRECEDE_STUDY_ID, PRECEDE->MRN cardinality (max distinct MRN per PRECEDE),
      MRN->PRECEDE cardinality (max distinct PRECEDE per MRN).

    SECTION 7 -- PID-03 cardinality assertion. Named macro: SELECT COUNT(*) INTO
      :n_multi TRIMMED from a subquery grouping by PRECEDE_STUDY_ID HAVING
      COUNT(DISTINCT ENCRYPTED_MRN) > 1 (over included rows only). If &n_multi > 0
      %fail_out (PRECEDE maps to more than one MRN). MRN->many PRECEDE is reported in
      SECTION 6, NOT asserted.

    SECTION 8 -- PID-04 build_or_append_xwalk (D-01, D-02, D-04). Named macro
      %build_or_append_xwalk (this exact name -- referenced by must_haves). Logic:

      PRE-CONDITION: assert the backup folder (&xwalk_backup_path) exists using
      %sysfunc(fileexist(&xwalk_backup_path.)) before any case branch; %fail_out if
      absent (message: xwalk_backup_path does not exist -- create the folder first).
      Alternatively use OPTIONS DLCREATEDIR before the LIBNAME call so SAS creates it.
      Either way must be explicit; SAS does not auto-create library paths.

      Use a datetime-stamped backup filename pecan_id_xwalk_YYYYMMDD_HHMM.sas7bdat
      (not yymmddn8. alone) so two runs on the same day do not overwrite each other.
      Compute the stamp with %sysfunc(datetime()) formatted datetime20. and strip spaces.
      To find the latest backup, DIR PIPE list pecan_id_xwalk_*.sas7bdat and take the
      last line alphabetically (YYYYMMDD_HHMM sorts correctly).

      FOUR CASES (detect xwalk and backup independently before branching):
        1. FIRST RUN (no xwalk AND no backup): build g.pecan_id_xwalk from
           g.master_data_merged over INCLUDED rows (exclusion rule above), assigning
           pecan_ID as a rank in ascending order of each distinct MRN's SMALLEST
           PRECEDE_STUDY_ID ordered NUMERICALLY (if PRECEDE is char, order by
           input(PRECEDE_STUDY_ID, best32.) so "10" does not precede "9"). ENCRYPTED_MRN
           stored $40, pecan_ID numeric. Write the first dated backup via a DATA step
           (LIBNAME the backup folder, then data bak.pecan_id_xwalk_<stamp>; set
           g.pecan_id_xwalk; run;). %put NOTE initial build and backup path.

        2. RE-RUN (xwalk AND backup both exist): (1) assert g.pecan_id_xwalk equals
           latest backup by two-way NOT EXISTS row diff (n_xwalk_only + n_backup_only = 0,
           %fail_out otherwise); (2) identify new MRNs (in merged, not in xwalk), assign
           pecan_ID starting at max(pecan_ID)+1 in the same numeric-PRECEDE order,
           PROC APPEND base=g.pecan_id_xwalk data=work.new_mrns (new rows only, never
           rewrite); (3) assert every backup row still present after append; (4) write a
           new dated backup. %put NOTE append counts.

        3. BACKUP PRESENT, XWALK MISSING (deletion D-04 scenario): %fail_out immediately
           with message "g.pecan_id_xwalk is missing but a backup exists at
           &xwalk_backup_path -- restore from backup before re-running." Never rebuild
           from scratch when a backup exists; restoring preserves existing patient numbers.

        4. XWALK PRESENT, NO BACKUP: %fail_out with message "g.pecan_id_xwalk exists
           but no dated backup found in &xwalk_backup_path -- write a manual backup
           before re-running." (Alternatively, write the first backup automatically and
           log a WARNING rather than aborting -- document the choice made in DECISIONS.md
           as PCM-D-17 rationale.) Either way, do not proceed silently.

      Call %build_or_append_xwalk.

    SECTION 9 -- PID-07 linkage reach (D-20 through D-26). PROC IMPORT
      qc/19_raw_key_columns.csv and qc/19_raw_sheets.csv. Loop over key_columns rows
      where key_column_type = 'ENCRYPTED_MRN'. For each file+sheet+column: targeted
      re-import per D-25 (PROC IMPORT dbms=csv for CSVs; for xlsx use LIBNAME XLSX
      lazy column read or PROC IMPORT with SHEET= from 19_raw_sheets.csv -- NOT
      %import_csv/%import_xlsx). If key_columns records the column as numeric (D-26),
      do NOT convert; list it in a "type mismatch: not compared" block.

      For r7/r8/r9 identification: use an EXPLICIT FILENAME LIST derived from the Phase 18
      source names (from 18_supplemental_raw_gap.sas or its QC output). Do not rely on
      the r7/r8/r9 label being derivable from key_columns alone -- attach the
      "Links on MRN (PCM-D-16 test)" label to the rows that match those exact filenames.
      The md3 source CSV section is labeled "reference (crosswalk source)" (D-23); it
      must also appear in the output so its YES/NO line is distinct from the reference row.

      MRN normalization before matching: apply strip() and NULL-as-blank to raw MRN values
      on BOTH sides before any comparison (same rule as 03_prep_md3.sas: treat
      strip(upcase(mrn_col)) = 'NULL' as blank). Without this, padding whitespace or the
      literal string "NULL" shows up as a linkage failure.

      Compute matched rows / total rows and matched distinct MRNs / total distinct MRNs
      against g.pecan_id_xwalk; write counts alongside percentages. UNENC_MRN-only files
      go in an exclusions list (D-21). For r7/r8/r9 sections add an explicit line
      "Links on MRN (PCM-D-16 test): YES" if any distinct MRN matches, else NO, with the
      distinct-MRN match rate (D-24). Write all output to qc/20_linkage_reach.txt using
      the PUT-to-fileref pattern (open fresh, append sections with mod). Log echo a
      summary at the end.

    SECTION 10 -- PID-08 DECISIONS.md amendment is handled in Task 4 (separate file edit,
      not a SAS write). In SECTION 10 just %put NOTE that DECISIONS.md is updated
      manually in Plan 20-01 Task 4, and %put NOTE ==== Phase 20 program 20 complete ====;
      then %restore_log.

    End every assertion path through %fail_out. No em-dashes, no smart quotes.
  </action>
  <verify>
    <automated>grep -c "build_or_append_xwalk\|proc append\|fail_out\|abort cancel" sas/20_pecan_id.sas</automated>
  </verify>
  <acceptance_criteria>
    - File sas/20_pecan_id.sas exists
    - `grep -n "%include" sas/20_pecan_id.sas` includes 00_config.sas on the first include line
    - `grep -c "19_raw_files.csv" sas/20_pecan_id.sas` >= 1 AND all four handoff CSV names present: `grep -c "19_raw_key_columns.csv\|19_raw_sheets.csv\|19_raw_variables_md3.csv" sas/20_pecan_id.sas` >= 3
    - `grep -n "2018_2022_X_MASTER_DATASET_20240402" sas/20_pecan_id.sas` >= 1 (md3 source reference)
    - `grep -n "certutil" sas/20_pecan_id.sas` >= 1 AND `grep -n "filevar" sas/20_pecan_id.sas` >= 1 (PID-01 checksum via PIPE)
    - `grep -in "max_mrn_len\|<= 40\|le 40" sas/20_pecan_id.sas` >= 1 (D-12 truncation guard)
    - `grep -in "'NULL'" sas/20_pecan_id.sas` >= 1 (D-15 placeholder rule)
    - `grep -c "build_or_append_xwalk" sas/20_pecan_id.sas` >= 2 (macro definition + call)
    - `grep -in "proc append" sas/20_pecan_id.sas` >= 1 AND base=g.pecan_id_xwalk referenced
    - `grep -n "xwalk_backup_path" sas/20_pecan_id.sas` >= 1 (D-04 dated backup)
    - `grep -n "20_linkage_reach.txt" sas/20_pecan_id.sas` >= 1
    - `grep -in "PCM-D-16 test" sas/20_pecan_id.sas` >= 1 (PID-07 r7/r8/r9 YES/NO line)
    - `grep -in "not compared\|type mismatch" sas/20_pecan_id.sas` >= 1 (D-26 numeric MRN guard)
    - `grep -c "abort cancel" sas/20_pecan_id.sas` >= 1 AND every abort is inside a macro (no open-code abort): every "abort cancel" line is within fail_out
    - No `proc sql` UPDATE: `grep -in "update " sas/20_pecan_id.sas` returns no PROC SQL UPDATE
    - No in-place rewrite of g.pecan_id_xwalk via `data g.pecan_id_xwalk; set g.pecan_id_xwalk` (grep confirms absent)
    - ASCII only: `grep -nP "[^\x00-\x7F]" sas/20_pecan_id.sas` returns nothing
  </acceptance_criteria>
  <done>Program 20 verifies checksum, cross-checks the CSV, audits and asserts cardinality, builds the append-only crosswalk with dated backup, and writes the linkage reach report -- all assertions routed through %fail_out.</done>
</task>

<task type="auto">
  <name>Task 4: Record PCM-D-17 and PCM-D-18 in DECISIONS.md</name>
  <read_first>
    - docs/DECISIONS.md (lines 8-26 = Pending Decisions table; lines 29-60 = resolved-entry
      body format, e.g. PCM-D-05)
  </read_first>
  <files>docs/DECISIONS.md</files>
  <action>
    Two changes, ASCII only (no em-dashes, no smart quotes):

    1. In the Pending Decisions table (lines 10-25), add two rows after the PCM-D-14 row:
       | PCM-D-17 | pecan_ID derivation method + MRN retention | **Resolved 2026-09-23 -- see entry below** | Gerard |
       | PCM-D-18 | pecan_ID attach point (10b + 16b at build time) | **Resolved 2026-09-23 -- see entry below** | Gerard |

    2. Append two full resolved-entry bodies at the end of the file, matching the
       PCM-D-05 body style (heading, **Decision:**, **Rationale:**, attribution/date line).

       PCM-D-17 body must state: pecan_ID is a surrogate sequential integer; distinct
       ENCRYPTED_MRN numbered in ascending order of each MRN's smallest PRECEDE_STUDY_ID
       (numeric order); blank and placeholder ('NULL') MRNs excluded; crosswalk
       g.pecan_id_xwalk is append-only with a dated backup outside git; ENCRYPTED_MRN
       retained alongside pecan_ID (no DROP). Ruled out: unsalted SHA-256 hash;
       salted/keyed hash. Attributed to Gerard Garvan, dated 2026-09-23.

       PCM-D-18 body must state: pecan_ID attached by programs 10b and 16b at build time
       (single producer each); g.master_data_merged untouched; g.master_data_harmonized
       and g.analytic_cohort each go 174 -> 175 columns; PID-05 assertions (row count
       unchanged, zero blank pecan_ID where MRN non-blank/non-placeholder, zero
       PRECEDE_STUDY_ID gaining a second pecan_ID) live in 10b and 16b. Attributed to
       Gerard Garvan, dated 2026-09-23.
  </action>
  <verify>
    <automated>grep -c "PCM-D-17\|PCM-D-18" docs/DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PCM-D-17" docs/DECISIONS.md` >= 2 (table row + body heading)
    - `grep -c "PCM-D-18" docs/DECISIONS.md` >= 2 (table row + body heading)
    - `grep -in "2026-09-23" docs/DECISIONS.md` shows PCM-D-17 and PCM-D-18 dated entries
    - `grep -in "Gerard Garvan\|Gerard" docs/DECISIONS.md` attribution present in both new bodies
    - `grep -in "append-only" docs/DECISIONS.md` >= 1 (PCM-D-17 crosswalk property)
    - `grep -in "174" docs/DECISIONS.md` AND `grep -in "175" docs/DECISIONS.md` (PCM-D-18 column change)
    - ASCII only: `grep -nP "[^\x00-\x7F]" docs/DECISIONS.md` returns nothing
  </acceptance_criteria>
  <done>DECISIONS.md records PCM-D-17 and PCM-D-18 as resolved, attributed to Gerard Garvan, dated 2026-09-23, with full rationale bodies.</done>
</task>

</tasks>

<verification>
- Program 19 emits all four CSV handoffs (files, key_columns, sheets, variables_md3) and verifies each exists.
- 00_config.sas defines &xwalk_backup_path outside git/qc.
- Program 20 exists with all required sections; all asserts route through %fail_out; no PROC SQL UPDATE; no in-place xwalk rewrite; ASCII only.
- DECISIONS.md records PCM-D-17 and PCM-D-18.
- Runtime confirmation (executor runs program 19 then program 20 in separate SAS sessions per PCM-C-05): logs show `NOTE: ==== Phase 20 program 20 complete ====` and no ERROR; qc/20_linkage_reach.txt exists; g.pecan_id_xwalk exists with a dated backup.
</verification>

<success_criteria>
- All four acceptance-criteria task blocks pass their greps.
- Crosswalk g.pecan_id_xwalk built append-only with a dated backup; distinct pecan_ID count logged.
- qc/20_linkage_reach.txt contains a "Links on MRN (PCM-D-16 test): YES/NO" line for r7/r8/r9.
- PCM-D-17 and PCM-D-18 resolved in DECISIONS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/20-pecan-id-derivation/20-01-SUMMARY.md`
</output>
