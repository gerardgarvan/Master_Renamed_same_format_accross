---
phase: 20-pecan-id-derivation
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - sas/10b_concept_harmonize.sas
  - sas/16b_cohort_rebuild.sas
  - sas/08_dictionary.sas
files_created:
  - docs/DATA_DICTIONARY.xlsx
autonomous: true
requirements: [PID-05, PID-06, PID-08]
must_haves:
  truths:
    - "Program 10b joins g.pecan_id_xwalk and g.master_data_harmonized goes 174 -> 175 columns"
    - "Program 16b carries pecan_ID through and g.analytic_cohort goes 174 -> 175 columns"
    - "10b and 16b each assert row count unchanged, zero blank pecan_ID where MRN non-blank, zero PRECEDE gaining a second pecan_ID"
    - "16b writes distinct pecan_ID counts and 1/2/3+ encounter distribution to qc/16b_pecan_id_counts.txt"
    - "08_dictionary.sas appends an explicit pecan_ID row and _gate5 expects n_dict_meta + 1"
    - "DATA_DICTIONARY.xlsx contains a pecan_ID row with a derivation note"
  artifacts:
    - path: "sas/10b_concept_harmonize.sas"
      provides: "pecan_ID join + PID-05 assertions + 175-column update"
      contains: "pecan_id_xwalk"
    - path: "sas/16b_cohort_rebuild.sas"
      provides: "pecan_ID pass-through + PID-05 assertions + PID-06 counts + 175-column update"
      contains: "16b_pecan_id_counts.txt"
    - path: "sas/08_dictionary.sas"
      provides: "explicit pecan_ID dict_final row + _gate5 update"
      contains: "pecan_ID"
  key_links:
    - from: "sas/10b_concept_harmonize.sas"
      to: "g.pecan_id_xwalk"
      via: "PROC SQL left join on ENCRYPTED_MRN, promote via WORK (PCM-T-02)"
      pattern: "pecan_id_xwalk"
    - from: "sas/16b_cohort_rebuild.sas"
      to: "g.master_data_harmonized"
      via: "SET carries pecan_ID through the cohort filter"
      pattern: "pecan_ID"
    - from: "sas/08_dictionary.sas"
      to: "work.dict_final"
      via: "explicit row append after dict_final built; _gate5 = n_dict_meta + 1"
      pattern: "n_dict_meta"
---

<objective>
Attach pecan_ID to the two producer datasets and document it. Program 10b joins the
crosswalk built in Plan 01 onto g.master_data_harmonized; program 16b carries pecan_ID
through the cohort filter, runs the PID-06 counts, and asserts attachment; program 08
adds an explicit pecan_ID row to the data dictionary and regenerates DATA_DICTIONARY.xlsx.

Purpose: Complete PID-05, PID-06, PID-08 (dictionary side). This is Wave 2: it depends on
g.pecan_id_xwalk existing (Plan 01, Wave 1).
Output: modified 10b, 16b, 08; regenerated DATA_DICTIONARY.xlsx (175 rows).

CRITICAL research finding (must be honored): the 08_dictionary.sas derivation_map is
LEFT-joined onto dict_with_owner, which contains ONLY g.master_data_merged variables.
pecan_ID is NOT in g.master_data_merged, so a derivation_map entry would be silently
dropped. pecan_ID requires an EXPLICIT ROW appended to work.dict_final AFTER the main
join, AND _gate5 must change from `ne &n_dict_meta` to `ne %eval(&n_dict_meta + 1)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-pecan-id-derivation/20-CONTEXT.md
@.planning/phases/20-pecan-id-derivation/20-RESEARCH.md
@sas/10b_concept_harmonize.sas
@sas/16b_cohort_rebuild.sas
@sas/08_dictionary.sas

<interfaces>
<!-- Exact insertion points and current assertion values. Extracted from codebase. -->

g.pecan_id_xwalk (built by Plan 01): columns ENCRYPTED_MRN ($40), pecan_ID (num).

10b (sas/10b_concept_harmonize.sas):
- SECTION 5 %build_harmonized writes g.master_data_harmonized (DATA step ends line 950,
  macro call line 952). Insert the pecan_ID join AFTER line 952, BEFORE SECTION 6.
- SECTION 6 assertions begin line 957. work.src_changed logic (lines 969-989) compares
  MASTER_DATA_MERGED to MASTER_DATA_HARMONIZED column-by-column: pecan_ID is a NEW column
  present only in harmonized, so it will NOT appear in src_changed (which keys off columns
  IN merged that vanished/changed). Confirm no assertion counts total columns == 174 in 10b
  (grep found no `ne 174` in 10b; the merged-side assertion is `ne 176` and is unaffected).
- g.master_data_merged has ENCRYPTED_MRN (owned by md3, $40) available for the join key.

16b (sas/16b_cohort_rebuild.sas):
- SECTION 2 (lines 155-158): data work.cohort_candidate; set g.master_data_harmonized;
  where ... run;  -- a full SET, so pecan_ID flows through automatically once 10b adds it.
- SECTION 5 %verify_cohort_cols (lines 387-398): asserts `&n_cohort_cols ne 174` -> UPDATE to 175.
- SECTION 6 %assert_harmonized_unchanged (lines 410-423): asserts `&n_cols ne 174 or
  &n_rows ne 41150` on g.master_data_harmonized -> UPDATE the 174 to 175 (rows stay 41150).
- SECTION 7 writes qc/16b_cohort_missingness.txt via `data _null_; file "..." mod;` pattern.
  PID-06 output goes to a SEPARATE file qc/16b_pecan_id_counts.txt.

08 (sas/08_dictionary.sas):
- work.dict_final built at SECTION 4 (lines 289-303), re-created at SECTION 5 lines 319-322.
  Append the pecan_ID row AFTER line 322 (final dict_final), BEFORE SECTION 6 (line 325).
- _gate5 (lines 330-335): `%if &n_final ne &n_dict_meta` -> change to `%eval(&n_dict_meta + 1)`.
- n_dict_meta set at line 127 from g.master_data_merged column count.
- dict_final columns: varname, type, length, source, derivation ($200), label, coverage_pct.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pecan_ID join + PID-05 assertions + column update to 10b</name>
  <read_first>
    - sas/10b_concept_harmonize.sas (lines 895-952 = SECTION 5 build; lines 955-1050 =
      SECTION 6 assertions; header lines 1-40 for constraints)
    - .planning/phases/20-pecan-id-derivation/20-RESEARCH.md (Pattern 5 = 10b/16b join)
  </read_first>
  <files>sas/10b_concept_harmonize.sas</files>
  <action>
    After %build_harmonized is called (line 952) and BEFORE SECTION 6 (line 955), insert a
    new SECTION "5b: Attach pecan_ID (PID-05, PCM-D-18)". Two steps, honoring PCM-T-02
    (no in-place rewrite -- build in WORK, then promote):

      proc sql noprint;
        create table work.harmonized_with_pid as
        select h.*, x.pecan_ID
        from g.master_data_harmonized as h
        left join g.pecan_id_xwalk as x
          on h.ENCRYPTED_MRN = x.ENCRYPTED_MRN;
      quit;

      data g.master_data_harmonized;
        set work.harmonized_with_pid;
      run;

    Then add PID-05 assertions inside a named macro %assert_pecan_attach (routed through
    the existing %fail_out), using the exclusion rule for the blank test:
      - n_post = count(*) of g.master_data_harmonized; %fail_out if n_post ne &n_rows
        (row count unchanged, &n_rows is the merged/harmonized row count already in scope
        -- confirm the in-scope macro var name in SECTION 6; it is &n_rows / &n_out)
      - n_blank_pid = count where pecan_ID is missing AND not missing(ENCRYPTED_MRN)
        AND strip(upcase(ENCRYPTED_MRN)) ne 'NULL'; %fail_out if > 0
      - n_dup_pid = count from (select PRECEDE_STUDY_ID, count(distinct pecan_ID) as n_pid
        group by PRECEDE_STUDY_ID having calculated n_pid > 1); %fail_out if > 0
    %put NOTE for each measured value.

    Column-count update: 10b has NO `ne 174` assertion (verified by grep), so no numeric
    edit is needed there. Add a NEW explicit column-count confirmation after the join:
      %macro assert_harm_175;
        %local n_hc;
        proc sql noprint;
          select count(*) into :n_hc trimmed from dictionary.columns
          where libname='G' and upcase(memname)='MASTER_DATA_HARMONIZED';
        quit;
        %if &n_hc ne 175 %then %fail_out(msg=g.master_data_harmonized has &n_hc columns after pecan_ID join%str(,) expected 175);
        %put NOTE: [10b] g.master_data_harmonized has 175 columns after pecan_ID attach;
      %mend assert_harm_175;
      %assert_harm_175;

    Do NOT touch the merged-side `ne 176` assertion (pecan_ID is not added to merged).
    ASCII only; period-terminated macro vars in any paths; no PROC SQL UPDATE.
  </action>
  <verify>
    <automated>grep -c "pecan_id_xwalk\|pecan_ID\|assert_harm_175" sas/10b_concept_harmonize.sas</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pecan_id_xwalk" sas/10b_concept_harmonize.sas` >= 1 (the left join)
    - `grep -n "harmonized_with_pid" sas/10b_concept_harmonize.sas` >= 2 (create + set)
    - The join and promotion appear AFTER `%build_harmonized;` (line ~952) and BEFORE SECTION 6 (`grep -n` line ordering confirms)
    - `grep -in "assert_pecan_attach\|n_blank_pid\|n_dup_pid" sas/10b_concept_harmonize.sas` >= 3 (PID-05 assertions)
    - `grep -in "'NULL'" sas/10b_concept_harmonize.sas` present in the blank-pid test
    - `grep -c "175" sas/10b_concept_harmonize.sas` >= 1 (assert_harm_175)
    - The merged-side `176` assertion is unchanged: `grep -n "176" sas/10b_concept_harmonize.sas` still present
    - No new `data g.master_data_harmonized; set g.master_data_harmonized` in-place pattern (promotion is from work.harmonized_with_pid)
    - No PROC SQL UPDATE: `grep -in "update " sas/10b_concept_harmonize.sas` returns no SQL update
    - Every new abort routes through %fail_out (no bare open-code %abort)
    - ASCII only: `grep -nP "[^\x00-\x7F]" sas/10b_concept_harmonize.sas` returns nothing
  </acceptance_criteria>
  <done>10b attaches pecan_ID via WORK-then-promote, asserts row count unchanged + zero blank pecan_ID + zero duplicate pecan_ID per PRECEDE, and confirms 175 columns.</done>
</task>

<task type="auto">
  <name>Task 2: Update 16b column asserts, add PID-05 assertions, add PID-06 counts</name>
  <read_first>
    - sas/16b_cohort_rebuild.sas (lines 155-158 SECTION 2 filter; lines 369-398 SECTION 5
      promotion + verify_cohort_cols; lines 410-423 assert_harmonized_unchanged;
      lines 449-473 SECTION 7 QC summary + PUT pattern)
    - .planning/phases/20-pecan-id-derivation/20-CONTEXT.md (D-17, D-18, D-19 PID-06 spec)
  </read_first>
  <files>sas/16b_cohort_rebuild.sas</files>
  <action>
    Because 16b reads g.master_data_harmonized (which now carries pecan_ID after 10b),
    the SECTION 2 full SET already propagates pecan_ID -- no filter change needed. Make
    these edits:

    1. SECTION 5 %verify_cohort_cols (line 394-395): change `ne 174` -> `ne 175` and the
       message/NOTE text "174" -> "175". This is the g.analytic_cohort column count.

    2. SECTION 6 %assert_harmonized_unchanged (lines 419-421): change `&n_cols ne 174`
       -> `&n_cols ne 175` and the message/NOTE "174 columns" -> "175 columns". Rows stay
       41150 (do not change 41150).

    3. Add SECTION "5c: PID-05 attachment assertions" after promotion (after
       %verify_cohort_cols, line 398), inside a named macro routed through %fail_out,
       measured on g.analytic_cohort:
       - row count unchanged: count(*) of g.analytic_cohort must equal &n_admitted
         (already asserted by %verify_promotion; add the pecan-specific checks below)
       - n_blank_pid = count where pecan_ID missing AND not missing(ENCRYPTED_MRN)
         AND strip(upcase(ENCRYPTED_MRN)) ne 'NULL'; %fail_out if > 0
       - n_dup_pid = count from (select PRECEDE_STUDY_ID, count(distinct pecan_ID) as n_pid
         group by PRECEDE_STUDY_ID having calculated n_pid > 1); %fail_out if > 0
       %put NOTE each measured value.

    4. Add SECTION "6b: PID-06 pecan_ID counts" writing to a NEW file
       qc/16b_pecan_id_counts.txt (PUT-to-fileref pattern, open fresh then append with
       mod). Report side by side for BOTH g.master_data_harmonized (41,150) and
       g.analytic_cohort (13,890):
       - distinct pecan_ID count in each
       - encounter-per-patient distribution: how many pecan_IDs have exactly 1, exactly 2,
         and 3+ rows (encounters). Compute via PROC SQL group by pecan_ID then bucket.
       Echo a summary to the log. Use grep-able key=value lines
       (e.g. distinct_pecan_id_harmonized=NNNN, pecan_id_1enc_cohort=NNNN, etc.).

    ASCII only; no PROC SQL UPDATE; no in-place rewrite; aborts via %fail_out.
  </action>
  <verify>
    <automated>grep -c "175\|16b_pecan_id_counts.txt\|pecan_ID" sas/16b_cohort_rebuild.sas</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ne 174" sas/16b_cohort_rebuild.sas` returns ZERO matches (both 174 asserts updated)
    - `grep -c "175" sas/16b_cohort_rebuild.sas` >= 2 (verify_cohort_cols + assert_harmonized_unchanged)
    - `grep -n "41150" sas/16b_cohort_rebuild.sas` still present in assert_harmonized_unchanged (rows unchanged)
    - `grep -in "n_blank_pid\|n_dup_pid" sas/16b_cohort_rebuild.sas` >= 2 (PID-05 assertions)
    - `grep -in "'NULL'" sas/16b_cohort_rebuild.sas` present in the blank-pid test
    - `grep -c "16b_pecan_id_counts.txt" sas/16b_cohort_rebuild.sas` >= 1 (PID-06 output file)
    - `grep -in "distinct.*pecan\|pecan.*distinct\|distinct_pecan_id" sas/16b_cohort_rebuild.sas` >= 1 (distinct counts)
    - `grep -in "1enc\|2enc\|3\+\|encounter" sas/16b_cohort_rebuild.sas` >= 1 (encounter distribution)
    - Both g.master_data_harmonized and g.analytic_cohort referenced in the PID-06 section (grep both memnames near 16b_pecan_id_counts.txt)
    - No PROC SQL UPDATE; every abort via %fail_out
    - ASCII only: `grep -nP "[^\x00-\x7F]" sas/16b_cohort_rebuild.sas` returns nothing
  </acceptance_criteria>
  <done>16b carries pecan_ID through, asserts 175 columns and PID-05 conditions, and writes PID-06 distinct counts + encounter distribution for both datasets to qc/16b_pecan_id_counts.txt.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit pecan_ID row to 08_dictionary.sas and update _gate5, regenerate xlsx</name>
  <read_first>
    - sas/08_dictionary.sas (lines 224-303 SECTION 4 derivation_map + dict_final;
      lines 309-322 SECTION 5 dict_final2 rebuild; lines 325-349 SECTION 6 _gate5;
      line 127 n_dict_meta source; lines 351-359 SECTION 7 ODS EXCEL header)
    - .planning/phases/20-pecan-id-derivation/20-RESEARCH.md (Pattern 4 + Pitfall 1 =
      derivation_map LEFT JOIN drops pecan_ID; explicit row required)
  </read_first>
  <files>sas/08_dictionary.sas</files>
  <action>
    Do NOT add pecan_ID to work.derivation_map (SECTION 4) -- the LEFT JOIN onto
    dict_with_owner (which holds only g.master_data_merged columns) would silently drop it
    (Pitfall 1). Instead:

    1. After the FINAL work.dict_final is built (SECTION 5, after line 322 where
       `data work.dict_final; set work.dict_final2; format coverage_pct 6.1; run;`), and
       BEFORE SECTION 6 (_gate5, line 325), append ONE explicit pecan_ID row via a DATA
       step that matches the dict_final column structure
       (varname, type, length, source, derivation, label, coverage_pct):

       data work.pecan_row;
         length varname $64 type $4 length 8 source $32 derivation $200 label $256 coverage_pct 8;
         varname     = "pecan_ID";
         type        = "num";
         length      = 8;
         source      = "Phase 20";
         derivation  = "surrogate patient key derived from ENCRYPTED_MRN in Phase 20; attached to g.master_data_harmonized and g.analytic_cohort by programs 10b and 16b";
         label       = "PeCAN surrogate patient linkage key";
         coverage_pct = .;
         output;
       run;

       data work.dict_final;
         set work.dict_final work.pecan_row;
       run;

       (Confirm exact column names/lengths against the actual dict_final at line 319-322
       and adjust the pecan_row LENGTH statement to match to avoid PROC APPEND-style
       truncation warnings.)

    2. Update _gate5 (lines 330-335): change
         %if &n_final ne &n_dict_meta %then
       to
         %if &n_final ne %eval(&n_dict_meta + 1) %then
       and update the failure message text to reference `&n_dict_meta + 1` (the pecan_ID
       row is the +1). Keep the assertion routed through %fail_out.

    ASCII only; no PROC SQL UPDATE; no in-place rewrite trap (the
    `data work.dict_final; set work.dict_final work.pecan_row;` is a WORK dataset rebuild,
    which is acceptable -- PCM-T-02 concerns permanent g.* datasets; but to be safe and
    match the pattern, build into work.dict_final_all then rename, OR keep the WORK
    set-self which is low-risk for WORK. Prefer building work.dict_final_all and then
    `data work.dict_final; set work.dict_final_all; run;` to avoid any set-self ambiguity.)
  </action>
  <verify>
    <automated>grep -c "pecan_ID\|n_dict_meta + 1\|pecan_row" sas/08_dictionary.sas</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pecan_ID" sas/08_dictionary.sas` >= 1 (explicit row varname)
    - `grep -n "pecan_row\|dict_final_all" sas/08_dictionary.sas` >= 1 (explicit-row DATA step)
    - The pecan_ID row is added AFTER dict_final's final build (line ~322) and BEFORE _gate5 (line ~330): grep -n line ordering confirms
    - pecan_ID is NOT added to work.derivation_map: `grep -n "derivation_map" sas/08_dictionary.sas` shows no pecan_ID varname line inside the SECTION 4 block (lines 224-285)
    - `grep -n "n_dict_meta + 1\|%eval(&n_dict_meta" sas/08_dictionary.sas` >= 1 (_gate5 updated)
    - `grep -in "surrogate patient" sas/08_dictionary.sas` >= 1 (derivation note text)
    - No PROC SQL UPDATE; no `data work.dict_final; set work.dict_final ` self-set (prefer dict_final_all intermediary)
    - ASCII only: `grep -nP "[^\x00-\x7F]" sas/08_dictionary.sas` returns nothing
  </acceptance_criteria>
  <done>08_dictionary.sas appends an explicit pecan_ID row with derivation note and _gate5 expects n_dict_meta + 1; running 08 regenerates DATA_DICTIONARY.xlsx with the pecan_ID row.</done>
</task>

</tasks>

<verification>
- 10b attaches pecan_ID (join to g.pecan_id_xwalk), asserts PID-05, confirms 175 columns; merged 176-column assertion unchanged.
- 16b column asserts read 175 (no `ne 174` remaining); PID-05 asserted; PID-06 counts written to qc/16b_pecan_id_counts.txt for both datasets.
- 08 appends explicit pecan_ID row and _gate5 expects n_dict_meta + 1; no derivation_map entry for pecan_ID.
- Runtime confirmation (executor runs 20 -> 10b -> 16b -> 08 in separate SAS sessions per PCM-C-05): no ERROR in logs; g.master_data_harmonized and g.analytic_cohort each 175 columns; DATA_DICTIONARY.xlsx contains a pecan_ID row.
</verification>

<success_criteria>
- All three task acceptance-criteria blocks pass their greps.
- g.master_data_harmonized and g.analytic_cohort each have 175 columns with pecan_ID populated; row counts unchanged (41,150 and 13,890).
- PID-05 assertions pass in both 10b and 16b; PID-06 counts present.
- DATA_DICTIONARY.xlsx has a pecan_ID row with derivation note.
</success_criteria>

<output>
After completion, create `.planning/phases/20-pecan-id-derivation/20-02-SUMMARY.md`
</output>
