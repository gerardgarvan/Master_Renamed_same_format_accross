---
phase: 21-runner-wiring-d3-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sas/17_summary_stats_by_domain.sas
  - docs/DECISIONS.md
autonomous: false
requirements:
  - FIX-01

must_haves:
  truths:
    - "DOMAIN_MAP_APPROVED is set to 1 at line 145 of 17_summary_stats_by_domain.sas with a comment pointing to PCM-D-19 in DECISIONS.md"
    - "pecan_ID is excluded from program 17 statistics by the SAME mechanism program 17 already uses for PRECEDE_STUDY_ID and ENCRYPTED_MRN"
    - "Program 17 reads g.analytic_cohort as its primary source instead of g.analysis_base; a keyed comparison of the two datasets is written to qc/17_pcm_d20_compare.txt (not the SAS listing)"
    - "Every hard-coded expectation about the base dataset in program 17 (row count, existence check, column list) is updated for g.analytic_cohort"
    - "PCM-D-20 is recorded in docs/DECISIONS.md with rationale, date, and the reversed D-10 note"
    - "docs/DECISIONS.md entry for PCM-D-19 approves DOMAIN_MAP_APPROVED=1; its result line is filled in only after the run confirms the D3 sheet"
  artifacts:
    - path: "sas/17_summary_stats_by_domain.sas"
      provides: "Modified program with gate=1, input redirect, PECAN_ID row, PROC COMPARE block"
      contains: "DOMAIN_MAP_APPROVED = 1"
    - path: "docs/DECISIONS.md"
      provides: "PCM-D-19 and PCM-D-20 decision records"
      contains: "PCM-D-20"
  key_links:
    - from: "sas/17_summary_stats_by_domain.sas line ~6"
      to: "g.analytic_cohort"
      via: "libname g reference in PROC COMPARE and data step"
      pattern: "analytic_cohort"
    - from: "sas/17_summary_stats_by_domain.sas line 145"
      to: "Sections 5-11 gate"
      via: "%gate_stats macro"
      pattern: "DOMAIN_MAP_APPROVED = 1"
---

<objective>
Fix the D3 cognitive domain gate (FIX-01) and implement the PCM-D-20 input redirect for program 17.

Purpose: DOMAIN_MAP_APPROVED=0 at line 145 prevents program 17 from producing the xlsx workbook with
the D3 sheet. g.analysis_base is a P:-drive artifact not produced by the pipeline (Q1 research finding).
PCM-D-20 redirects program 17 to read g.analytic_cohort instead, with a PROC COMPARE validation block
to document the difference before the redirect takes effect.

Output:
- sas/17_summary_stats_by_domain.sas modified (gate flip, input redirect, PECAN_ID exclusion row, PROC COMPARE)
- docs/DECISIONS.md updated with PCM-D-19 and PCM-D-20 entries
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-runner-wiring-d3-fix/21-CONTEXT.md

<interfaces>
From sas/17_summary_stats_by_domain.sas (key lines):
  Line 6:   "g.analysis_base" -- input dataset reference in program header (Reads: section)
  Line 145: %let DOMAIN_MAP_APPROVED = 0;   <- gate to flip to 1
  Line 1666-end: data work.domain_lookup; DATALINES block
  Line 1711: COGNITIVE_SCORE,D3,instrument,...   (already correct, no change)
  Line 1712: COGNITIVE_CATEGORY,D3,instrument,...  (already correct, no change)

From sas/00_config.sas:
  Line 21:  %let g_path = P:\PeCAN Master Data\Gerard\...\merge;
  libname g is assigned by programs that use it; the executor must verify
  that program 17 assigns libname g before the PROC COMPARE step.

PCM-D-20 (planning_context decision):
  - Redirect program 17 to read g.analytic_cohort instead of g.analysis_base
  - Run PROC COMPARE between g.analysis_base and g.analytic_cohort BEFORE the redirect
    to document the difference (rows, columns, PRECEDE overlap)
  - Add PECAN_ID exclusion DATALINES row (reverses D-10)
  - Confirm every column program 17 uses exists in g.analytic_cohort
  - Record in DECISIONS.md as PCM-D-20

PCM-D-19 (CONTEXT.md D-09):
  - DOMAIN_MAP_APPROVED=0 -> 1; comment points to PCM-D-19 in DECISIONS.md
  - Record PCM-D-19 in DECISIONS.md: approves gate, supersedes v1 checkpoint-2

DATALINES rules: no em dashes (use --), no commas inside description field, ASCII only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip gate, redirect input, add keyed comparison, exclude pecan_ID in program 17</name>
  <files>sas/17_summary_stats_by_domain.sas</files>
  <read_first>
    - sas/17_summary_stats_by_domain.sas (full file -- lines 1-30 header; ~130-145 config/gate; 196-205 gate macros;
      234-274 base-dataset existence and row-count assertions; every g.analysis_base reference; 1602-1610 GUARD 5
      identifier regex and Section 3b; 1666-1730 DATALINES)
    - sas/00_config.sas lines 1-68 (g_path, libname conventions)
  </read_first>
  <action>
Make the following targeted edits to sas/17_summary_stats_by_domain.sas. Do NOT restructure other code.

EDIT 1 -- Header "Reads" section (approx line 22):
  "  Reads   : g.analysis_base            (read-only)"
  ->
  "  Reads   : g.analytic_cohort          (read-only; redirected from g.analysis_base per PCM-D-20 2026-09-23)"

EDIT 2 -- Gate at line 145:
  "%let DOMAIN_MAP_APPROVED = 0;"
  ->
  "%let DOMAIN_MAP_APPROVED = 1;  /* PCM-D-19 -- approved by Gerard 2026-09-23 -- see docs/DECISIONS.md */"
  The comment must NOT assert the workbook result; that is confirmed after the run.

EDIT 3 -- Keyed comparison block (PCM-D-20 audit). Place it AFTER libname g is assigned and AFTER the log
has been routed (%route_log or equivalent), and BEFORE the base-dataset existence checks at ~234.
PROC COMPARE output must NOT go to the SAS listing: in batch the .lst file is written next to -sysin,
i.e. inside the git working tree, and it would contain record values (PHI). Route it to qc/ instead.
Compare by key, not by row position:

  %macro pcm_d20_compare;
    %local n_b n_c k_b k_c n_b_only n_c_only;
    %if %sysfunc(exist(g.analysis_base)) = 0 %then %do;
      %put NOTE: [PCM-D-20] g.analysis_base not found -- comparison skipped;
      %return;
    %end;
    proc sql noprint;
      select count(*), count(distinct PRECEDE_STUDY_ID) into :n_b trimmed, :k_b trimmed from g.analysis_base;
      select count(*), count(distinct PRECEDE_STUDY_ID) into :n_c trimmed, :k_c trimmed from g.analytic_cohort;
      select count(*) into :n_b_only trimmed from g.analysis_base
        where PRECEDE_STUDY_ID not in (select PRECEDE_STUDY_ID from g.analytic_cohort);
      select count(*) into :n_c_only trimmed from g.analytic_cohort
        where PRECEDE_STUDY_ID not in (select PRECEDE_STUDY_ID from g.analysis_base);
    quit;
    proc sort data=g.analysis_base   out=work._d20_b; by PRECEDE_STUDY_ID; run;
    proc sort data=g.analytic_cohort out=work._d20_c; by PRECEDE_STUDY_ID; run;
    proc printto print="&qc_path.\17_pcm_d20_compare.txt" new; run;
    title "PCM-D-20: g.analysis_base (rows &n_b) vs g.analytic_cohort (rows &n_c)";
    title2 "PRECEDE only in analysis_base: &n_b_only -- only in analytic_cohort: &n_c_only";
    proc compare base=work._d20_b compare=work._d20_c criterion=0.00001 listvar maxprint=(40,500);
      id PRECEDE_STUDY_ID;
    run;
    title;
    proc printto; run;
    %put NOTE: [PCM-D-20] analysis_base rows=&n_b keys=&k_b -- analytic_cohort rows=&n_c keys=&k_c;
    %put NOTE: [PCM-D-20] PRECEDE only in base=&n_b_only -- only in cohort=&n_c_only;
  %mend pcm_d20_compare;
  %pcm_d20_compare;

  (Confirm the qc path macro variable name used in this program; adjust &qc_path if it differs.)

EDIT 4 -- Redirect every data-reading reference. Replace each "g.analysis_base" in the program body
(set, from, data= references; research lists lines 234-274, 367, 399, 471, 478, 534, 604, 644, 650)
with "g.analytic_cohort". Leave only the header note and the %pcm_d20_compare block referring to
g.analysis_base.

EDIT 5 -- Update hard-coded expectations tied to the old base dataset. Lines ~234-274 assert the base
dataset exists and check its row count. Change the expected row count to 13890 (the g.analytic_cohort
count asserted by 16b) and update any message text. Grep for any other literal tied to analysis_base
(row counts, column counts such as 174, expected variable lists) and update each; record every change
in the SUMMARY.

EDIT 6 -- Column coverage check. List every variable program 17 reads BY NAME from the base dataset
(for example the year variable driving the per-year blocks, and any variable used by the D15
analysis_base_ext merge). Add an in-program assertion macro that checks each is present in
g.analytic_cohort via dictionary.columns and calls %fail_out naming any missing variable. Enumerate
the variables individually (PCM-T-12).

EDIT 7 -- Exclude pecan_ID using the EXISTING identifier mechanism. g.analytic_cohort carries pecan_ID
(Phase 20), so D-10 is reversed. Before editing, read GUARD 5 and Section 3b (~1602-1610) and determine
how PRECEDE_STUDY_ID and ENCRYPTED_MRN are currently kept out of the statistics:
  (a) If the identifier regex already matches pecan_ID (case-insensitive), no DATALINES row is needed --
      record that finding in the SUMMARY and add pecan_ID to any explicit identifier list the regex
      block keeps, if one exists.
  (b) If identifiers are excluded by DATALINES rows, add one row for PECAN_ID using EXACTLY the domain
      and assign_rule values those identifier rows use. ASCII only, no commas inside the description,
      "--" instead of dashes, e.g. description "patient linkage key -- excluded per PCM-D-20".
  (c) If GUARD 5 aborts when an identifier appears in the lookup, do not add a row; use (a).
Do not invent new domain or assign_rule values. Lines 1711-1712 (COGNITIVE_SCORE, COGNITIVE_CATEGORY)
remain unchanged.
  </action>
  <verify>
    <automated>findstr /n "DOMAIN_MAP_APPROVED = 1" "C:\Master_Renamed_same_format_accross\sas\17_summary_stats_by_domain.sas"</automated>
    Also run: findstr /n "analysis_base" "C:\Master_Renamed_same_format_accross\sas\17_summary_stats_by_domain.sas"
    Also run: findstr /n "pcm_d20_compare 17_pcm_d20_compare.txt" "C:\Master_Renamed_same_format_accross\sas\17_summary_stats_by_domain.sas"
  </verify>
  <acceptance_criteria>
    - "DOMAIN_MAP_APPROVED = 1" present; its comment points to DECISIONS.md and asserts no result
    - "analysis_base" appears only in the header note and inside %pcm_d20_compare
    - PROC COMPARE uses an ID statement on PRECEDE_STUDY_ID and runs under PROC PRINTTO to qc/17_pcm_d20_compare.txt
    - The base row-count assertion expects 13890 (or reads the value 16b asserts), not the old analysis_base count
    - A column-coverage assertion macro exists and names each required variable individually
    - pecan_ID exclusion follows the existing identifier mechanism; SUMMARY states which of (a)/(b)/(c) applied
    - ASCII only; no open-code %IF; every abort via %fail_out
  </acceptance_criteria>
  <done>Program 17 has gate=1, reads g.analytic_cohort, audits the switch in qc/, checks its required columns, and excludes pecan_ID the same way it excludes other identifiers</done>
</task>

<task type="auto">
  <name>Task 2: Record PCM-D-19 and PCM-D-20 in DECISIONS.md</name>
  <files>docs/DECISIONS.md</files>
  <read_first>
    - docs/DECISIONS.md (full file -- find the highest existing decision number and the formatting convention)
  </read_first>
  <action>
Append two new decision records to docs/DECISIONS.md, after the last existing entry. Use the same
formatting convention already in the file (horizontal rule separator if present, same heading level).

PCM-D-19 entry:
  ### PCM-D-19 -- DOMAIN_MAP_APPROVED Gate Approval (2026-09-23)
  **Status:** APPROVED
  **Decision:** Set DOMAIN_MAP_APPROVED = 1 in sas/17_summary_stats_by_domain.sas line 145.
  **Rationale:** The DATALINES rows for COGNITIVE_SCORE and COGNITIVE_CATEGORY (lines 1711-1712) were
  confirmed correct at domain=D3 assign_rule=instrument. The gate was previously held at 0 pending
  Checkpoint 1 review (v1.0). That review is now complete. This approval supersedes the v1 checkpoint-2
  approval which was made when D3 was absent from the workbook.
  **Effect:** Sections 5-11 of program 17 are no longer blocked by the gate.
  **Result:** PENDING -- to be filled in at the Task 3 checkpoint after program 17 runs and the D3
  sheet is confirmed present and populated in qc/17_summary_stats_by_domain.xlsx.
  **Attribution:** Gerard, 2026-09-23

PCM-D-20 entry:
  ### PCM-D-20 -- Program 17 Input Redirect: g.analysis_base -> g.analytic_cohort (2026-09-23)
  **Status:** APPROVED (Option B from D-07a discussion)
  **Decision:** Program 17 now reads g.analytic_cohort as its primary source instead of g.analysis_base.
  A keyed comparison (%pcm_d20_compare) writes rows, PRECEDE overlap in both directions and a PROC
  COMPARE by PRECEDE_STUDY_ID to qc/17_pcm_d20_compare.txt whenever g.analysis_base is present.
  **Rationale:** g.analysis_base is a P:-drive artifact not produced by any program in this repo.
  A clean end-to-end run per RUN-01 requires all inputs to be pipeline-produced. g.analytic_cohort
  (produced by 16b_cohort_rebuild.sas; INPATIENT+OBSERVATION per PCM-D-05; 175 columns including
  pecan_ID; 13,890 rows) is the pipeline-produced replacement. Whether it matches g.analysis_base is
  NOT assumed: the comparison file records the difference, and the Task 3 checkpoint summarizes it here.
  **Provenance of g.analysis_base:** unknown at the time of this decision (no program in the repo writes
  it). Gerard to add its origin here if recalled.
  **Reverses:** D-10. g.analytic_cohort carries pecan_ID (Phase 20 output), so pecan_ID is excluded
  from the statistics using the same mechanism program 17 applies to PRECEDE_STUDY_ID and
  ENCRYPTED_MRN (see 21-01-SUMMARY for which mechanism applied).
  **Column check:** An in-program assertion confirms every variable program 17 reads by name exists in
  g.analytic_cohort; the base row-count expectation is updated to 13,890.
  **Population shift:** PENDING -- summarized at the Task 3 checkpoint from qc/17_pcm_d20_compare.txt.
  **Attribution:** Gerard, 2026-09-23
  </action>
  <verify>
    <automated>findstr /n "PCM-D-20" "C:\Master_Renamed_same_format_accross\docs\DECISIONS.md"</automated>
    Also run: findstr /n "PCM-D-19" "C:\Master_Renamed_same_format_accross\docs\DECISIONS.md"
  </verify>
  <acceptance_criteria>
    - findstr returns at least one line containing "PCM-D-20"
    - findstr returns at least one line containing "PCM-D-19"
    - Both entries include "2026-09-23" as the date
    - PCM-D-20 entry mentions "Reverses" D-10 and "g.analytic_cohort"
    - PCM-D-19 entry mentions "DOMAIN_MAP_APPROVED" and "D3"
  </acceptance_criteria>
  <done>DECISIONS.md contains PCM-D-19 and PCM-D-20 entries with full rationale and attribution</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run program 17, confirm D3 and the PCM-D-20 comparison, finalize DECISIONS.md</name>
  <what-built>Program 17 edits (Task 1) and draft PCM-D-19 / PCM-D-20 entries (Task 2).</what-built>
  <how-to-verify>
    Run program 17 standalone in a fresh SAS session AFTER 16b has produced g.analytic_cohort
    (the full runner in Plan 21-02 may be used instead).
    1. Log shows no ERROR and no %fail_out from the gate, the column-coverage assertion or GUARD 5.
    2. qc/17_summary_stats_by_domain.xlsx contains a D3 sheet with COGNITIVE_SCORE and COGNITIVE_CATEGORY populated.
    3. pecan_ID does not appear in any statistics sheet.
    4. qc/17_pcm_d20_compare.txt exists (if g.analysis_base is present); note row counts, PRECEDE overlap
       in both directions, and the main differing variables.
    Then fill in the PENDING lines in docs/DECISIONS.md: PCM-D-19 Result (D3 confirmed, date) and
    PCM-D-20 Population shift (counts from the comparison file).
  </how-to-verify>
  <resume-signal>Type "approved" with the D3 result and comparison counts, or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:
1. findstr /n "DOMAIN_MAP_APPROVED = 1" sas\17_summary_stats_by_domain.sas -- must return a hit
2. findstr /n "analytic_cohort" sas\17_summary_stats_by_domain.sas -- must return hits in code body
3. SUMMARY states how pecan_ID is excluded (regex or DATALINES row matching existing identifiers)
4. findstr /n "PCM-D-19\|PCM-D-20" docs\DECISIONS.md -- both must return hits
5. Task 3 checkpoint: program 17 run confirms the D3 sheet; DECISIONS.md PENDING lines filled in
</verification>

<success_criteria>
- DOMAIN_MAP_APPROVED = 1 at line 145 of program 17 (was 0)
- g.analysis_base replaced by g.analytic_cohort in all data-reading references in program 17
- Keyed comparison (%pcm_d20_compare) writes to qc/17_pcm_d20_compare.txt, guarded with %sysfunc(exist())
- pecan_ID excluded via the existing identifier mechanism (SUMMARY records which)
- PCM-D-19 and PCM-D-20 both recorded in docs/DECISIONS.md, with PENDING lines filled in after the run
- FIX-01 satisfied: the regenerated workbook is confirmed to contain a populated D3 sheet
</success_criteria>

<output>
After completion, create .planning/phases/21-runner-wiring-d3-fix/21-01-SUMMARY.md
</output>
