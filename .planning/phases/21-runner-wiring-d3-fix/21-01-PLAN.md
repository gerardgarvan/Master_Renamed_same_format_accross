---
phase: 21-runner-wiring-d3-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sas/17_summary_stats_by_domain.sas
  - docs/DECISIONS.md
autonomous: true
requirements:
  - FIX-01

must_haves:
  truths:
    - "DOMAIN_MAP_APPROVED is set to 1 at line 145 of 17_summary_stats_by_domain.sas with a PCM-D-20 attribution comment"
    - "A PECAN_ID exclusion DATALINES row exists in program 17's domain_lookup with assign_rule=OUT_OF_SCOPE"
    - "Program 17 reads g.analytic_cohort as its primary source instead of g.analysis_base, preceded by a PROC COMPARE that documents the difference"
    - "PCM-D-20 is recorded in docs/DECISIONS.md with rationale, date, and the reversed D-10 note"
    - "docs/DECISIONS.md entry for PCM-D-19 approves DOMAIN_MAP_APPROVED=1 and references this plan"
  artifacts:
    - path: "sas/17_summary_stats_by_domain.sas"
      provides: "Modified program with gate=1, input redirect, PECAN_ID row, PROC COMPARE block"
      contains: "DOMAIN_MAP_APPROVED = 1"
    - path: "docs/DECISIONS.md"
      provides: "PCM-D-19 and PCM-D-20 decision records"
      contains: "PCM-D-20"
  key_links:
    - from: "sas/17_summary_stats_by_domain.sas line ~6"
      to: "g.analytic_cohort"
      via: "libname g reference in PROC COMPARE and data step"
      pattern: "analytic_cohort"
    - from: "sas/17_summary_stats_by_domain.sas line 145"
      to: "Sections 5-11 gate"
      via: "%gate_stats macro"
      pattern: "DOMAIN_MAP_APPROVED = 1"
---

<objective>
Fix the D3 cognitive domain gate (FIX-01) and implement the PCM-D-20 input redirect for program 17.

Purpose: DOMAIN_MAP_APPROVED=0 at line 145 prevents program 17 from producing the xlsx workbook with
the D3 sheet. g.analysis_base is a P:-drive artifact not produced by the pipeline (Q1 research finding).
PCM-D-20 redirects program 17 to read g.analytic_cohort instead, with a PROC COMPARE validation block
to document the difference before the redirect takes effect.

Output:
- sas/17_summary_stats_by_domain.sas modified (gate flip, input redirect, PECAN_ID exclusion row, PROC COMPARE)
- docs/DECISIONS.md updated with PCM-D-19 and PCM-D-20 entries
</objective>

<execution_context>
@C:\Users\Owner\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Owner\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-runner-wiring-d3-fix/21-CONTEXT.md

<interfaces>
From sas/17_summary_stats_by_domain.sas (key lines):
  Line 6:   "g.analysis_base" -- input dataset reference in program header (Reads: section)
  Line 145: %let DOMAIN_MAP_APPROVED = 0;   <- gate to flip to 1
  Line 1666-end: data work.domain_lookup; DATALINES block
  Line 1711: COGNITIVE_SCORE,D3,instrument,...   (already correct, no change)
  Line 1712: COGNITIVE_CATEGORY,D3,instrument,...  (already correct, no change)

From sas/00_config.sas:
  Line 21:  %let g_path = P:\PeCAN Master Data\Gerard\...\merge;
  libname g is assigned by programs that use it; the executor must verify
  that program 17 assigns libname g before the PROC COMPARE step.

PCM-D-20 (planning_context decision):
  - Redirect program 17 to read g.analytic_cohort instead of g.analysis_base
  - Run PROC COMPARE between g.analysis_base and g.analytic_cohort BEFORE the redirect
    to document the difference (rows, columns, PRECEDE overlap)
  - Add PECAN_ID exclusion DATALINES row (reverses D-10)
  - Confirm every column program 17 uses exists in g.analytic_cohort
  - Record in DECISIONS.md as PCM-D-20

PCM-D-19 (CONTEXT.md D-09):
  - DOMAIN_MAP_APPROVED=0 -> 1; comment points to PCM-D-19 in DECISIONS.md
  - Record PCM-D-19 in DECISIONS.md: approves gate, supersedes v1 checkpoint-2

DATALINES rules: no em dashes (use --), no commas inside description field, ASCII only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip gate, redirect input, add PROC COMPARE and PECAN_ID row in program 17</name>
  <files>sas/17_summary_stats_by_domain.sas</files>
  <read_first>
    - sas/17_summary_stats_by_domain.sas (full file -- especially lines 1-30 for header/Reads section, line 145 for gate, lines 1666-1730 for DATALINES block)
    - sas/00_config.sas lines 1-68 (confirm g_path and libname assignment pattern)
  </read_first>
  <action>
Make four targeted edits to sas/17_summary_stats_by_domain.sas. Do NOT restructure or reorder any other code.

EDIT 1 -- Header "Reads" section (approx line 22-24):
Change:
  "  Reads   : g.analysis_base            (read-only)"
To:
  "  Reads   : g.analytic_cohort          (read-only; redirected from g.analysis_base per PCM-D-20 2026-09-23)"

EDIT 2 -- Gate flip at line 145:
Change:
  "%let DOMAIN_MAP_APPROVED = 0;"
To:
  "%let DOMAIN_MAP_APPROVED = 1;  /* PCM-D-19 approved 2026-09-23 -- see DECISIONS.md */"

EDIT 3 -- Add PROC COMPARE block immediately AFTER the %include "00_config.sas" line and BEFORE Section 0 content begins (insert after approximately line 130, before the init_pipeline_flag macro block). The block must be wrapped in a %macro so it is not open-code %IF:

  /* =========================================================================
     PCM-D-20 VALIDATION: Compare g.analysis_base with g.analytic_cohort
     Run once before the redirect to document the difference.
     If g.analysis_base does not exist on this machine (P: artifact), this
     proc will error gracefully -- check the log for NOBS differences.
     ========================================================================= */
  %macro pcm_d20_compare;
    %if %sysfunc(exist(g.analysis_base)) %then %do;
      proc compare base=g.analysis_base compare=g.analytic_cohort
                   criterion=0.00001 listall maxprint=(40,32);
        title "PCM-D-20: g.analysis_base vs g.analytic_cohort (pre-redirect audit)";
      run;
      title;
    %end;
    %else %do;
      %put NOTE: [PCM-D-20] g.analysis_base not found on this machine -- skipping PROC COMPARE. Using g.analytic_cohort as primary input per PCM-D-20.;
    %end;
  %mend pcm_d20_compare;
  %pcm_d20_compare;

EDIT 4 -- DATALINES block: add PECAN_ID exclusion row (per PCM-D-20 which reverses D-10).
Find the DATALINES block in data work.domain_lookup (line ~1673 onward).
Insert one new row immediately after the last D1 block row (after ZIPCODE or STATE row) or
as a standalone row before the D2 block. Exact insertion point: after the CANCER,D2 row
(line ~1710) and before the COGNITIVE_SCORE,D3 row (line 1711). Use this exact text:
  PECAN_ID,OUT_OF_SCOPE,identifier,patient linkage key -- not a clinical variable -- excluded per PCM-D-20 2026-09-23

Confirm that lines 1711-1712 remain exactly:
  COGNITIVE_SCORE,D3,instrument,named cognitive instrument score -- instrument membership overrides timing
  COGNITIVE_CATEGORY,D3,instrument,named cognitive instrument category -- instrument membership overrides timing

Also find every occurrence of "g.analysis_base" in the file body (outside the header comment and
the new PCM-D-20 PROC COMPARE block) and replace with "g.analytic_cohort".
Search for patterns like: "set g.analysis_base", "from g.analysis_base", "data=g.analysis_base"
Replace each with the analytic_cohort equivalent.
  </action>
  <verify>
    <automated>findstr /n "DOMAIN_MAP_APPROVED = 1" "C:\Master_Renamed_same_format_accross\sas\17_summary_stats_by_domain.sas"</automated>
    Also run: findstr /n "analytic_cohort" "C:\Master_Renamed_same_format_accross\sas\17_summary_stats_by_domain.sas"
    Also run: findstr /n "PECAN_ID" "C:\Master_Renamed_same_format_accross\sas\17_summary_stats_by_domain.sas"
    Also run: findstr /n "pcm_d20_compare" "C:\Master_Renamed_same_format_accross\sas\17_summary_stats_by_domain.sas"
  </verify>
  <acceptance_criteria>
    - findstr returns at least one line containing "DOMAIN_MAP_APPROVED = 1" (not 0)
    - findstr for "analytic_cohort" returns hits in the data-reading portions of the program
    - findstr for "PECAN_ID" returns a DATALINES row containing "OUT_OF_SCOPE"
    - findstr for "pcm_d20_compare" returns the macro definition and call
    - No remaining "g.analysis_base" references outside the header comment and the PROC COMPARE base= argument
    - The DATALINES row for PECAN_ID uses only ASCII characters, no commas inside the description field (description uses -- not commas as separators)
  </acceptance_criteria>
  <done>Program 17 has gate=1, input redirected to g.analytic_cohort, PROC COMPARE audit block present, PECAN_ID OUT_OF_SCOPE DATALINES row inserted</done>
</task>

<task type="auto">
  <name>Task 2: Record PCM-D-19 and PCM-D-20 in DECISIONS.md</name>
  <files>docs/DECISIONS.md</files>
  <read_first>
    - docs/DECISIONS.md (full file -- find the highest existing decision number and the formatting convention)
  </read_first>
  <action>
Append two new decision records to docs/DECISIONS.md, after the last existing entry. Use the same
formatting convention already in the file (horizontal rule separator if present, same heading level).

PCM-D-19 entry:
  ### PCM-D-19 -- DOMAIN_MAP_APPROVED Gate Approval (2026-09-23)
  **Status:** APPROVED
  **Decision:** Set DOMAIN_MAP_APPROVED = 1 in sas/17_summary_stats_by_domain.sas line 145.
  **Rationale:** The DATALINES rows for COGNITIVE_SCORE and COGNITIVE_CATEGORY (lines 1711-1712) were
  confirmed correct at domain=D3 assign_rule=instrument. The gate was previously held at 0 pending
  Checkpoint 1 review (v1.0). That review is now complete. This approval supersedes the v1 checkpoint-2
  approval which was made when D3 was absent from the workbook.
  **Effect:** Sections 5-11 of program 17 now execute, producing qc/17_summary_stats_by_domain.xlsx
  with a D3 sheet containing COGNITIVE_SCORE and COGNITIVE_CATEGORY.
  **Attribution:** Gerard, 2026-09-23

PCM-D-20 entry:
  ### PCM-D-20 -- Program 17 Input Redirect: g.analysis_base -> g.analytic_cohort (2026-09-23)
  **Status:** APPROVED (Option B from D-07a discussion)
  **Decision:** Program 17 now reads g.analytic_cohort as its primary source instead of g.analysis_base.
  A PROC COMPARE block (%pcm_d20_compare macro) runs before the main program body to document the
  difference (rows, columns, PRECEDE overlap) when g.analysis_base is available on the machine.
  **Rationale:** g.analysis_base is a P:-drive artifact not produced by any program in this repo.
  A clean end-to-end run per RUN-01 requires all inputs to be pipeline-produced. g.analytic_cohort
  (produced by 16b_cohort_rebuild.sas) is the correct replacement: same cohort definition,
  174 columns, 13,890 rows.
  **Reverses:** D-10 (which stated no PECAN_ID DATALINES row was needed). g.analytic_cohort carries
  pecan_ID (Phase 20 output), so a PECAN_ID OUT_OF_SCOPE exclusion row is added to the domain_lookup
  DATALINES to prevent it from entering the domain assignment flow.
  **Column check:** All columns program 17 reads from the base dataset must be confirmed present in
  g.analytic_cohort. The executor must grep program 17 for every dataset reference and verify.
  **Attribution:** Gerard, 2026-09-23
  </action>
  <verify>
    <automated>findstr /n "PCM-D-20" "C:\Master_Renamed_same_format_accross\docs\DECISIONS.md"</automated>
    Also run: findstr /n "PCM-D-19" "C:\Master_Renamed_same_format_accross\docs\DECISIONS.md"
  </verify>
  <acceptance_criteria>
    - findstr returns at least one line containing "PCM-D-20"
    - findstr returns at least one line containing "PCM-D-19"
    - Both entries include "2026-09-23" as the date
    - PCM-D-20 entry mentions "Reverses" D-10 and "g.analytic_cohort"
    - PCM-D-19 entry mentions "DOMAIN_MAP_APPROVED" and "D3"
  </acceptance_criteria>
  <done>DECISIONS.md contains PCM-D-19 and PCM-D-20 entries with full rationale and attribution</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. findstr /n "DOMAIN_MAP_APPROVED = 1" sas\17_summary_stats_by_domain.sas -- must return a hit
2. findstr /n "analytic_cohort" sas\17_summary_stats_by_domain.sas -- must return hits in code body
3. findstr /n "PECAN_ID.*OUT_OF_SCOPE" sas\17_summary_stats_by_domain.sas -- must return a DATALINES row
4. findstr /n "PCM-D-19\|PCM-D-20" docs\DECISIONS.md -- both must return hits
5. (Optional, if SAS available) Run program 17 in SAS and confirm qc\17_summary_stats_by_domain.xlsx is produced with a D3 sheet
</verification>

<success_criteria>
- DOMAIN_MAP_APPROVED = 1 at line 145 of program 17 (was 0)
- g.analysis_base replaced by g.analytic_cohort in all data-reading references in program 17
- PROC COMPARE audit block (%pcm_d20_compare) present and calls correctly guarded with %sysfunc(exist())
- PECAN_ID,OUT_OF_SCOPE DATALINES row present in work.domain_lookup
- PCM-D-19 and PCM-D-20 both recorded in docs/DECISIONS.md
- FIX-01 requirement satisfied: program 17 can produce the D3 sheet when run
</success_criteria>

<output>
After completion, create .planning/phases/21-runner-wiring-d3-fix/21-01-SUMMARY.md
</output>
