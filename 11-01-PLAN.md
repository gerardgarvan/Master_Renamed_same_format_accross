---
phase: 11-fix-yellow-highlighted-sections-in-most-recent-word-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Osborne1232026.R
  - scratch_diag_11.R
autonomous: false
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-08

must_haves:
  truths:
    - "The actual levels of df$syphilis_bucket are recorded in the summary, verbatim"
    - "The three Syphilis Category rows account for every non-NA syphilis_bucket row within each adequate_tx group (no silently dropped level)"
    - "AdeqTx_No/AdeqTx_Yes Syphilis Category percentages use denominators equal to the column headers (No=79, Yes=39) or the documented D-08 alternative"
    - "Follow-up Visit Tri_p in table5_data is a computed p-value string, not empty, not XXX, and not simulation-dependent"
    - "No literal hard-coded values remain for AdeqTx Syphilis Category rows"
  artifacts:
    - path: "scratch_diag_11.R"
      provides: "Data-level diagnostic that prints the real crosstab before any edit is made"
      contains: "useNA\\s*=\\s*\"ifany\""
    - path: "Osborne1232026.R"
      provides: "Corrected AdeqTx re-derivation block and computed Follow-up Visit Tri_p"
      contains: "tri_fu_p"
  key_links:
    - from: "AdeqTx helper block (~L474-555)"
      to: "table5_data tibble (~L560-578)"
      via: "AdeqTx_No / AdeqTx_Yes / AdeqTx_p vectors"
      pattern: "AdeqTx_No\\s*=\\s*AdeqTx_No"
    - from: "Tri_p in table5_data"
      to: "Follow-up Visit row"
      via: "tri_fu_p computed variable"
      pattern: "tri_fu_p"
---

<objective>
Fix two bugs in Osborne1232026.R that produce yellow-highlighted wrong values in Table 5 of
Osborne_Figures_and_Tables_July_31_2026.docx:

1. Syphilis Category rows (Less likely / Possible / Proven) under the Adequacy of Syphilis
   Treatment columns (AdeqTx_No / AdeqTx_Yes) show wrong counts AND wrong percentages.
   The root cause is diagnosed from data before any edit is made — see Task 1.

2. The Follow-up Visit row has a missing/placeholder p-value in the Tri_p column. Replace it
   with a deterministic chi-square (or exact Fisher) p-value of followed_up by
   trimester_first_visit, formatted via fmt_p().

Purpose: Produce correct R code so that regeneration in Plan 02 yields a docx with no
yellow-highlighted errors.

Output: Corrected Osborne1232026.R + scratch_diag_11.R. No docx regeneration in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-fix-yellow-highlighted-sections-in-most-recent-word-docs/11-CONTEXT.md
@.planning/phases/11-fix-yellow-highlighted-sections-in-most-recent-word-docs/11-CONTEXT-AMENDMENTS.md

<prior_analysis>
<!-- Arithmetic done on the July_31 values BEFORE this plan was written. -->
<!-- Do not re-derive; use it to aim the diagnostic. -->

<!-- Displayed values: Less likely 3(4%)/8(21%), Possible 65(84%)/29(74%), Proven 0(0%)/0(0%) -->

<!-- Yes column: 8/39 = 20.5% -> "21%".  29/39 = 74.4% -> "74%". -->
<!--   The Yes DENOMINATOR (39) IS ALREADY CORRECT. Counts sum to 37, not 39. -->

<!-- No column: 3/79 = 3.8%, 65/79 = 82.3% -- but 84% is displayed. -->
<!--   3/77 = 3.9%, 65/77 = 84.4% -> the No denominator is 77, not 79. -->
<!--   Counts sum to 68 of those 77. -->

<!-- CONCLUSION: two distinct defects, and the denominator is the SMALLER one. -->
<!--   (a) ~11 No-group rows and ~2 Yes-group rows fall into NONE of the three -->
<!--       levels named in syph_levels. Combined with Proven = 0/0 in a congenital -->
<!--       syphilis cohort (not a plausible data fact), this points hard at a -->
<!--       LEVEL-STRING MISMATCH: syphilis_bucket has a level whose label is not -->
<!--       literally "Proven" (e.g. "Confirmed", "Proven/Confirmed", "Confirmed case"), -->
<!--       or a fourth level exists that is being silently dropped. -->
<!--   (b) 2 No-group rows have NA syphilis_bucket, shrinking colSums to 77. -->

<!-- A denominator-only fix (77 -> 79) does NOT fix this. It moves 84% to 82% -->
<!-- and leaves Proven at 0. Do not apply a denominator-only fix. -->
</prior_analysis>

<interfaces>
<!-- fmt_p() signature (already defined in Osborne1232026.R, reuse as-is): -->
<!-- fmt_p(p) -> character: 3dp, no leading zero, "<.001" floor -->

<!-- AdeqTx helper block entry point (~L474): -->
<!-- df_t5 <- df  # full sample alias; adequate_tx factor levels c("No","Yes") -->

<!-- Syphilis Category block (~L504-519): -->
<!-- syph_tbl <- table(df_t5$syphilis_bucket, df_t5$adequate_tx) -->
<!-- syph_n_no  <- colSums(syph_tbl)["No"] -->
<!-- syph_n_yes <- colSums(syph_tbl)["Yes"] -->
<!-- syph_levels <- c("Less likely", "Possible", "Proven") -->

<!-- Follow-up Visit block (~L526-535): -->
<!-- fu_tbl <- table(df_t5$followed_up, df_t5$adequate_tx) -->
<!-- fu_p_val <- fmt_p(chisq.test(fu_tbl)$p.value) -->

<!-- table5_data tibble (~L560-578): -->
<!-- Tri_p = c(".659", ".352", ".200", "", "", "", ".777", "", "", "") -->
<!--   1=Birthweight, 2=GA, 3=Syphilis Category header, 4=Less likely, -->
<!--   5=Possible, 6=Proven, 7=RPR Titer, 8=Follow-up Visit header, 9=No, 10=Yes -->
<!-- Position 8 must become the computed followed_up x trimester_first_visit p-value -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Establish ground truth about the source file (no edits)</name>
  <files>Osborne1232026.R</files>
  <action>
Three facts must be recorded before any edit. Run each and paste the output into the summary.

FACT 1 — Does the literal "XXX" actually exist in the R source?
  Select-String -Path "Osborne1232026.R" -Pattern "XXX"

  CONTEXT D-05 claims XXX is hard-coded in table5_data, but the interfaces block above
  shows position 8 as "". These cannot both be true. If this search returns NOTHING, the
  XXX in the July_31 docx was typed in Word by a reviewer and the R source is simply blank
  there. Record which is the case. This determines whether the "no XXX" checks in Plan 02
  are meaningful or vacuous, and D-05 in CONTEXT.md must be corrected either way.

FACT 2 — What is the exact trimester variable name?
  Select-String -Path "Osborne1232026.R" -Pattern "trimester"

  Record the exact column name (trimester_first_visit, trimester, pnc_trimester, ...).
  Do not assume trimester_first_visit exists.

FACT 3 — Is the AdeqTx helper block a bare `{ ... }` or a `local({ ... })`?
  Read Osborne1232026.R lines 470-580 in full and record which.

  This matters for Plan 02: variables inside local() are invisible to the harness. The
  Plan 02 harness has been written to read values out of `table5_data` (a top-level object)
  rather than out of block internals, so either form is acceptable — but record it, and do
  NOT change a bare block into local() or vice versa.
  </action>
  <verify>
All three facts are written into 11-01-SUMMARY.md verbatim, including the exact
trimester column name and whether "XXX" appears in the R source.
  </verify>
  <done>
FACT 1, FACT 2, FACT 3 recorded. No files modified.
  </done>
</task>

<task type="auto">
  <name>Task 1: Diagnose the Syphilis Category bug FROM DATA (no edits yet)</name>
  <files>scratch_diag_11.R</files>
  <action>
This is a data bug. It cannot be diagnosed by static analysis and must not be guessed at.
Create scratch_diag_11.R in C:\Users\Owner\Documents\Osborne with exactly this content,
then run it and paste the full output into the summary.

---- scratch_diag_11.R content ----

# scratch_diag_11.R -- Phase 11 read-only diagnostic. Makes NO edits.
# Run: & "C:/Program Files/R/R-4.5.2/bin/Rscript.exe" scratch_diag_11.R

suppressPackageStartupMessages({ library(dplyr) })

if (!file.exists("OsborneData2.xlsx")) {
  stop("OsborneData2.xlsx not found -- this diagnostic requires the PHI data file. ",
       "Run it on the machine that has the data. Do not proceed to Task 2 without it.")
}

env_d <- new.env()
source("Osborne1232026.R", local = env_d)
df <- env_d$df

cat("\n===== 1. syphilis_bucket levels (VERBATIM, quoted) =====\n")
print(levels(factor(df$syphilis_bucket)))
cat("\nclass: ", class(df$syphilis_bucket), "\n")

cat("\n===== 2. adequate_tx levels and counts (NA shown) =====\n")
print(table(df$adequate_tx, useNA = "ifany"))

cat("\n===== 3. FULL crosstab, NA rows and columns shown =====\n")
print(table(df$syphilis_bucket, df$adequate_tx, useNA = "ifany"))

cat("\n===== 4. Column totals, non-NA bucket only vs. all rows =====\n")
tb <- table(df$syphilis_bucket, df$adequate_tx)
cat("colSums(crosstab)  : "); print(colSums(tb))
cat("adequate_tx totals : No =", sum(df$adequate_tx == "No", na.rm = TRUE),
    " Yes =", sum(df$adequate_tx == "Yes", na.rm = TRUE), "\n")
cat("rows with adequate_tx set but syphilis_bucket NA:\n")
print(table(df$adequate_tx[is.na(df$syphilis_bucket)], useNA = "ifany"))

cat("\n===== 5. Do the three hard-coded labels match the real levels? =====\n")
syph_levels <- c("Less likely", "Possible", "Proven")
real <- levels(factor(df$syphilis_bucket))
cat("hard-coded : "); print(syph_levels)
cat("in data    : "); print(real)
cat("MISSING from data (hard-coded label that matches nothing): ")
print(setdiff(syph_levels, real))
cat("UNCLAIMED in data (real level no hard-coded label captures): ")
print(setdiff(real, syph_levels))

cat("\n===== 6. Trimester variable =====\n")
cat("candidate columns: ")
print(grep("trimester", names(df), value = TRUE, ignore.case = TRUE))

cat("\n===== 7. followed_up x trimester crosstab (fill in name from #6) =====\n")
tri_col <- grep("trimester", names(df), value = TRUE, ignore.case = TRUE)[1]
if (!is.na(tri_col)) {
  print(table(df$followed_up, df[[tri_col]], useNA = "ifany"))
  cat("\nexpected counts (chisq):\n")
  print(suppressWarnings(chisq.test(table(df$followed_up, df[[tri_col]]))$expected))
} else {
  cat("NO trimester column found -- STOP and report.\n")
}

cat("\n===== DIAGNOSTIC COMPLETE =====\n")

---- end of file content ----

INTERPRETING THE OUTPUT — apply in this order:

PRIMARY (expected, per prior_analysis): section 5 shows a non-empty UNCLAIMED or MISSING set.
  That is the bug. The fix is to align syph_levels with the real level strings, keeping the
  three displayed row labels in table5_data unchanged if the underlying level string differs
  (e.g. map real level "Confirmed" -> displayed row "Proven"). Do not rename data values.
  Prefer an explicit named mapping so the intent is legible:
    syph_levels <- c("Less likely" = "<real level>", "Possible" = "<real level>",
                     "Proven" = "<real level>")
  and index the table by the mapped value while labelling with the name.

SECONDARY: section 4 shows colSums smaller than the adequate_tx totals -> NA syphilis_bucket
  rows exist. Compute denominators from adequate_tx directly:
    syph_n_no  <- sum(df_t5$adequate_tx == "No",  na.rm = TRUE)
    syph_n_yes <- sum(df_t5$adequate_tx == "Yes", na.rm = TRUE)
  BUT SEE D-08 BELOW — this alone changes what the percentages mean.

D-08 GATE (STOP CONDITION): if section 4 shows any rows with adequate_tx set but
  syphilis_bucket NA, then the three category rows CANNOT sum to 79/39 while also being
  honest. You have three options and you are NOT authorised to pick one:
    (i)  denominator = 79/39, three rows sum to less than the denominator (percentages
         are "% of all in group", the missing rows are implicitly unreported);
    (ii) denominator = non-missing only (77/39), rows sum to the denominator, and the
         column header N or a footnote must say so;
    (iii) add an explicit "Missing" row so the rows sum to 79/39.
  STOP, report the NA counts to Gerard, and record his choice as D-08 in CONTEXT.md before
  continuing. If section 4 shows zero NA rows, D-08 is moot — record "no missing, moot".

Only after the above: make targeted edits to the AdeqTx helper block in Osborne1232026.R
(~L504-519). Preserve the block's structure and all other rows (Birthweight, GA, RPR Titer,
Follow-up Visit). Do not leave diagnostic message()/cat() calls in Osborne1232026.R —
scratch_diag_11.R is where diagnostics live.
  </action>
  <verify>
Re-run the diagnostic after the edit is applied, plus this check:

  & "C:/Program Files/R/R-4.5.2/bin/Rscript.exe" -e "e<-new.env(); source('Osborne1232026.R', local=e); print(e$table5_data[4:6, grep('AdeqTx', names(e$table5_data))])"

The three printed rows must show non-zero counts for all three categories (in particular
Proven must NOT be 0/0), and the counts must be consistent with the crosstab in section 3
of the diagnostic output. Paste both outputs into the summary.

A grep for a code pattern is NOT acceptable verification for this task.
  </verify>
  <done>
The real syphilis_bucket levels are recorded verbatim; the level-mismatch (and NA handling,
if D-08 applied) is fixed in the helper block; the re-derived AdeqTx Syphilis Category values
match the crosstab cell-for-cell; Proven is no longer 0/0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compute Follow-up Visit Tri_p deterministically</name>
  <files>Osborne1232026.R</files>
  <action>
STEP 1: In the AdeqTx helper block, AFTER the fu_tbl block (~L526-535) and BEFORE the
AdeqTx_No assembly (~L538), add:

  # Trimester x Follow-up p-value (Tri_p column, Follow-up Visit row)
  tri_fu_tbl <- table(df_t5$followed_up, df_t5$<TRIMESTER_COL>)
  tri_fu_exp <- suppressWarnings(chisq.test(tri_fu_tbl)$expected)
  if (any(tri_fu_exp < 5, na.rm = TRUE)) {
    tri_fu_p <- fmt_p(fisher.test(tri_fu_tbl)$p.value)
  } else {
    tri_fu_p <- fmt_p(chisq.test(tri_fu_tbl)$p.value)
  }

<TRIMESTER_COL> is the exact name confirmed in Task 0 FACT 2 / diagnostic section 6.

Two deliberate differences from the earlier draft of this plan, do not revert them:

  - NO simulate.p.value. A 2xK table this size is computed exactly by fisher.test. With
    simulate.p.value = TRUE and no seed, every regeneration would print a slightly different
    p-value into FINAL.docx, which makes the deliverable non-reproducible and defeats any
    diff-based verification. If fisher.test errors with a workspace complaint, raise
    workspace (e.g. workspace = 2e7); only if that also fails, use
    simulate.p.value = TRUE with an immediately preceding set.seed(20260901) and record
    the seed in the summary.

  - suppressWarnings on the expected-counts call. chisq.test emits a chi-squared
    approximation warning on sparse tables. That warning is expected and is NOT the
    "must exit 0" failure condition in Plan 02 Task 1 — do not "fix" it by changing the test.

STEP 2: In table5_data (~L566) change position 8 only:
  Tri_p = c(".659", ".352", ".200", "", "", "", ".777", tri_fu_p, "", "")

Do NOT change positions 1-7 or 9-10 (D-01: only fix yellow-highlighted cells). Note that
those remaining literals are known tech debt — see D-09 in CONTEXT-AMENDMENTS.md — but they
are out of scope here.

If Task 0 FACT 3 found the helper block is local({...}), tri_fu_p will not be visible at
table5_data's scope. In that case return the value from the block rather than converting
the block: have the block's last expression be a list, or assign
tri_fu_p into the enclosing scope explicitly. Record what you did.
  </action>
  <verify>
  & "C:/Program Files/R/R-4.5.2/bin/Rscript.exe" -e "e<-new.env(); source('Osborne1232026.R', local=e); cat('Tri_p[8] = [', e$table5_data$Tri_p[8], ']\n')"

Must print a p-value string (e.g. ".042", "<.001"). Must NOT print an empty string, "XXX",
or error. Run it TWICE and confirm the two runs print the identical value (reproducibility).
  </verify>
  <done>
table5_data$Tri_p[8] is a computed, deterministic p-value string derived from
followed_up x <TRIMESTER_COL> via chisq.test or exact fisher.test, formatted with fmt_p().
Two consecutive runs produce byte-identical values.
  </done>
</task>

</tasks>

<verification>
Run from C:\Users\Owner\Documents\Osborne. Every check is data-level; grep alone does not
constitute verification for this phase.

1. Diagnostic reruns clean:
   & "C:/Program Files/R/R-4.5.2/bin/Rscript.exe" scratch_diag_11.R
   Section 5 shows empty MISSING and UNCLAIMED sets.

2. Syphilis Category values are real:
   & "C:/Program Files/R/R-4.5.2/bin/Rscript.exe" -e "e<-new.env(); source('Osborne1232026.R', local=e); print(e$table5_data[4:6, ])"
   Proven row is not 0(0%)/0(0%). Counts match diagnostic section 3.

3. Tri_p position 8 populated and stable (run twice, compare):
   & "C:/Program Files/R/R-4.5.2/bin/Rscript.exe" -e "e<-new.env(); source('Osborne1232026.R', local=e); cat(e$table5_data$Tri_p[8])"

4. Tri_p positions 1-7 and 9-10 unchanged from before this plan:
   compare against git diff or the pre-edit copy of the line.

5. No diagnostic cat()/message() left in Osborne1232026.R.
</verification>

<success_criteria>
- The real syphilis_bucket level strings are recorded verbatim in the summary
- syph_levels aligns with those real levels; no level is silently dropped
- Proven row is non-zero in both AdeqTx columns
- D-08 resolved (either "no missing, moot" or Gerard's recorded choice)
- table5_data$Tri_p[8] is a deterministic computed p-value
- No simulate.p.value without a recorded set.seed()
- All other table5_data values unchanged (no regressions to prior phase fixes)
- FACT 1 recorded: whether "XXX" exists in the R source at all
</success_criteria>

<output>
Create `.planning/phases/11-fix-yellow-highlighted-sections-in-most-recent-word-docs/11-01-SUMMARY.md`
containing:
- FACT 1 / FACT 2 / FACT 3 answers verbatim
- Full scratch_diag_11.R output (before and after the fix)
- The real syphilis_bucket levels, quoted
- Which defect(s) were found and the exact edit applied for each
- D-08 resolution (moot, or Gerard's choice and date)
- The observed tri_fu_p value and confirmation it was identical across two runs
- Confirmation that Tri_p positions 1-7, 9-10 were not touched
</output>
